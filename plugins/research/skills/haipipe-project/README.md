haipipe-project
===============

Skill for creating, reviewing, and summarizing haipipe research projects.
Covers both the experiment side (examples/) and the code side (code/ + code-dev/)
in one unified flow.

---

Commands
--------

  /haipipe-project new
      Start a new project from scratch. Asks a few questions (project ID,
      dataset, stages, new code needed?), then scaffolds the full folder
      structure under examples/ and optionally creates Track A stubs in
      code-dev/ and code/hainn/. Checks code/INDEX.md first to suggest
      reusing existing Fns or models across projects.

  /haipipe-project review
  /haipipe-project review examples/ProjC-Model-WeightPredict
      Inspect an existing project AND generate any missing standard docs.
      Auto-detects the current project from git status if no path is given.

      What it checks (read-only):
        - Project naming convention
        - Folder structure (mandatory: tasks, docs, paper; optional: cc-archive, docs)
        - cc-archive session history presence (if folder exists)
        - Task-level config YAML naming and stage prefixes
        - Task-result alignment (task folders, run-result pairing)
        - Code sync: config FnClass/ModelClass -> code/haifn/ + code/hainn/ resolution,
          YAML arg signatures, builder <-> generated file freshness,
          task imports -> codebase class existence

      What it generates (write access to docs/ and tasks/ only):
        - docs/TODO.md              created from project scan if missing; updated if exists
        - docs/data-map.md          pipeline flow diagram + stage table (always regenerated)
        - docs/dependency-report.md Fn/model deps, cross-project reuse, missing impls
        - tasks/INDEX.md            built from existing tasks if missing; synced if exists

      Never touches: task-level config/, code/, code-dev/, task *.py scripts, cc-archive/

  /haipipe-project summarize
  /haipipe-project summarize examples/ProjC-Model-WeightPredict
      Generate a plain-English post-development summary. Writes
      docs/project-summary.md with a short description, key results table,
      and an ASCII pipeline flow chart. Updates tasks/INDEX.md statuses.
      Designed to be readable by someone with zero prior context.

  /haipipe-project organize
  /haipipe-project organize examples/ProjC-Model-WeightPredict
      Inventory all project files (and related external Fn/model files),
      propose a reorganization to match the standard layout,
      and -- if the user approves -- apply moves then immediately verify
      that all imports and paths still work.

  /haipipe-project organize verify
  /haipipe-project organize verify examples/ProjC-Model-WeightPredict
      Skip inventory and proposal; run only the post-reorganization
      verification (import resolution, config FnClass check, relative paths).
      Use after manual file moves.

  /haipipe-project nb
  /haipipe-project nb examples/ProjC-Model-WeightPredict
      Create a demo or parameterized notebook inside a task folder.
      Demo: converts {task}.py to {task}.ipynb at task root.
      Run: creates a parameterized .ipynb in tasks/{task}/runs/.

  /haipipe-project help [question]
      Describe what you want in plain English. Routes your request to the
      right subskill and specific step, and suggests the exact call to make.
      Example: /haipipe-project help "I want to see what files are in this project"

---

What It Creates / Generates
-----------------------------

New projects (/haipipe-project new):

  examples/Proj{Series}-{Category}-{Num}-{Name}/
  +-- tasks/             Task-folder layout: tasks/{task}/ contains:
  |   |                    {task}.py (or notebook), config/, runs/, results/
  |   +-- INDEX.md           Mandatory task index (task, data, stage, description, status)
  +-- docs/              Planning and summary documents
  |   +-- TODO.md            Pre-filled pipeline progress tracker
  |   +-- project-summary.md Generated by /haipipe-project summarize
  +-- paper/             Paper drafts, figures, and submission materials (mandatory)
  +-- cc-archive/        CC session history -- cc_*.md, di_*.md (optional)

Existing projects (/haipipe-project review):

  Generates all docs/ files and tasks/INDEX.md from what already exists.
  No restructuring -- only adds what is missing.
      +-- docs/data-map.md          Pipeline flow diagram (from task-level config/ YAMLs)
      +-- docs/dependency-report.md Fn/model deps + cross-project reuse

Two shared indexes across the codebase:

  code/INDEX.md        All implemented Fns and models -- checked before
                       creating anything new (reuse across projects first)
  tasks/INDEX.md       Per-project task index by data + functionality

---

Standard Project Layout
-------------------------

  examples/Proj{Series}-{Category}-{Num}-{Name}/
  |
  |-- tasks/                   **Mandatory.** One subfolder per task.
  |   |-- INDEX.md             Task-level index (task, data, stage, description, status)
  |   +-- {task}/              Each task is self-contained:
  |       |-- {task}.py        Main task script (or notebook .ipynb)
  |       |-- config/          YAML pipeline configs for this task
  |       |-- runs/            Run scripts (shell scripts to launch task)
  |       +-- results/         Output from runs (metrics.json, report.md, etc.)
  |
  |-- docs/                    **Mandatory.** Planning and summary documents.
  |   |-- TODO.md              Pipeline progress tracker
  |   |-- data-map.md          Pipeline flow diagram (auto-generated)
  |   |-- dependency-report.md Fn/model dependency report (auto-generated)
  |   +-- project-summary.md   Human-readable summary (auto-generated)
  |
  |-- paper/                   **Mandatory.** Paper drafts, figures, LaTeX/Overleaf files.
  |
  |-- cc-archive/              **Optional.** CC session history (cc_*.md, di_*.md).
  |
  +-- docs/                    **Optional.** Additional documentation beyond the standard set.

Note: config/ is inside each task folder, not at the project top level.
Note: notebooks live inside their task folder as {task}.ipynb (not in a separate nb/ folder).

---

Write Access Summary
---------------------

  Command           Writes to                           Never touches
  ----------------  ----------------------------------  -------------------------
  new               examples/{id}/, code-dev/,          (creates new files only)
                    code/hainn/, code/INDEX.md
  review            docs/, tasks/INDEX.md               task-level config/, code/,
                                                        code-dev/
  summarize         docs/project-summary.md,            task-level config/, code/,
                    tasks/INDEX.md (status only)         code-dev/
  organize          docs/organize-report.md,            task-level config/, code/,
                    examples/{id}/ (moves only),        code-dev/, cc-archive/
                    tasks/INDEX.md (if renamed)          session files
  organize verify   docs/organize-report.md (append)    (read only -- no moves)
  help              (none -- read only, routing only)   (everything)

---

Skill Files
-----------

  SKILL.md                    Router and dispatch table
  ref/project-structure.md    Standard layout, naming rules, INDEX.md formats,
                              docs/ template, auto-example rule
  ref/code-structure.md       code-dev/ builder pattern, hainn/ model layout,
                              code/INDEX.md registry spec
  fn/fn-new.md                Scaffold flow (both tracks, reuse check)
  fn/fn-review.md             Gap analysis + doc generation + code sync check
  fn/fn-summarize.md          Post-development summary + ASCII flow chart
  fn/fn-organize.md           File inventory + reorganization proposal + verification
  fn/fn-nb.md                 Create demo or parameterized notebook inside a task folder
  fn/fn-help.md               Intent routing: natural-language -> subskill + step
