# learn-azure — Feedback Inbox

Feedback about the learn-azure wiki and diagrams, captured by `/learn-azure feedback "<text>"` or harvested by `/learn-azure digest`.

One file per item: `<YYYY-MM-DD>_<slug>.md` (`status: open|fixed`). Fix in a later revision pass; keep files as history (never delete).
