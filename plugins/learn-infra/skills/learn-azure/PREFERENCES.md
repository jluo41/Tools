# learn-azure — Behavioral Preferences (portable)

Global "how to work" preferences for this skill, kept HERE under git so they survive a machine change. ALWAYS read and honor these in a learn-azure session.

These are global BEHAVIORAL preferences (how the agent should act), NOT wiki defects — defects go to the `feedback/` inbox.

## Preferences

(none yet — add via `/learn-azure digest` when a global pref is flagged, or by hand)
