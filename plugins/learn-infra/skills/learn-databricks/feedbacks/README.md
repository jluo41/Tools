# learn-databricks — Feedback Inbox

Feedback about the learn-databricks lessons and journey docs, captured by `/learn-databricks feedback "<text>"` or harvested by `/learn-databricks digest`.

One file per item: `<YYYY-MM-DD>_<slug>.md` (`status: open|fixed`). Fix in a later revision pass; keep files as history (never delete).
