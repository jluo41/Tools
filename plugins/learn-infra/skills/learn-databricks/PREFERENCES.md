# learn-databricks — Behavioral Preferences (portable)

Global "how to work" preferences for this skill, kept HERE under git so they survive a machine change. ALWAYS read and honor these in a learn-databricks session.

These are global BEHAVIORAL preferences (how the agent should act), NOT lesson defects — defects go to the `feedbacks/` inbox.

## Preferences

(none yet — add via `/learn-databricks digest` when a global pref is flagged, or by hand)
