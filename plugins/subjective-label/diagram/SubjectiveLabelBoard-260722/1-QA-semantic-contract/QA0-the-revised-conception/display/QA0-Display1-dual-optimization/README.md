# QA0-Display1-dual-optimization

- claim: one calibration round is a DUAL optimization — the human concept `H_t` and the external annotation policy `Θ_t` improve together, and the seal is what keeps the machine's prelabels diagnostic rather than authoritative
- kind: algorithm, TeX-native (✒️ tex; `algorithm2e`, the recipe IS the float body)
- caption-job: give a reader the whole round in one object, in the order it happens, so the pages that own each step (QC1 · QC3 · QB2 · QB3 · QD4) can be read as parts of it rather than as five separate procedures
- fragility: none data-bound; the algorithm states no count, capacity or threshold, and `m_t` appears as a name the caller supplies — QA0 fixes no value, and the unit contract calls an invented number a defect
- renderer: CC as the TeX hand; rebuild = `xelatex preview.tex` from this folder
- picked: 260816 CC · one candidate, clean on the first compile
- accepted: ⬜ · awaiting JL's read of preview.pdf, the row no machine may tick
- history: the first display unit on this board; it replaces nothing — QA0 §8 keeps its ASCII `H_t ⇄ Θ_t` figure, and this unit states the round precisely beside it so the two can be compared before any ASCII is retired
