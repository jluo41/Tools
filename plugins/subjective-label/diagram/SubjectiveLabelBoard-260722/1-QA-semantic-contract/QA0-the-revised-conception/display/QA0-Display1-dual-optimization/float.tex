% CALLER-OWNED. The caption, the label and the placement are QA0's; the body is the
% renderer's and is inputted, never inlined. Numbers typed here would be a defect.
\begin{algorithm}[H]
\input{recipe/dual-optimization.tex}
\caption{One calibration round as a dual optimization. The human concept $H_t$ and the
  external annotation policy $\Theta_t$ improve together: the machine proposes and
  pre-labels under seal, the person rules blind, and only then is the seal opened so the
  disagreement can diagnose the policy rather than replace the judgment.}
\label{alg:qa0-dual-optimization}
\end{algorithm}
