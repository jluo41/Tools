% CALLER-OWNED. The caption, the label and the placement are QD4's; the body is
% inputted, never inlined. A threshold typed here would be a defect.
\begin{figure}[htbp]
\centering
\input{recipe/stop-gate.tex}
\caption{The stopping rule as a conjunction. Calibration stops only when all four gates
  hold at once, so a strong overall score cannot buy a failed class, an unmeasured
  region, or an unowned high-severity risk. Every threshold is project configuration
  fixed before the sequence is read, and a failed gate reopens the round rather than
  lowering the bar.}
\label{eq:qd4-stop-gate}
\end{figure}
