# 📋 板 01 · subjective-label — 给这套方法拿一个不是自己打的分数

**状态** 🟡 进行中 · 开板 2026-07-22 · 6 件事全做完就关板

> **这块板要干的事，一句话**：
> 引擎已经写好了，但从没在真实数据上跑过。
> 把它在公开数据上跑一次，拿到一个**不是自己给自己打的分数**。
> 做完这块板，我们才敢对外说这套方法有效。

```text
📊  6 件事 · ✅ 0 完成 · 🔧 4 待做 · 🧠 2 等 JL 拍板 · ░░░░░░ 0%
    ⚠️ 两个决定压着四天的活。不定，RA 只能干其中两件小的。
```

---

## 1 · 这个项目在做什么

```text
   病人在网上写的医生评论
        ↓
   给每条打一个人格特质标签：宜人性 / 尽责性 / 开放性 …
   每个标签三档：HIGH · LOW · NONE
        ↓
   产出两样东西：一份标注规则（guideline） + 一批标好的数据
```

请真人标太慢太贵，所以用**一组大模型**代替一队人类标注员。

## 2 · 为什么这件事难

「这条评论体现了高的宜人性」—— 这是**判断**，不是事实。
两个真人看同一条评论，也会打出不同的分。

所以没有标准答案可以对照。
我们只能想办法证明一件事：**我们标得跟真人一样好**。

## 3 · 我们已经做到哪一步

```text
   三个维度跑完了：
      B01  agreeableness   宜人性     κ = 0.937
      B02  conscientiousness 尽责性    κ = 0.937
      B03  openness        开放性     κ = 0.932

   κ（Cohen's kappa）＝ 两方打分有多一致的统计量。0 ＝ 跟瞎猜一样，1 ＝ 完全一致。
   0.93 在标注领域算很高。看起来很好。
```

## 4 · ⚠️ 但这个 0.93 不算数

```text
   0.93 是拿哪些题考出来的？
      ⇒ 拿「规则作者自己看过、并且亲手标过」的那 24 条。

   同一版规则，换成没看过的题再考一次：
      B03_dim_openness   看过的 24 条 → 0.932
                         没看过的题   → 0.667      ⬅ 掉了 0.27

   ⇒ 高分只说明规则**记住了**那 24 条，
     不说明它在新数据上管用。
```

这不是某个人做错了，是**方法上的一个洞**：出题的人和答题的人是同一批。

## 5 · 出路：正确性只能从外面借

```text
   公开数据集里有一类特殊的：
      它保留了**每一位**人类标注者的原始打分（per-rater），
      而不是只给你一个合并后的"标准答案"。

   ⇒ 于是能算出：真人和真人之间，彼此有多不一致。
     这个数字就是**天花板** —— 人类自己也就做到这个程度。

   ⇒ 如果我们的引擎能达到那个天花板，
     就说明它标得跟真人一样好。

   这个一次性的认证，原文里叫 autonomy license。
   要跑的那组公开数据集，原文里叫 battery
   （也是配置文件 ref-config.md 里的字段名：license: {battery: [...]}）。
```

⚠️ 注意 license 只需要拿**一次**，是引擎级的。
拿到之后，换任何新的构念（construct）跑，都继承这份可信度 —— 前提是新构念跟
battery 里的内容沾边，沾多远要如实写出来。

## 6 · 那要做哪 6 件事

```text
   ┌ 拿 license ─────────────────────────────────────────┐
   │  ① battery 放哪几个数据集      🧠 JL 拍板            │
   │        ↓ 定了②才能开工                               │
   │  ② 真跑一遍，把 license 拿回来  🔧 RA ~3d  ⬅ 最重的一条│
   └──────────────────────────────────────────────────────┘
   ┌ 补上引擎的最后一块（跟上面并行，不用等）──────────────┐
   │  ③ 让引擎不再写死在 openness 上 🔧 RA ~1d ✅可立即开工 │
   └──────────────────────────────────────────────────────┘
   ┌ 回头重验我们自己的三个维度 ───────────────────────────┐
   │  ④ 用什么标准选构念            🧠 JL 拍板            │
   │        ↓ 定了⑤才能开工                               │
   │  ⑤ B01/B02/B03 重跑一遍        🔧 RA ~2d             │
   └──────────────────────────────────────────────────────┘
   ┌ 收尾 ────────────────────────────────────────────────┐
   │  ⑥ 两个 B02 编号撞了，理干净    🔧 RA ~0.5d ✅可立即开工│
   └──────────────────────────────────────────────────────┘
```

**怎么在板上留言**：在对应那件事的 💬 下面写一行。三个人各一条车道，谁都不删别人的。

```text
> JL [#battery-list]: battery 就用 POPQuorn + DICES，别铺太宽
>> RA [#battery-list]: POPQuorn 下好了，DICES 要申请
>> CC0722 [#battery-list]: [已处理] registry 加好了
```

---

## ① battery 里放哪几个数据集   〔#battery-list〕

**🧠 JL 拍板** · ⛔ 挡着 ② · 🟠 待办

> **为什么它排第一**：battery 覆盖多宽，license 就只敢宣称多宽。
> 放少了跑得快，但只能说"这套方法在很窄的范围内有效"；
> 放多了覆盖广，但 ② 要多跑好几天。这个不定，② 无从下手。

- ❓ **要决定什么** 用哪几个公开数据集来考这台引擎，考几个。
- 📍 **现在什么样** 代码 `lib/license.py` 写好了，registry 留了位置，
  但**一个真实数据集都还没下载**。
  候选来自 RA 做的调研 P02：POPQuorn · DICES · GoEmotions · LeWiDi。
  ⚠️ GoEmotions 标的是情绪，跟人格特质不是一回事（已实测 κ 只有 0.25–0.30）。
  要放进去的话，得先想清楚它到底证明了什么。
- 🎯 **怎么算做完** 板上写下最终名单，每个数据集配两样：它的人类天花板 κ 是多少、
  为什么选它。然后写进 `ref/ref-datasets.md`。
- 💬

## ② 真跑一遍，把 license 拿回来   〔#license-run〕

**🔧 RA ~3d** · ⏳ 等 ① · 🟠 待办 · **本板最重的一条**

> **为什么它在①后面**：①只是选考题，②才是真去考。
> 这一条跑完，引擎才第一次有了一个不是自己给自己打的分数。

- ❓ **要做什么** 把整条引擎放到那组公开数据集上真跑一遍，
  看它的 κ 有没有达到人类天花板。
- 📍 **现在什么样** `lib/license.py` 的自测过了（喂好数据判 PASS，喂随机数判 BELOW），
  但**从没碰过真实数据**。RA 自己在 note-update 里也把它列在
  「还没做的 = 真实运行」第一条。
- 🎯 **怎么算做完** 每个数据集落一行结果：名字 · 样本量 · 引擎 κ · 人类天花板 · 过没过。
  然后写一句总判定，并且如实写明：这份 license 覆盖哪类构念，不覆盖哪类。
- 💬

---

## ③ 让引擎不再写死在 openness 上   〔#lexicon-auto〕

**🔧 RA ~1d** · 不等任何人，✅ 现在就能开工 · 🟠 待办

> **为什么它可以并行**：它跟 license 没关系。但不做完，
> 「这是一台通用引擎」这句话就是假的 —— 里面还有一份写死的词表。

- ❓ **要做什么** 引擎第一步要先估算"这个特质在语料里大概多常见"（base rate），
  靠的是一份关键词表（lexicon）。现在这份词表是照着 openness 手写死的。
  要改成：根据构念的定义，让大模型自动生成。
- 📍 **现在什么样** `probe_base_rate.py` 里的词表是硬编码的。
  `lib/sample.py` 已经写好了，就等这份词表喂给它。
- 🎯 **怎么算做完** 换一个跟医生完全无关的构念（比如"讽刺"），
  跑一次能自动出词表、算出 base rate，全程没有一行写死的东西。结果贴在这条下面。
- 💬

---

## ④ 用什么标准来选构念   〔#objective-pick〕

**🧠 JL 拍板** · ⛔ 挡着 ⑤ · 🟠 待办

> **为什么它排在重跑前面**：新引擎不再让人手写构念定义，
> 而是让多个大模型各提一个候选，然后**按一个标准挑最好的那个**。
> 这个标准（原文里叫 objective）不定，⑤ 跑出来的东西没法判好坏。

- ❓ **要决定什么** 这个标准用哪一种：
  (a) **downstream** —— 看这个标签能不能预测下游结果（比如医生的阿片类处方量）
  (b) **discriminance** —— 看这个标签跟兄弟特质分得开不开
- 📍 **现在什么样** `lib/construct.py` 两种都支持了，但医生这个项目的标准还空着。
  ⚠️ 卡点：CMS 数据是 PHI（受保护健康信息），`_WorkSpace/1-CMS-Store` 和
  `2-Data-Store` 只能待在安全服务器上。笔记本上跑不了真的 downstream，
  除非找一个能合法搬出来的聚合指标当代理。
- 🎯 **怎么算做完** 二选一，并写下理由：
  (a) 用 downstream，说明接哪个指标、怎么在不搬 PHI 的前提下算出来；
  (b) 这一轮先用 discriminance，downstream 推到下一块板。
- 💬

## ⑤ B01/B02/B03 重跑一遍   〔#rerun-3dims〕

**🔧 RA ~2d** · ⏳ 等 ④ · 🟠 待办

> **为什么它是收口**：前面借来的外部认证 + ④定好的标准，
> 都是为了这一步 —— 回头看看我们自己那三个 0.93 到底还剩多少。

- ❓ **要做什么** 三个维度用新的评分方式重跑。新方式把题分成两套：
  一套**固定的题**（fixed anchor，永不变，用来比较不同版本的规则），
  一套**每轮换新的题**（held-out，规则从没见过，用来看真实水平）。
- 📍 **现在什么样** `lib/converge.py` 的自测已经能在离线数据上复现那个 0.93/0.67 陷阱，
  但还没在真实项目文件夹里跑过。
  旧的固定题全部取自规则作者标过的那批（B01 实测 24 条 100% 重叠），
  所以重跑等于作废 12 个历史数字：B01 v01–v04 · B02 v01–v02 · B03 v01–v04。
- 🎯 **怎么算做完** 三个项目文件夹各产出一行新结果，**同时给出两个数**：
  固定题 κ 和没见过的题 κ。旧曲线移进各自的 `_archive/`，注明为什么作废。
- 💬

---

## ⑥ 两个 B02 编号撞了   〔#b02-naming〕

**🔧 RA ~0.5d** · 不等任何人，✅ 现在就能开工 · 🟠 待办

> **为什么虽小但不能不做**：⑤ 要重跑三个文件夹，编号乱着就没法引用结果。

- ❓ **要做什么** RA 的 note-update 里写的是 `tasks/B02_dim_openness/`，
  但磁盘上是 `B02_dim_conscientiousness/`，而 openness 另有一个 `B03_dim_openness/`。
  同一个 B02 指了两个不同的东西。
- 📍 **现在什么样** 磁盘上：A01_sample_corpus · B00_PhysicianTrait ·
  B01_dim_agreeableness · B02_dim_conscientiousness · B03_dim_openness ·
  B04_dim_extraversion。
  项目仓另有一个分支 `b03-openness` 没合，上面挂着 commit f7e97f2。
- 🎯 **怎么算做完** 编号唯一；`INDEX.md` 跟磁盘对得上；`b03-openness` 分支要么合、
  要么明确废弃；医生项目到底用哪个构念定义，写进板子。
- 💬

---

## 现在卡在谁身上

```text
   🧠 等 JL 拍两个板 —— 落了它们，RA 六件事全部解锁
      ① battery 放哪几个数据集    ⛔ 挡着 ② （~3 天的活）
      ④ 用什么标准选构念          ⛔ 挡着 ⑤ （~2 天的活）

   🔧 RA 现在就能开工的两件
      ③ 让引擎不再写死在 openness 上   ~1 天
      ⑥ 两个 B02 编号撞了              ~0.5 天
```

---

## 💡 下一块板（现在不做，先记着）

```text
· 板 02「对外说明文档订正」
    diagram/ 里 0-overview.txt · 1-three-loops.txt · 4-cascade.txt 描述的是
    2026-04/06 的旧设计，部分已经跟实际不符 + README 补一张流程图
    + plugin 缺 CHANGELOG.md 和版本号
  ⏳ 为什么现在不做：引擎还没真跑过（本板①②⑤），现在改完还得再改一遍。
     本板关掉那天，就是板 02 开板那天。

· 把全部 5000 条评论标完（现在只标了 48 条），或者训一个小分类器接手
· 请第二位真人在我们自己的评论上标一批，作为自家数据的天花板
```

---

## 附录 A · 术语对照

```text
   construct          构念 —— 你要标的那个东西本身，比如"开放性"
   guideline          标注规则 —— 一份写清楚"什么算 HIGH/LOW/NONE"的文档
   gallery            例子册 —— 已经标好并附了理由的样本，规则从它长出来
   κ (Cohen's kappa)  一致性统计量，0 = 瞎猜，1 = 完全一致
   human ceiling      人类天花板 —— 真人之间彼此的一致性，我们的上限
   per-rater          数据集保留了每一位标注者的原始打分，而不是只给合并结果
   battery            用来考引擎的那组公开数据集（ref-config.md 里的字段名）
   autonomy license   一次性认证：引擎在 battery 上达到人类天花板，
                      之后换新构念也继承这份可信度
   anchor / fixed set 固定的题，永不变，用来比较不同版本的规则
   held-out           每轮换新的题，规则没见过，用来看真实水平
   objective          挑构念候选时用的标准（downstream 或 discriminance）
   base rate          这个特质在语料里大概多常见
   panel              一组同时打标的大模型
   validator          负责验的模型，规定必须跟打标的模型不是同一个
```

## 附录 B · 引擎完整跑一遍是什么样

```text
   0  先摸底         估算这个特质在语料里多常见（base rate）
   1  定构念         多个大模型各提一个候选定义 → 各标一批样本
                     → 按 objective 挑最好的那个 → 针对分歧再打磨
   2  打标           一组大模型打标；负责验的模型必须换一个
   3  改规则         用分类器找出模型最拿不准的样本 → 据此改规则 → 出新版本
   4  量成绩         一致性 + 稳定性 + 没见过的题上的表现 + objective 分数
   5  判收敛         objective 不再涨 且 规则不再变 且 没见过的题上掉得不多
   6  铺全量         用分类器 + 分层策略把整个语料标完
   ─────────── 以下是引擎级的，一辈子只做一次 ───────────
   L  拿 license     整条引擎跑一遍 battery，确认 κ ≥ 人类天花板

   本板 6 件事各自在修哪一步：
      L  ⬅ ①② 　　　　整块板的重心
      0  ⬅ ③   　　　　这一步的词表还写死在 openness 上
      1  ⬅ ④   　　　　没有 objective，这一步挑不出来
      4·5⬅ ⑤   　　　　新的评分方式就在这两步
      —  ⬅ ⑥   　　　　编号卫生，不在流程上
```

⚠️ 整条流程里，需要人的地方只剩三个：说出 objective（每类构念一次）、
给 license 签字（一辈子一次）、极端个案的人工复核（可选，可关）。
其余全自动 —— 这就是为什么 L 这一步不做完，整台机器都没有可信度。

## 附录 C · 这台引擎已经修掉的 8 个方法学问题

来自 2026-07-17 那次会 + 2026-07-21 的独立复核，两边撞到了同样的结论。
代号 F1–F8 是 RA 在 note-update 里的编号，这里保留是为了对得上原文。

```text
F1  出题的和答题的是同一批 → 每轮换没见过的题 · 验的模型换一个 · 正确性外部量一次
F2  在数据集 A 上验证，却宣称在 B 上成立 → license 只覆盖跟 battery 沾边的构念
F3  没有人类天花板 → 天花板从公开的 per-rater 数据集来，引擎级摊销一次
F4  "说得清楚" 不等于 "标得对" → 一致性 / 清晰度 / 正确性，三个数分开报
F5  向量空间里靠得近 ≠ 标签一样 → 难例改从训练过的分类器里挖
F6  专门挑出来的难例被当成了普通样本 → 代表性样本和难例样本分成两池
F7  每轮随机抽 24 条来打分 → 改成固定的题 + 每轮换新的题
F8  规则不再变 ≠ 规则是对的 → 收敛条件要加上"没见过的题上掉得不多"
```

---

## 🗄️ 归档

```text
_superseded/note-update-v3-260721.md    RA 2026-07-21 写的方法学分析，本板取代它
_superseded/260721-workflow-audit.txt   CC 2026-07-21 的九步骤复核（压缩版，
                                        原件 611 行已丢失，见板内说明）
```
