## 0.1.0 — 2026-08-24

- **Created with the roadmap type on JL's 260824 ruling** (roadmap 完了之后还有
  collection … collection 完之后再去 seed): one intake-desk page per paper at
  `A1-SD-story/SD03-collection/`, P3 Collection (collect) of journey 0.5.0's
  establish loop.
- Its `probe/` lane IS the campaign's dispatch surface — one card per
  released Roadmap row, handed to the existing task/discovery orchestrators;
  nothing new is invented.
- Middle divisions are laps L<n>: ran (R ids) → cards (PP ids) → landed
  (QA paths + one-line readings) → settle (E-flip PROPOSALS the Seed alone
  writes).
- The Intake law: QA files are the substance; this page registers and never
  restates — no second evidence authority.

haipipe-page-for-collection · Changelog
=======================================

Skill-scoped changelog (never loaded at invocation; read on demand). Versions
match SKILL.md frontmatter `version:`. Newest first.

**v0-series rule:** inherited from `haipipe-board`; this skill stays on
`0.x.x` and never reaches `1.0.0` without JL's explicit say-so.
