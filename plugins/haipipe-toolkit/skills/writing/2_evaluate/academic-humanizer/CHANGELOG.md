## 2026-09-22 · vendored

- Copied from https://github.com/AIScientists-Dev/academic-humanizer at commit `02281d83` (upstream version 0.3.2, MIT). Role `evaluator` for papers, theses, rebuttals and NSF/NIH proposals; the catalog entry `academic-humanizer` now resolves here instead of `path: null`.
- Only the files the skill needs were copied; images and the upstream repository
  scaffolding stayed in `references/`. The SKILL.md body is unchanged; the frontmatter
  carries the haipipe provenance stamp.
