haipipe-run-timeline — Changelog
================================

Skill-scoped changelog (never loaded at invocation; read on demand). Versions match the skill frontmatter `version:`. Newest first.


## [1.3.0] — 2026-07-19

- ⑨ TOMBSTONES erased. Owner ruling (JL): "不需要留退役告示,直接抹除任何痕迹" — a doc states the CURRENT contract and never names the dead thing.
  The audit checklist's 💀 line naming the retired gateway agent becomes a ✅ line stating that dispatch
  goes DIRECT from the consumer lane to the two executor orchestrators.


## [1.2.0] — 2026-07-19

- Example timeline's probe-file edit line now shows `target: + ### a-executor`, matching what a PROBE-phase
  ⑤ INTERPRET actually writes. Vocabulary: `a-consumer:` as a PROBE-FILE FIELD is gone — the probe entry's answer subsection is
  `### a-executor` (the copy of the answering QA file's answer, the consumer-side single source of truth).
  The a-consumer CONCEPT is untouched: it remains the per-consumer interpretation written in the STAGE DOC
  (station 2, anchored `[source: PP<NN>]`).
