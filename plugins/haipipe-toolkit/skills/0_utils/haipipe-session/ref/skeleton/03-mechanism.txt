<MECHANISM> — how it works
==========================
Part of: diagram/<YYMMDD>-<topic>/  ·  6 files, one theme each, no index.
STATUS: 🟡 DRAFT   ·   flip to → SPEC OF RECORD — R1-Rn closed JL <date>. SHIPPED.
Theme: the moving parts: the loop, the gates, the states. How, not what.


  ⚠️  RENAME THIS FILE per topic — never ship it as `03-mechanism`.
        a process that cycles      →  03-how-it-runs
        a stage with human gates   →  03-gates
        a protocol / status ladder →  03-the-rules
        what is on disk, exactly   →  03-the-files
      Rename the TITLE above too, and re-cut its `===` underline to the new length.


THE <LOOP | LADDER | GATE SET> — <n> steps
==========================================

```text
   ① <STEP>          <what it does>
        │
   ② <STEP>          <what it does>
        │
        ⛔ GATE <n>   👈 <the human decides: X · Y · Z>
        │              (a declined <thing> is LOGGED, never deleted)
        ▼
   ③ <STEP>  ──▶ 🤖 <who executes it>
        │
   📄 <what lands on disk>
```


EACH STEP, IN ONE LINE
======================

  ① <STEP>   <who runs it, what it reads, what it writes.>
  ② <STEP>   <...>
  ③ <STEP>   <...>


⛔ THE GATES — where a HUMAN decides
====================================

  ⚠️  Name every gate, and say WHAT IS BEING AUTHORIZED. A gate that authorizes
      nothing is a decoration; a gate whose question is vague gets rubber-stamped.

  ⛔ GATE <n> — <name>
     asks:  <the literal question put to the human>
     🗳️  (a) <option>   (b) <option>   (c) <option>
     💰 authorizes: <the SPEND — GPU-weeks, calendar-months, a person's week>
     ❌ on decline: <what is logged, and where>


🚦 THE STATUS LADDER
====================

  ⚠️  Delete this section if the topic has no states.

```text
   🟡 <state>   →   🟠 <state>   →   🟢 <state>
      <meaning>       <meaning>       <meaning>

   ⚠️  THE LAUNDERING STATE: <the one everything parks in and never leaves>
      → the check that stops it: <the date check · the hard fail>
```
