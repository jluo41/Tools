Rulings — the append-only ledger, and what is still open
========================================================
Part of: diagram/<YYMMDD>-<topic>/  ·  6 files, one theme each, no index.
STATUS: 🟡 DRAFT   ·   flip to → SPEC OF RECORD — R1-Rn closed JL <date>. SHIPPED.
Theme: every decision this topic ever made. 🔒 MANDATORY — never delete, never split.


  🔒 MANDATORY FILE. Every topic folder has an 05. No exceptions.

  🔒 APPEND-ONLY. Never rewrite a ruling, never renumber, never delete one. A
     superseded ruling gets a NEW ruling under it saying so — the old text STAYS.
     A declined option is LOGGED, not erased.

  🔒 NEVER SPLIT BY DATE. A later session APPENDS here; it does not open a new
     folder. The whole value of this file is that ONE grep finds every ruling this
     topic ever made — and every place the closure could still be wrong.
       $ grep -rn '>> CC0714' diagram/<YYMMDD>-<topic>/    ← trace ONE session
       $ grep -n 'R[0-9]' 05-rulings.txt                   ← trace EVERY decision


✅ JL RULED — <YYYY-MM-DD>
==========================

  ⚠️  Body starts at COLUMN 22; continuations align to column 22. Ids are LOCAL to
      this folder, minted in order, NEVER renumbered.

  R1  <TOPIC KEY>    <THE RULING.>
                     <why, in one or two lines.>
                     📌 <where it was ADOPTED — the file / skill it now lives in>

  R2  <TOPIC KEY>    <...>
                     ⬅ this WIRES <an older ruling, dated> to <what it now governs>.


⚙️  CC DEFAULTED — live in the build, say so if any is wrong
============================================================

  ⚠️  Decisions the assistant made WITHOUT asking. They are shipped and running.
      Surfacing them is the point — an undeclared default is a decision stolen.

  R<n> <TOPIC KEY>   <what was defaulted, and which file holds it as written.>
                     ⚠️  WHY: <the failure it prevents>
                     ⚠️  COST: <what it breaks the first time it fires>


👀 WHERE THE CLOSURE COULD STILL BE WRONG
=========================================

  ⚠️  Argue against your own design here. A folder with no self-doubt section is a
      folder that has not been reviewed. Name the ruling and name the cost.

  1. ⚠️  <RULING id — the way it bites.>
        <what goes wrong, concretely, and when.>
        ❓ Sure? <the alternative, stated fairly.>

  2. ⚠️  <the tax this design puts on a human, forever.>
        ❓ Worth it? Counter-argument: <the strongest defence.>

  3. ⚠️  <the precedent that says this could be a thing nobody fills.>
        📌 the honest precedent: <a real thing that died. Name it, and its date.>


🔴 <THE OPEN RULING> — <why it is still open>
=============================================

  ⚠️  Open items go LAST, in bold relief. If everything is closed, write
      "NOTHING OPEN — <date>" and keep the heading.

```text
   🚪 <the blocker> · <the numbers>
      <what it unlocks — and what is unfalsifiable until it lands>

      rung:   <the ladder: application → DUA → enclave>
      clock:  📅 <CALENDAR-MONTHS | days | a GPU-week>
      👤 owner:  <UNASSIGNED>
      📄 status: <UNFILED>
      ⛔ consequence: <what cannot proceed. No amount of code changes it.>

   ⇒ <why this is the highest-leverage item here — and the one thing no agent can do.>
```

  👉 JL: ____________________________________________________________________
