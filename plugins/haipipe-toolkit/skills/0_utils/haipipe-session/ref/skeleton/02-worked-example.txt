The worked example — the real case, not a mock
==============================================
Part of: diagram/<YYMMDD>-<topic>/  ·  6 files, one theme each, no index.
STATUS: 🟡 DRAFT   ·   flip to → SPEC OF RECORD — R1-Rn closed JL <date>. SHIPPED.
Theme: ONE concrete case, end to end. Nothing abstract belongs in this file.


  ⚠️  HARD RULE: a REAL case — real names, real paths, real ids, real numbers.
      A mock here is worthless: the point is to prove the design survives contact
      with something true. If it is not on disk it does not go here — it goes in 06.
      If you cannot name a real case, the design is not ready for a folder.


<THE CASE> — <the real paper / run / incident>
==============================================

  This is not a mock. It is <what it is, and as of what date>.
  Read <the key line> and then read the ⚠️ line under it.


## <Section as it really looks on disk>
=======================================

    <N1>  <the real content, real numbers, real paths>
    <N2>  <...>

    <Q1>  ❓ <the real question>
          ✅ <what we found — dataset · N=<n> · the actual numbers>
          ❌ <what it CANNOT do, and why>
             🛑 <the one line that changes a decision>
                ⬅ ⚠️  THE LINE THAT WOULD HAVE STOPPED <the expensive mistake>

    <Q2>  ❓ <...>
          🏗️  COMMISSIONED · owner <who> · eta <YYYY-MM-DD> · <the cost>


WHY THIS EXAMPLE EXISTS — the receipt
=====================================

  ⚠️  The example IS the argument. Show the incident that proves the thing was
      missing. Evidence on disk beats a paragraph of motivation.

```text
   📅 <date>   <what was ALREADY known>
                ⬆ <the fact that was already on disk>

   📅 <date>   <what was done anyway>
                    │
                    ▼
   📊 <the outcome>:  <inconclusive / wrong / expensive>
                      — and nobody could say <why>, because <the reason>.

   💸 <what it cost>.
   🛑 <the one line above> is what stops it. It costs 30 seconds.
```

  ⇒ this is the whole argument for <the thing>, in one incident. The bundling that
    caused it is the ANTI-PATTERN — see the constitution file (04).
