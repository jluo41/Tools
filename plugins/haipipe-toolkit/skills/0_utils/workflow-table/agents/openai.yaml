interface:
  display_name: "Workflow Table"
  short_description: "Map workflow plans and runtime display in one table"
  default_prompt: "Use $workflow-table to design or audit a workflow's plan and display table, including skill coverage, Run profiles, and human gates."
policy:
  allow_implicit_invocation: true
