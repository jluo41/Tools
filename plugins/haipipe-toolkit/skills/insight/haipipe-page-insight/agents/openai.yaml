interface:
  display_name: "Task Insight Page"
  short_description: "Bind reusable R runs to new data as riNN insight runs"
  default_prompt: "Use $haipipe-page-insight to bind a normal R ticket to a new frozen dataset as an riNN Insight Run, then produce version-bound DIKW findings."
