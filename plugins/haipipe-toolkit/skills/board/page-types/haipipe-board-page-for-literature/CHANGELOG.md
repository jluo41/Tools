haipipe-board-page-for-literature · Changelog
===================================

Skill-scoped changelog (never loaded at invocation; read on demand). Versions match
SKILL.md frontmatter `version:`. Newest first.

**v0-series rule:** inherited from `haipipe-board`; this skill stays on `0.x.x` and
never reaches `1.0.0` without JL's explicit say-so.

## 0.2.1 - 2026-08-05

Paper-family projection path repointed to
`paper/haipipe-paper/probe/topic-entry-contract.md` (workers/ dissolved,
thin-paper phase 2).

## 0.2.0 - 2026-08-05

**De-parallelized from the value sibling; the route key is explicit** (review fix).

- The register carries a REQUIRED `route: outward` line, the base's type
  resolution step ②: topic filenames look like stage filenames, so the key,
  not the filename, resolves the type.
- The shared close semantics (SUPPORTED/BOUND · DEFERRED · WITHDRAWN; an
  unresolved row holds the topic open; the gate reads the register) moved to
  the new Register-row states section of
  `haipipe-board/ref/topic-entry-contract.md`. This file now says only what
  SUPPORTED takes on the outward route.
- The LOAD paragraph, close section, Files closer, and frontmatter summary were
  byte-parallel with `-for-value`; each is rewritten in this route's own voice,
  searching what already exists, so a noun-swap no longer reproduces the
  sibling.

## 0.1.0 - 2026-08-05

**Created on JL's ruling** (QB6 Decision Now, option D + display standing alone:
"how about the -for-literature, and -for-values, and -for-display, I want to
include them as well").

The topic types load ONE shared core, `haipipe-board/ref/topic-entry-contract.md`,
and add only their route's translation layer, so the register and entry anatomy
is stated once. Display is mirror-shaped but stands alone: its unit is produced
BY the project and closes on human acceptance, not on shipping.
