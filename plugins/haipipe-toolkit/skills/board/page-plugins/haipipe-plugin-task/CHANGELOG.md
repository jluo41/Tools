
## 0.1.1 · 2026-08-31

MERGING banner: the lane's job moves to evidence/pagex/ (unit symmetry);
contract stands until the pagex card reads task-unit status.

haipipe-plugin-task — Changelog
===============================

Skill-scoped changelog (never loaded at invocation). Versions match SKILL.md frontmatter `version:`. Newest first.

## 0.1.0 - 2026-08-18

- Born from JL's ask on the QPf13 design round ("task: what task folder is
  associated with this page"), shaped as pagex's fourth twin: same store
  grammar and drag-to-rank, inverted on the one rule that matters — task
  links whole FOLDERS, pagex links only files. Status read from
  `plan.yaml` / `report.yaml` / `QA/*.md` on disk, mirroring plugview.py's
  `_display_state`. Shipped whole: `live/task.py`, the drawer entry, and the
  roster row went 🟢 in the same round.
