# Changelog · haipipe-plugin-render

## 0.2.0 · 2026-09-03

- Make `delivery/render/` the only current writer destination; flat `render/`
  remains a compatibility read only.

## 0.1.3 — 2026-08-31
- Make the Folder-native Application render verb the executable writer; the
  HTTP route is an optional adapter, not a prerequisite.
- Define the warrant stamp as zero-or-more promoted Principles plus every
  directly bound signed Wisdom handoff.

## 0.1.2 — 2026-08-31
- Surface home named: the 📱 segment inside the 📤 Delivery tab
  (haipipe-plugin-delivery), a ghost until the lane's route ships.
