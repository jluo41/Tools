1d-advice: <intervention name> (design advice, venue-free)
===========================================================

Date: YYYY-MM-DD
Status: DRAFT
Ladder rung 1d, the DELIVERABLE: actionable advice, each entry derived from >=1 claim in the 1c ledger.
Advice is counsel, not mandate: downstream venue-ALIGNED stages adopt or decline entries (recorded there, not here).
Content-WHAT (survives retarget) -- distinct from 2-venue.md Artifact Principles (channel-HOW).
Every entry must pass the W-actionability test: could the artifact stage write the exact move from this line?


Advice
------

One **A<n>** per entry: the guidance in one sentence, role (exploit | explore), derivation, scope/boundary, status.
Close the section with a No-action line: every supported/weak C consumed by no entry, each with a one-line why.

**A1 - <short name, e.g. majority framing where compliance is high> - exploit - <active | caveated>**

<The guidance, one sentence, executable.>
Derivation: C<n> (<status>), C<n> (<status> - <caveat if weak>).
Scope: <who/when it applies; when to revisit>.

**A2 - <short name> - explore - <status>**

<The bet, one sentence, executable; a deliberate test-to-learn move on weak/GAP evidence.>
Derivation: C<n> (weak | GAP -- the bet).
Settles: C<n> via iterate (the deployed arm's result flips this claim).
Rails: <the compliance boundary the bet stays inside>.
Scope: <scope>.

No-action: <supported/weak C ids consumed by no entry, each with a one-line why -- or "none">.


Rejected
--------

Entries considered and dropped, one sub-item each with the refuting C id or reason.
Negative wisdom is first-class: this saves the next round from re-deriving it.
(May be empty.)

**R1 - <short name>**

<The dropped guidance in one sentence.>
Reason: <refuting C<n> status, or why it fails actionability/scope>.


Q-consumer
----------

<Questions this rung raises (rare — derivation is in-stage; a NEW gap routes back to 1c-claims). May be empty. One ## Q-Advice-<n> block each.>

## Q-Advice-1 · <question title>

Ask: <what it wants.>

Why: <which advice entry needs it, and what breaks if it stays open.>