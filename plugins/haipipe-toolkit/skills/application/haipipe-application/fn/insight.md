# `insight` · create or resume one Application-local Insight Page

1. Resolve the Application root and require one `serves:` id from the Brief Insight Need Map or an existing Design Aim.
2. Show the release packet before new evidence work: Application need, neutral question, target DIKW level, existing PageX matches, Task/Discovery source routes, and the Aim it blocks.
3. Reuse a matching local `page-type: insight` Page when its question/scope covers the need; otherwise create `1-insights/I<NN>-<slug>/I<NN>-<slug>.md` through `haipipe-page`.
4. Load `haipipe-page-for-task`, `haipipe-page-for-insight`, Probe for Task/Discovery sources, and PageX for accepted Page inputs.
5. Keep D/I/K evidence-led. Add Application-contextual W only after K settles; never write final message copy.
6. Publish Division 8 as the versioned Design Handoff and mark dependent Design PageX bindings stale when it changes.
7. Run the full Page workflow until CHECK settles or holds the Page.

Return the Insight Page path, target level, source versions, settled Handoff ids, blocked dependents, and next phase.
