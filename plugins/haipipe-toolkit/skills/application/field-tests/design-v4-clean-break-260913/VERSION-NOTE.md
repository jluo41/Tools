# Version-label correction

The `v4` directory name and `v4.0.x` strings inside these frozen field-test
records reproduce the version labels visible to the field desks at execution
time. Those labels were assigned without user approval and are retracted; they
are not Design releases.

The tested clean-break behavior now belongs to Design family version `0.4.0`.
The transcript evidence remains unchanged so the field-test record stays
truthful. Any future `1.0.0` or higher Design release requires explicit user
approval.
