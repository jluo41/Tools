idea-creator — Changelog
========================

Skill-scoped changelog (never loaded at invocation; read on demand). Versions match SKILL.md frontmatter `version:`. Newest first.


## [1.0.1] — 2026-07-14 — Compose onward into the `qa` verb, not the retired gateway

"Composing with Other Skills" pointed at `/haipipe-probe → open a probe on the chosen idea (evidence → verdict)`. Both halves of that line are dead: the probe GATEWAY is retired (a probe is now a paper-level document, not a dispatch console), and "verdict" as a probe outcome is deleted (R7). Re-pointed at the executors' question door — `/haipipe-discovery qa "<Q>"` and `/haipipe-task qa "<Q>"` — plus a line stating the boundary: a ranked idea is not a claim, and this layer never opens one. Spec: `Tools/plugins/haipipe-toolkit/diagram/260714-probe-qa/` v3; constitution: `probe/haipipe-probe/SKILL.md` 8.0.0.


## [1.0.0] — 2026-05-31

- baseline metadata added.
