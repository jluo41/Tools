# The workbench storage roster · one row per lane or category name

The single list of Page material names (design.excalidraw, JL 260815).
A subfolder of a Page's home folder is board material only if its name is here.
A row identifies storage, not necessarily a separate Workbench Skill: a category
Workbench may directly own one of its internal lanes.
`kind`: PRIMARY = originals a person makes, committed; DERIVED = projections of the page's text, regenerable, gitignorable.

`page.toml` is optional Page registration, not a workbench directory. A declared
`source-content:` identifies an editable file import under materials; this is
distinct from an immutable evidence capture and is not automatically evidence.
`delivery/web/` is the Page-owned static reading export, built by
`haipipe-page/cli/page.py build`; it does not add another public workbench.

**Outline is one Workbench over context, plan, and evidence materials:** Citation/Bib,
Value, and Display are internal lanes of its Evidence Space. The typed Evidence Item table
(`outline/<stem>-evidence-items.md`) is where SHAPE specifies the ready target
and SURVEY plans Supporting Runs, one Local Input, and one Local Run.

| name       | kind    | storage (`<page>/<name>/`)          | surface (right-pane tab)             | writer                                        | status |
|------------|---------|-------------------------------------|--------------------------------------|-----------------------------------------------|--------|
| `draw/`    | PRIMARY | folded Page: `studio/draw/<stem>.excalidraw`; flat legacy Page: Group `draw/<stem>.excalidraw`; Group relations remain `draw/group.excalidraw` | the 🖌 upper half of the 🎨 Studio tab (the live Excalidraw editor, ✨ Draw it on its bar); the old separate 🖌 row folded 260831 | the xcal save path (`servers/workbench-studio/xcal.py`, mints a missing scene directly in `studio/draw/`) + `/_board/autodraw` (`servers/workbench-studio/autodraw.py`: `claude -p` AUTHORS from the page's .md; stamped, refuses hand-drawn; transparent fills, Comic Shanns) | 🟢 internal Studio lane; flat folded-Page `draw/` is read-only migration input |
| `chat/`    | PRIMARY | `studio/chat/<YYMMDD-HHMM>/` kept sessions: `digest.md` + `transcript.md`; `-02` only on same-minute collision | the 💬 lower half of the 🎨 Studio tab (GUI/TUI segment rides with it); the old separate 💬 row folded 260831 | `/_board/chat-keep` writes both kept-session files directly to `studio/chat/`; Page edits keep their ordinary Outline log records | 🟢 internal Studio lane; flat `chat/` is read-only migration input |
| `meeting/` | LEGACY | historical Page-local `<YYMMDD-HHMM>/digest.md` + optional transcript | no menu and no Page writer; Folder may expose existing bytes read-only | current records use `haipipe-project-meeting` at `<project-or-SPACE>/meetings/`; decisions route into affected Pages' Outline records | ⚰️ legacy Page input only; no new Page `meeting/` folder |
| `slide/`   | DERIVED | `<stem>-deck.html`                  | the 🎞 segment inside the 📤 Delivery tab, now carrying the ✨ authoring bar (one explicit press → /_board/autodeck); the shell's native 🎞 row folded 260831 evening with the studio fold | `/_board/autodeck` (`servers/workbench-studio/autodeck.py`: `claude -p` AUTHORS from the page's .md; overwrite always, validation before write) | 🟢 live |
| `latex/`   | DERIVED | `<stem>.tex` + compiled `<stem>.pdf`| 📜 LaTeX segment inside the 📤 Delivery tab (`haipipe-workbench-page`, 260831): the compiled PDF, tex fallback, built on click | `/_board/latex` → `md2tex.py` + LuaLaTeX      | 🟢 built this round |
| `word/`    | DERIVED | `<stem>.docx` + preview `<stem>.pdf`| 📝 Word segment inside the 📤 Delivery tab (`haipipe-workbench-page`, 260831): the PDF twin; ⬇ the .docx, built on click | `/_board/word` → `md2docx.py` + `docx2pdf.py` | 🟢 built this round |
| `web/` | DERIVED | `delivery/web/index.html` plus imported static dependencies | standalone static reading site; live editing uses the Page server | `haipipe-page/cli/page.py build` | 🟢 Page-owned standalone export |
| `CITE` | RESULT | ledger-selected `results/<re-run>/result.yaml` plus frozen `payload.bibliography` source | read-only Citation Cards inside Outline → Evidence Space | `haipipe-workbench-page/ref/evidence/citations.md`; person verification belongs to the Item ledger | 🟢 current Result; old `outline/evidence/bibex/` is migration input only |
| `DISPLAY` | RESULT | typed envelope at `results/<re-run>/result.yaml`; unit at `results/<re-run>/payload/<unit>/` with `intake/`, `recipe/`, `float.tex`, and README source records | 🖼 Displays section inside 📃 Page → Evidence Space, read-only over Page Results | `haipipe-workbench-page/ref/evidence/displays.md` routes renderer craft and reserves the human `accepted:` tick | 🟢 Page Result; no separate `outline/evidence/display/` copy |
| `materials/` | PRIMARY | Page imports or governed immutable source captures, recorded by the owning input contract | Folder inspection; Evidence Space presents the selected Result | import/capture owner records origin and classification; an import alone is not evidence | 🟢 source material; retired `outline/evidence/materials/` is migration input |
| `outline/skill/` | MIXED | `<stem>.md` PRIMARY (the page's skill list: one `- <name>` row per skill, ORDER = the person's rank) + derived `<stem>-skill.html` | Folder inspection only; no reader-facing Space or editor | `/_board/skill` and related compatibility routes | 🟢 Outline-owned; sibling `skill/` is read-only migration input |
| `pagex/`   | MIXED   | historical ranked list + derived links at `outline/evidence/pagex/` | no active lens; migration input only | `haipipe-workbench-page/ref/evidence/pagex.md`; old `/_board/pagex*` routes remain readable | 🟠 legacy only; migrate evidence to Supporting Run Results or Local Input |
| `outline/` | PRIMARY | versioned Outline Markdown with embedded Draft fields, Evidence Item ledger, and process records | Outline opens first: Draft, Evidence, Run, and Delivery Spaces | the selected RP/RE and controller owners write their declared records; browser prompt-copy actions write nothing | 🟢 shared Page workspace |
| `design` | RESULT | Commission, Generate, and Verify Run Results; old Adopt decisions remain history | Design Items across Goal, Design, Insight, Run, and Delivery Space | the Design workbench permits Commission, Generate/Revise/Verify queueing, item/draft requests, and supported recovery actions; workers own their Results | 🟢 independent Verify pass makes a candidate ready; no current Adopt button or additional Adopt gate |
| `render/` | DERIVED | `delivery/render/<stem>-<unit>-v<N>.<ext>` one file per unit per version (`.txt` sms/push/reminder · `.html` ui-card/dashboard · `.docx` report) + `manifest.json` carrying design/warrants/render stamps | 📱 Render segment inside the 📤 Delivery tab when served; Folder files remain the complete surface | `haipipe-workbench-page/ref/delivery.md` names the Folder-native writer of the owning Design contract; `POST /_board/render` is an optional adapter. Regenerated never edited; an unpinned venue refuses | 🟢 internal Delivery lane |
| `workflow/` | PRIMARY / HISTORY | `folder.yaml` is current authoritative Folder identity; historical Run/pass receipts remain readable | Folder inspection; Run Space projects Run definitions and actual Run records | the owning Folder contract governs identity writes; controller actions, Versions, and Steps do not create extra Workflow rows | 🟢 current identity plus preserved history; `phase.yaml` is a read-only identity migration fallback |
| `labeling/` | MIXED | one subjective-label job, page-local: `config.yaml` · corpus/ · policy/ · rounds/ · gold/ · handoff/ · test/ · evaluation/ · production/ · audit/ · gates/ · runs/ · results/ are canonical; `REPORT.md` · `.state.json` · view/ · cache/ are rendered/cache and never authority. One Job Page = one corpus snapshot × one target construct × one identified human authority | 🏷 One Labeling surface on any real job-capable Page, with five Spaces: **Data** (Contract · Schema · Embedding) · **Labeling** (Label · Rounds · Guideline) · **Quality** (Test · Evaluation · Audit) · **Run** (Runs · Runs · Workflow map) · **Delivery** (Handoff · Final labels). Space = Workspace, one concept. Workflow P0-P5 is phase state shown in Run → Runs and the `Next:` header, not a Space. The page opens on the Space that holds the next step. Overview views never put item text in the page HTML; Labeling → Label holds the label definitions with chat-prompt buttons; Labeling → Rounds shows a round's item text once the chat has shown the item; Data → Embedding fetches other development items' text only on request, never an item waiting in a round, and logs each fetch in `labeling/exposure/group_examples.jsonl`. Studio Chat opens separately and is never a permanent bottom panel | `subjective-label-workflow` dispatches the Building/Scanning ORDER machines; their human event writer, Keeper, runner, reconciler and auditor write named artifacts. Runs in every Run P0-P5 allocate a Ticket `labeling/runs/<RUN>.yaml` and a Result `labeling/results/<RUN>/`. The browser has ONE write door, `POST /_board/labeling/act`, with ten actions (`confirm_meaning` · `release_round` · `open_item` · `first` · `final` · `build_embedding` · `embedding_status` · `embedding_item` · `group_examples` · `embedding_item_text`); each call needs same-origin, `attest: true`, the human id, the matching generated Page URL, and a canonical job, and the engine re-checks authority, HOLD, G0, event order, and sealed custody. No approve, freeze, or run button. Chat is transport, never gold; at HOLD the server independently blocks GUI/TUI/Draw write and run doors and the engine refuses every write-door action. Legacy flat Board sources resolve the exact folded page-task sidecar, and no write lands outside it | 🟢 built 260831 · five Spaces + one write door 260916 |
| `delivery/` | CATEGORY | parent folder holding what leaves the page: `latex/ word/ slide/ render/` (JL 260831: "put them in the folder like delivery") | the four segments of the Delivery tab | every lane writer lands only at `delivery/<lane>/`; flat names are read-only migration aliases | 🟢 canonical nested storage 260903 |
| `studio/` | CATEGORY | the HUMAN's room (JL 260831: "this one is closer to the human"): folded-Page `chat/` and `draw/` sit inside it; contracts: `haipipe-workbench-studio/ref/chat.md` and `ref/draw.md` | ONE 🎨 Studio tab — the drawing above with its ✨ bar, the chat below with its GUI/TUI segment; a sceneless page shows the chat full height | the two internal lane writers target `studio/<lane>/` directly; flat aliases are reader-only | 🟢 public category workbench |
| `scripts/` | PRIMARY | optional freestyle implementation material for Runs, any language and any internal shape. It does not define a Run, needs no manifest or one-to-one binding, and may contain files no current ticket calls. Shared Task Job code remains in the Job's `src/`; a lane-local recipe may stay with its lane | read-only inspection through Folder; a Run detail links only the exact script paths its ticket uses | the person or chat authors; the Runs presenter never interprets its tree | 🟡 rostered 260831 · freestyle 260901 |
| `runs/` | PRIMARY | authored shell/Markdown tickets, THE ONE execution door for Folder-local and Job-backed dialects. Pair each ticket by logical Run address: Folder-local `<folder>/runs/<run>.sh ↔ <folder>/results/<run>/`; Job-backed Task `<job>/<task>/runs/<run>.sh ↔ <job>/results/<task>/<run>/`. A ticket may call a local script, skill, CLI, API, or worker. The Labeling dialect keeps its Tickets and Results inside the job, at `labeling/runs/<RUN>.yaml` ↔ `labeling/results/<RUN>/`, and does not create this Page-level lane | one row inside 📃 Page → Run Space, filterable as Page Writing, Page Evidence, Supporting, or Labeling; clicking opens its Ticket + paired Result detail | the person or chat writes generic tickets; the owning subjective-label Run authors Labeling Tickets; tickets execute | 🟡 rostered 260831 · Labeling filter 260901 |
| `results/` | PRIMARY | paired Run history and outputs: receipt plus declared artifacts, never PHI. Computational projections may be regenerated under their Run contract; interactive Writing Steps/Versions preserve original feedback and accepted text and are never regenerated or discarded as cache, and NEVER inside `outline/evidence/`. Resolve it through the Folder-local or Job-backed Task dialect above; an orphan or address mismatch is invalid. A Result becomes Page evidence only when an evidence lane binds or aggregates it | the returned half of the same Run card/detail, never an independent Results section; Run Space exposes it and Evidence Space may cite the binding | owning Run worker; interactive workflow appends Markdown history, never edits completed Steps/Versions; Draft Space copies a scoped prompt for the owning RP; only the explicit Scratch lane writes its bounded notes and receipt in the browser | 🟡 rostered 260831 · paired in-row 260901 |
| `logging/` | PRIMARY | run/session logs the page owns      | 🧾 (planned)                         | the owning runner                             | ⚰️ RETIRED 260831: change history is outline/<stem>-log.md, executions are runs/ |
| `_fixture/`| PRIMARY | specimen inputs/outputs             | none — underscore keeps it off-board | the specimen's builder                        | 📋 declared |

Writers for the DERIVED document trio live beside their caller in `servers/workbench-page/exporters/` (`md2tex.py`, `md2docx.py`, `docx2pdf.py`); `servers/workbench-page/export.py` calls them by path. They are server code, not a skill.
Current Word/LaTeX exports use only the ledger-selected Results and record `evidence-selection.json`. Pages without a ledger use the labelled legacy migration profile, including its `--paper-root` discovery; missing current bindings never fall back to a legacy Bib or display.
The generic public Page Workbench skills are exactly two, and each pairs with
one served workbench: `haipipe-workbench-page` (📃 Page with its Run Space,
📤 Delivery, 📂 Folder: everything `servers/workbench-page` renders) and
`haipipe-workbench-studio` (🎨 Studio: `servers/workbench-studio`). Run Space,
Delivery, Folder, Chat, Draw, LaTeX, Word, Slide, Render, Skill,
Citation/Bib, Value, and Display are internal references owned by those two
(`haipipe-workbench-page/ref/run-space.md`, `ref/delivery.md`, `ref/folder.md`).
Design (`skills/design/haipipe-workbench-design`), Insight
(`skills/insight/haipipe-workbench-insight`), Paper
(`skills/paper/haipipe-workbench-paper`) and Labeling (`subjective-label`'s
`haipipe-workbench-labeling`) own their workbench skills beside their writers;
each pairs with `servers/workbench-<x>`. Meeting is project/SPACE material owned by
`haipipe-project-meeting`.

Category and meta Workbenches need not correspond one-to-one with storage rows.
`haipipe-workbench-page/ref/folder.md` is the 📂 meta-surface over this list;
Outline owns Context, Shape, Citation/Bib, Value, Display, run lineage, and
the embedded Skills record; `outline/skill/` retains its own storage and
writer contract without a top-level picker row;
Delivery (`haipipe-workbench-page/ref/delivery.md`) owns latex, word, slide,
and render; Studio owns Draw and Chat; and Run Space
(`haipipe-workbench-page/ref/run-space.md`) joins Tickets to Results. Design joins its existing intent, candidate,
verification, Run, and delivery lanes in `haipipe-workbench-design`; it is a
domain category extension rather than a sixth generic Page workbench. Each public
category owns its internal lane contracts, writers, and gates without minting
duplicate skills.
⚠️ `outline/` was absent from this table until 260821 while `<page>/outline/` had been real storage since 260817 — a gap directly against this file's own opening law that a subfolder is board material only if its name is here.
✅ Since 260824 the opening law is CHECKED, not just written: `check.py · check_workbench_roster` scans every page folder and reports `workbench-not-rostered` for a subfolder whose name is missing from this table. Three breaches in four days were three too many for a rule enforced by reading.
⚠️ `render/` repeated it historically: its lane contract shipped 260820
before its roster row existed. Twice the material arrived before its name did,
so the order remains: roster row first, then the owning category reference.
