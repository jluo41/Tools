# Fresh-agent run: can a new agent discover and execute the architecture?
state: ✅ SETTLED · two fresh-context routing runs passed
owner: CC

## Opening

Will a new agent, without this design conversation, use the revised skills as intended?

The test gives it a realistic message-design problem containing both accepted insights and one missing premise. The agent must route the work, not merely repeat the architecture description.

### Writing Style

Record observed behavior and exact divergence. Do not award credit for vocabulary alone.

## Diagram

```text
fresh context + realistic application request
                    │
                    ▼
discover skill → choose Pages → route missing insight → design → promotion test
                    │
              pass / revise skill
```

## Content

### 1 · Contract

**Contract map**: the subparts below refine the Page decision into one bounded handoff.

```text
inputs → bounded contract → observable handoff
```

Design a small medication-adherence SMS application from a settled audience profile and adherence-timing finding while handling one unsupported safety premise.

#### 2 · Pass criteria

- identifies one Brief, at least one local Insight, and one or more Design Pages;
- scopes each Design to one audience × behavior job × primary venue;
- uses PageX for accepted Page evidence;
- routes the unsupported premise to a local Insight Page and keeps Probe out of Design;
- keeps SMS units as Design projections unless independent governance is shown;
- stops at the appropriate evidence or human acceptance gate.

#### 3 · Process evidence

Run 1 created one Brief, three local Insight Pages, and one SMS Design Page; it kept the initial and follow-up messages as repeated Design divisions and promoted no Artifact.

Run 1 routed the unsupported kidney-safety premise to a local Insight Page, allowed Probe only there, and stopped deployment at the human evidence/acceptance gates.

Run 2 split patient reminder and clinician escalation into two Design Pages because audience and behavior job differed; it again promoted no Artifact and isolated the pregnancy-safety gap in one local Insight Page.

Both runs discovered Application 0.8.0 and the four Page Type contracts from installed skills rather than the design discussion.

#### 4 · Verdict

PASS. The revised public skill produces the intended ownership, Page cardinality, evidence boundary, projection-first output, and human stops in fresh context.

The audit found and this round removed a stale base-Page roster, an obsolete generic-Design sentence, and the dormant flat-pool procedure text.

Legacy stage specialists remain installed as compatibility readers and are tracked as migration debt; neither fresh run selected them over the 0.8.0 Page routes.

## Aims

### A1 · Contract
- ✅ A1.1 · The new agent finds and loads the revised public skill.
  **Done when:** its process names the skill source rather than relying on prompt hints.
  **Now:** Both agents loaded the installed Application and Page contracts.


#### A2 · Behavior
- ✅ A2.1 · The route satisfies all six criteria.
  **Done when:** no architecture-critical divergence remains.
  **Now:** Both routes satisfied all six criteria after the roster cleanup.


## Discussion

## Files

### 📋 Contracts
- `../../../../application/haipipe-application/SKILL.md`
- `../../../../application/page-types/`

## Law

The test agent receives the problem, not the desired solution outline.

## Log

260820 · First fresh run passed SMS sequencing, local Insight routing, and Artifact admission; its stale-roster finding was repaired.
260820 · Second fresh run passed multi-audience Design splitting and found no behavior-critical route divergence.

- 260831 0113 · `## States` merged into `## Aims` (tick + `Now:` per Aim; asks and threads kept verbatim), skill 0.148.0