# Export

Export the promoted SVG to PDF with:

```bash
rsvg-convert -f pdf -o ../assets/figure.pdf ../assets/figure-1.svg
```

Compile `preview.tex` from the Display-unit root, then rasterize the first PDF page to `preview.png` for inline inspection.
