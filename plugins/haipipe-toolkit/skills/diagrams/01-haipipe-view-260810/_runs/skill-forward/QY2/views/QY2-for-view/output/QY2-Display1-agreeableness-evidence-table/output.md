# QY2-Display1 · Agreeableness evidence table

kind: table
status: rendered
acceptance: waiting

## Reader job

Let a reader compare the two answered QA Probes side by side — the canonical trait
definition and the observable project signal — without reopening each Probe file.

## View-body bindings

- EC1 (interpersonal trait, `\citep{john1999bigfive}`)
- EC2 (observable signal / 3 bound source records)

## Source data

The table summarizes:

1. `input/QA-probes/1-canonical-definition.md` — canonical trait description.
2. `input/QA-probes/2-observable-signal.md` — observable project signal and bound
   source-record count.

## Files

- `preview.png` — inline/browser inspection surface (real rendered PNG, PIL).
- `preview.pdf` — printable inspection surface (real rendered PDF, fpdf2).
- `assets/table.png` — same raster kept as a live source asset.
- `float.tex` — printable LaTeX wrapper embedding the PDF.

## Acceptance

State: waiting. No human has accepted this Display yet.
