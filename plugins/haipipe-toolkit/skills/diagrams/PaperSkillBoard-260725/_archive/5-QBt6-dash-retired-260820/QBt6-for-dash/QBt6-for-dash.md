# QBt6 · Regenerate one family Dash without granting it authority

state: ✅ SETTLED · current Dash contract validated
page-type: dash
dash_family: section
owner: JL
method: scan one family fresh and distinguish generated rows from authored interpretation

## Opening
How can a reader see every Section, probe, citation, or display unit without creating a second control plane?
Dash regenerates a complete rollup, including expected gaps and orphan units.
It reports source state and never accepts, closes, or edits an underlying unit.

**Where this page sits**: it observes the Page graph after work lands on owning Pages and plugins.

## Writing Style
Use explicit states in every generated cell.
Keep authored interpretation separate from generated measurement.

## Diagram
**Dash boundary**: repair the source and rebuild the report.

```text
Pages/plugins ─▶ fresh scan ─▶ generated rows ─▶ authored reading
     ▲                                   │
     └──────── fix owning unit ◀─────────┘
```

## Content
### 1 · Dash contract
**Four families**: one type reports section, probe, citation, or display units.

```text
expected rows + current rows + orphan rows + explicit gaps
```

The `dash_family` field selects the payload; it does not create another Page Type.

## Aims
### A1 · 📊 Dash contract
- A1.1 · Rebuilding a Dash measures the full current source family deterministically.
  **Done when:** every unit and gap appears and no row owns a decision.

## States
### A1 · 📊 Dash contract
- ✅ A1.1 · Four former dash variants are represented by one type plus one family field.

## Files
- `../../paper/page-types/haipipe-page-for-dash/SKILL.md` · source contract

## Log
260820 · Kept Dash as a derived Page and removed its former family-specific contracts.
260820 · Retired whole: JL proposed merging Dash into Narrative; rejected because only the `section` family was ever Narrative-shaped, and the other three (`probe · citation · display`) had no Page to fold into. A `closes when: never` type that owns no gate and states no decision was never Page-shaped to begin with. It survives as `/haipipe-paper status [family]`, a command owned by `haipipe-paper`, not a resolvable Page Type. Contract archived at `paper/page-types/_archive/haipipe-page-for-dash/`. Moved to `_archive/5-QBt6-dash-retired-260820/`.
