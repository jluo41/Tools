# Precedent for typed pages and contract drift

- state:    read
- route:    discovery
- bank:     ../../../../../../../examples/Fabricated-Project/discoveries/D01_typed-page-prior-art/QA/1-precedent.md

🚫 FABRICATED. Every key below is invented, the journals they name are invented,
and the search that found them never ran. The FORMAT is what this file teaches.

`route: discovery` means the answer was produced by a search living in the
discovery tree, and the bank stays there. This file holds the binding, a digest,
and the limits copied whole. The bank path above is not reachable from this
checkout, which is the ordinary state of a paper whose executor tree is not
cloned; it is a fact about the checkout, not a defect in the record.

An outward answer's product is PROSE, so this record declares no `- output:`
line: a citation is not a file. That is why `unit.py check` counts this drawer as
neither an atom nor a view, which is a real limit of the resolver on this route.

## Question

Has any published study measured contract drift on typed document pages, and
linked that drift to how long a page has declared its type key? Name the closest
work on each half: drift measured on any document corpus, and type declarations
studied without a drift outcome.

## Answer

UNOCCUPIED within this search's own scope. No published study does both halves at
once. Three neighbours came back and each is short exactly one leg.

```text
  key                  what it does                    the leg it misses
  ──────────────────────────────────────────────────────────────────────────
  imaginary2031typed   counts type declarations        no drift outcome; it
                       across 40 imaginary repos       stops at adoption
  nowhere2030drift     measures section drift over     no type key exists in
                       18 months of untyped wiki       that corpus, so tenure
                       pages                           cannot be defined
  pretend2029forms     links form-template age to      the unit is a form
                       field divergence in imaginary   instance, not a page
                       government filings              with a declared contract
  ──────────────────────────────────────────────────────────────────────────
  🚫 all three keys are INVENTED. They resolve in no bibliography, they name
     no real journal, and a key that looked real would be the actual hazard
```

The nearest neighbour is `imaginary2031typed`: it shares the typing half and
stops before the outcome, so this paper EXTENDS it rather than being occupied by
it.

The scope of the search is part of the finding. Two passes over three imaginary
indexes, titles and abstracts only, no gray literature and no citation chasing.

## Caveats

- Absence WITHIN a scope, never absence. The verdict rests on those two passes,
  and the scope travels with it wherever it is quoted. A row that writes "novelty
  confirmed" without the scope clause has overstated the search, not the finding.
- The drift-half neighbour measures drift on an untyped corpus, so its drift
  definition is not this paper's and the two numbers are not comparable.
- Every key is invented, and nothing here may enter any bibliography.

Copied WHOLE from the bank rather than digested, which is the rule for limits on
any route.
