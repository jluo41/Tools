# Proof packet · artifact-path mentions by Page-Type contract

question: How many artifact-path mentions does each measured Page-Type contract contain?
bank: `../../../../QBt5-for-value/QA-probe/QBt5-for-value/1-artifact-paths.data/counts.csv`
producer: `QBt5-for-value.md` · E1
scope: ten Page-Type contracts measured by the QBt5 E1 bank task
fields: `contract`, `paths`, `lines`

## Permission

The bank permits comparison of artifact-path mention counts across the ten measured contracts.
It permits a diagnostic comparison between mention count and contract line count.

## Ceiling

The measurement counts path-like terms named by the bank task.
It does not test whether a path resolves, whether the contract is correct, or whether the artifact is useful.

## Construction rule

Every candidate must read the bank CSV directly.
No candidate may add, remove, or hand-type a contract row.
Derived ordering is allowed when the visual states that it ranks the same values.
