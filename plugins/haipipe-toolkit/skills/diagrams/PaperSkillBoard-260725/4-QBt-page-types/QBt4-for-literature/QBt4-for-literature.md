# QBt4 · page-type LITERATURE · owns QA-probe records; route OUTWARD, and every key it claims must resolve in the shared bank

state: 🟡 PARTIAL · 2 of 2 questions answered · 1 answer never became a citation binding
route: outward
owner: JL
method: collect each consumer's positioning stake, send the neutral question out to the published record, and write what came back onto the asking row as a key, a positioning sentence, and a verdict
session: f6b4ab7d-1e5d-4c0a-a780-d73288d20a30

## Opening

What is already known about typed pages, who on this board is leaning on it, and has any of it become a citation the manuscript can use?
🚫 Every citation key here is invented, and none may enter a bibliography.
Read it for the format: a working `for-literature` page keeps this register of which question went out, who asked, and what came back.
The page owns no answer of its own.
A digest is not yet a citation: the asking row needs a key, a positioning sentence, and a verdict.

🚫 **Every citation on this page and in its drawer is invented.** No such paper, journal, or key exists, the keys are unmistakable on purpose, and nothing here may enter a bibliography.

An evidence page on the outward route asks what is already KNOWN, as against the inward route's question of what this project must PRODUCE. It owns no answer itself: the answer belongs to whoever read the literature, this paper keeps one bound record under `../QA-probe/`, and this page is the register saying which question went out, who asked, and what came back.

**Why a returned answer is not yet evidence**: a digest is a convenience, and the thing a manuscript may lean on is the citation binding written on the asking row, which is a key, one sentence placing this work next to the found result, and a novelty verdict. E2 below has the digest and not the binding, and that is why this page is 🟡.

**Why this page provides nothing**: it is a VIEW over N records, not an atom. Each `### E<n>` division points at one record, and a literature record's product is prose rather than a file, so it declares no `provides:` either. `QBt3` is the opposite shape: it IS its atom and does declare one.

**Covered elsewhere**: `QBt5` is the inward twin of this page, and this page copies its shape on purpose. `QB6` owns which types exist. The record anatomy is `haipipe-board/ref/topic-entry-contract.md`.

## Diagram

**One page, N conversations**: where an outward answer comes from, and what it must become before this page may lean on it.

```text
  🌍 the published record            the only place an outward answer exists
     a bank does not hold the literature. It holds one SEARCH that read it,
     and that search's own scope travels with the answer. None is here.
              │
              │  `- route: discovery` + `- bank: <path>`
              ▼
  📋 QA-probe/QBt4-for-literature/   one record per question. Same three H2s.
  │    1-typed-page-precedent.md     route: discovery · bank not cloned here
  │    2-tenure-confound.md          route: local · read here from two sources
  │                                  already in hand, so there is no bank
  │         │
  │         │  reached by reading, not by id: prose has no product to resolve
  │         ▼
  📄 QBt4-for-literature.md          ← this page. A VIEW. No output:.
       ### E1 · is the ground occupied?        🔗 1-typed-page-precedent
       ### E2 · did anyone assign the key?     🔗 2-tenure-confound
       ### E0 · incoming
  ─────────────────────────────────────────────────────────────────────
  the SEARCH holds what was read, and the scope it read within
  the RECORD holds the binding, a digest, and the limits copied whole
  the ROW holds the citation binding · key + positioning + verdict
  a digest on the page is NOT a citation · 🚫 every key here is invented
```

**Its own input and output**: a LITERATURE page reads a shared bank and emits a generated bibliography.

```text
 🏦 BANK    ../../../paper/venue/literature/bank.bib   283 entries, shared
 📥 INPUT   QA-probe/QBt4-for-literature/              one record per question sent out
            + `### Sources already named`              the claim lines: key · what job it does
 📤 OUTPUT  _fixture-qbt/QBt-page-types.bib                🤖 4 entries, GENERATED
              ▶ printed through misq.bst ──▶ References in the compiled PDF
```

## Content

### E1 · Has any published study measured contract drift on typed pages and linked it to type-key tenure?

**The binding**: what was asked, which record holds the answer, and what came back.

🔗 QA-probe: `QA-probe/QBt4-for-literature/1-typed-page-precedent.md` · state: read

```text
  🌍 bank      examples/Fabricated-Project/discoveries/
               D01_typed-page-prior-art/QA/1-precedent.md
               route: discovery · not cloned here
  🔑 keys      imaginary2031typed · nowhere2030drift · pretend2029forms
  📐 verdict   UNOCCUPIED within the search's own scope · novelty supported
  ─────────────────────────────────────────────────────────────────────
  🚫 all three keys are INVENTED and resolve in no bibliography anywhere
```

🔎 Establishes that the ground this paper claims is unoccupied, and names the three neighbours its gap sentences must cite and differentiate.

#### consumers

- `QBt5-for-value` · SUPPORTED.
  Stake: the inward page's E1 produces drift counts, and those counts are a contribution only while this ground is unoccupied; if a published study had already measured drift by key tenure, E1 there is a replication and the paper owes a different number.
  A-consumer: E1 on that page stands as written, and its digest may say "first measurement" only with the scope clause attached.
  citation binding · key `imaginary2031typed` 🚫 invented · positioning: this work EXTENDS it, measuring what happens to a page AFTER the key is declared, where that study stops at counting declarations · verdict: novelty SUPPORTED within the search's scope, source named.

#### answer digest

UNOCCUPIED within the scope of a two-pass sweep over three imaginary indexes: no published study both types a page's contract and measures how far the rendered page drifts from it.
Three neighbours came back, each short exactly one leg, with `imaginary2031typed` closest on the typing half.
The scope clause travels with the verdict, because a bounded search cannot report an absence without saying what it searched.

### E2 · Has any published design ASSIGNED a page's type key, rather than observing pages whose authors declared one?

**The binding, and the half that never landed**: the same three facts, for an answer that reached this page and stopped here.

🔗 QA-probe: `QA-probe/QBt4-for-literature/2-tenure-confound.md` · state: answered-local

```text
  🌍 bank      none · route: local, read here from two sources already in hand
  🔑 keys      imaginary2031typed · nowhere2030drift
  📐 verdict   no assigned-key design found · the ceiling is ASSOCIATION
  ─────────────────────────────────────────────────────────────────────
  ⬜ consumer  QBt3 §3's label block still names no source
               the answer is here, the binding was never written
  🚫 both keys are INVENTED and resolve in no bibliography anywhere
```

⚠️ Establishes the outside half of the honest ceiling on this paper's drift figure, and shows what it looks like when that half never reaches the sentence waiting for it.

#### consumers

- ⬜ `QBt3-for-display` · open.
  Stake: that page's §3 sets the figure's label to "association" and supports it with two facts about this paper's OWN design; a reader asking whether the field ever did better gets no answer from it, and the JIS desk screens on exactly that verb.
  A-consumer: the label stays "association", and §3's label block should carry one key plus one sentence saying the strongest published design is observational too.
  no citation binding written. The digest sits below and `QBt3` cites nothing, so no sentence in the manuscript points at this answer. The row stays ⬜.

#### answer digest

No published design assigns a type key. The two designs on file are a cross-section and a before-and-after on an untyped corpus, so the strongest published evidence is observational, exactly like this paper's.
That is good news for the label and bad news for anyone hoping a citation would lift it: "reduces" is not available from the literature either.

### E0 · incoming

**The standing queue**: positioning questions collected from other pages, not yet translated and not yet sent.

```text
  (empty)
```

📥 Establishes that nothing is waiting. A Q-consumer arriving from any page lands here with its stake attached, loses the stake on the way out, and becomes an `### E<n>` division with its own record when it goes.

### Sources already named

**The claim shape, which is where a key BECOMES this page's responsibility**: one line per key, the key and then what job that source does. A bare list cannot carry the second half, and the second half is the whole point: claiming a key means being answerable for why the paper is allowed to lean on it.

`bib-from-bank.py` reads exactly these lines. Every key below resolves in the shared bank at `paper/venue/literature/bank.bib`, 283 entries, and the generated `../_fixture-qbt/QBt-page-types.bib` holds these four and nothing else.

- \citep{brown2020language} · establishes that the general few-shot behaviour arrived with scale, which is what lets a language model be used as an instrument with no task-specific training.
- \citep{hu2024llm} · the design that does NOT trust the model with the label: it augments the text and leaves detection to a smaller model, which is the conservative form of the same move.
- \citep{Nazi2024LLMHealthcare} · the medical-domain survey, carried for its reservations rather than its enthusiasm: it is where the reliability and evaluation caveats are catalogued.
- \citep{mcgrath2018validity} · the prior question, kept separate on purpose: whether an online rating says anything true about a physician at all, tested against peer review.

- \citep{imaginary2031typed} · DELIBERATELY UNRESOLVABLE, claimed on purpose so the broken-citation state is demonstrated rather than described. Do not repair it.

**Two keys on this page are deliberately unresolvable.** `imaginary2031typed` and `nowhere2030drift` appear in `E1` and `E2` and exist in no bibliography anywhere. They are not an oversight and must not be repaired: they are the specimen's BROKEN state, and `bib-from-bank.py --check` is supposed to report them and exit non-zero. The sibling fixture at `PaperSkillBoard-260725/` carries the same device with `stock2005testing`, absent there exactly as it is absent in the paper it was lifted from, so the broken-citation example is a real defect rather than a staged one.

📚 Establishes the four keys this page is answerable for, and the two it is answerable for NOT resolving.

## Aims

### A1 · 🔎 E1 · precedent for typed pages
- A1.1 · The novelty verdict is never quoted without the scope it rests on.
  **Done when:** each consumer row under E1 states the verdict and the search scope in the same row, so no downstream page can lift one without the other.

### A2 · ⚠️ E2 · the assigned-key design
- A2.1 · The answer becomes a citation binding, or the row is withdrawn.
  **Done when:** `QBt3` §3's label block carries the key and the positioning sentence, and the `⬜` under E2 reads SUPPORTED.

### A3 · 📥 E0 · the incoming queue
- A3.1 · No positioning question sits in E0 longer than one working round.
  **Done when:** E0 is empty or every row in it carries the date it arrived.

## States

### Decision Now
- 📍 E2 has an answer and no citation binding. Either `QBt3` §3's label block takes the key and the positioning sentence, or the row is withdrawn and the label stays a design argument with nothing published behind it. The page stays 🟡 until one of those happens, which is the behaviour being demonstrated.

### A1 · 🔎 E1 · precedent for typed pages
- ✅ A1.1 · E1 carries one consumer row and it states both halves in the same line, `verdict: novelty SUPPORTED within the search's scope, source named`, with the E1 figure repeating `UNOCCUPIED within the search's own scope · novelty supported`, so no downstream page can lift the verdict with the scope clause stripped off it.

### A2 · ⚠️ E2 · the assigned-key design
- 🧠 A2.1 · Unmet on purpose, and waiting on the Decision Now row above: `QBt3-for-display` §3's label block still argues "association" from this paper's own two design facts with no key beside it, the consumer row under E2 still reads `⬜`, and `python3 unit.py check` in `4-QBt-page-types/` lists neither drawer record because a prose answer declares no `provides:`, so no command on this board will ever report the gap and the open row is the only thing holding it visible.

### A3 · 📥 E0 · the incoming queue
- ✅ A3.1 · E0 holds `(empty)` and no positioning question has arrived since the page was written, so the first branch of the Done when holds because the queue was never used rather than because anything was cleared out of it.

## Files

**📂 This page's own folders**: a LITERATURE page owns QA-probe records and emits a generated bibliography. Its bank lives OUTSIDE the paper and is never copied in.

```text
 🏦 BANK    ../../../paper/venue/literature/bank.bib      283 entries, shared, hand-kept
 📥 INPUT   QA-probe/QBt4-for-literature/                 the records this page asked for
              1-typed-page-precedent.md · 2-tenure-confound.md
            + this page's `### Sources already named` claim lines
 📤 OUTPUT  _fixture-qbt/QBt-page-types.bib                   🤖 generated, 4 entries
              ▶ printed through _fixture-qbt/misq.bst ──▶ References in the PDF

 ✅ CHECKLIST · what must be true, and how to see it
    ☑ citation chips render  each \citep{} in the claim lines becomes a chip
    ☑ bank resolves          every claimed key exists in bank.bib
    ☑ output regenerates     cli/bib-from-bank.py <stage>   writes the .bib
    ☑ it reaches the reader  0 undefined citations in the compiled PDF
    ☑ BROKEN is demonstrated imaginary2031typed is claimed on purpose and
                             reported; --check exits 1, and that is correct
```

- `QA-probe/QBt4-for-literature/`
  📥 The INPUT folder: one record per question this page sent out, named for the page that asked.
- `../_fixture-qbt/QBt-page-types.bib`
  📤 The OUTPUT: generated from this page's claims plus the manuscript's own `\citep`. Never hand-edited; fix the bank or fix the claim.
- `../../../paper/venue/literature/bank.bib`
  🏦 The shared bank, outside every paper. A key is added there once and claimed here.
- `../../../../board/haipipe-board/cli/bib-from-bank.py`
  The engine command that turns claims into the bibliography, and reports broken, unclaimed and unused keys.
- `../../paper/page-types/haipipe-page-for-literature/SKILL.md`
  The contract this page is an instance of.
- `../BoardSkillBoard-260722/QPs-page-structure/QPs2-page-types/QPs2-page-types.md`
  §7 states the five on-disk shapes; this page is a worked QA-PROBE one.

## Log

- 260807 1520 · [REVISE-CC] THE EVIDENCE POINTER WAS NEVER CLICKABLE, and the 260806 row below is the decision that did it. JL reported clicking the evidence card and getting nothing, for what he said was not the first time. Cause, measured rather than guessed: a ``` fence is CODE and the renderer links nothing inside code, so a `🔗 QA-probe:` pointer written as the figure's first row is inert BY CONSTRUCTION. It reads exactly like a working pointer and there is nothing to click. `QBt5` is the control that proved it: same anatomy, pointer written ABOVE its fence, and it rendered its link the whole time. Measured on the built HTML, `QBt4` had 0 links to its own two records and `QBt5` had 1 to its own. Both pointers moved above their fences with the path backticked, which satisfies the caption rule and the record anatomy at once, and `QBt4` now renders 2 resolving links to records that exist on disk.

- 260807 1520 · [REVISE-CC] The reason it kept coming back is that NOTHING COULD SEE IT. Every link check on this board answers "does this href resolve"; none can see a pointer that never became an href at all, because a dead link is visible and an ungenerated link is not. `cli/check.py` gained `check_evidence_pointer`, which reads the SOURCE line instead of the rendered anchor and reports `evidence-pointer-in-fence` and `evidence-pointer-missing` as ERRORs. It was verified by putting the defect back on a scratch copy and watching it fire at the exact line, then removing it; a check that has never been seen to fail is not yet a check.

- 260807 1200 · [REVISE-CC] the bibliography half made real. A new `### Sources already named` division claims four keys that resolve in the shared 283-entry bank, each with the sentence saying what job the source does, and `board/haipipe-board/cli/bib-from-bank.py` generated `../_fixture-qbt/QBt-page-types.bib` from those claims plus the manuscript's own `\\citep`. All four print in the compiled fixture through `misq.bst`, with 0 undefined citations. `imaginary2031typed` is now CLAIMED on purpose so the BROKEN state is demonstrated rather than described: the tool reports it and exits 1, and it must not be repaired.

- 260806 · [REVISE-CC] The Opening rendered as 136 characters of bare question, and the 🚫 notice sat behind the fold, which is the worst place for it on a page whose invented keys look like real ones. Four sentences now sit on stage under the question, at 480 characters: that every citation key here is invented and none may enter a bibliography, that this is a working `for-literature` page read for its format, that the page owns no answer of its own, and that a digest becomes usable only when the asking row carries a key, a positioning sentence, and a verdict. One drawer sentence was deleted as the on-stage text now says the same thing: "This page is a real `for-literature` evidence page and its FORMAT is what it teaches; its content is fabricated." The rest of the drawer is untouched, including the clause that the keys are unmistakable on purpose.
- 260806 1258 · [REVISE-CC] States now mirrors every Aim id, one row per id under the group headings Aims already uses. Judged against disk: A1.1 is ✅ because E1's single consumer row states the verdict and the scope clause in the same line, A3.1 is ✅ because E0's figure reads `(empty)`, and A2.1 is 🧠 rather than an untouched ⬜, since `QBt3-for-display` §3's label block still argues "association" from two facts about this paper's own design with no key beside it and the choice between writing the binding or withdrawing the row is the row already sitting in Decision Now. Worth naming while judging it: `python3 unit.py check` in `4-QBt-page-types/` lists neither drawer record, so no command on this board reports the missing binding and the open row is the only thing holding it visible. The six dated CC records that were sitting in States moved here unchanged, because they are history and States holds what is true now.
- 260806 · [DRAFT-CC] written as the `for-literature` specimen rather than an essay about one, on the ruling that a page-type example IS a page of its type, the way `QB4` is both the page grammar and a page obeying it. Both records were written under `QA-probe/QBt4-for-literature/`, named after this page exactly, and every citation in them is invented.
- 260806 CC · Page written as the `for-literature` specimen and as the outward twin of `QBt5`. The two share a shape on purpose: the same head keys with `route: outward` instead of `inward`, the same E divisions, the same three-H2 record. What differs is what a returned answer must BECOME, which on this route is a citation binding rather than a bound product.
- 260806 CC · Every key on this page and in both records is invented, and invented LOUDLY: `imaginary2031typed`, `nowhere2030drift`, `pretend2029forms`. This repository holds a real MISQ paper with a real bibliography two folders away, so a plausible-looking fake key is a citation waiting to be copied. The keys are unusable on purpose, and the marking sits in the title, in the Opening, and inside every figure that carries one.
- 260806 CC · The two records carry different routes, the way `QBt5`'s do. `1-typed-page-precedent` is `route: discovery` and names a bank that is not cloned here; `2-tenure-confound` is `route: local`, answered by re-reading two sources already on the shelf, so it names no bank. A local answer on the outward route is legitimate and cheap, and its cost is written in its own Caveats: nobody searched, so the absence it reports is bounded by what was already in hand.
- 260806 CC · Neither record declares a `provides:`. A literature answer's product is prose, so `unit.py check` counts this drawer as nothing at all: not an atom, not a view. That is a limit of the resolver on this route rather than a defect in the records, and it is worth naming because the inward twin's two records both resolve while this page's two are invisible to the same command.
- 260806 CC · The pointer line and the caption rule collide, and the collision is resolved here rather than left open. The record anatomy makes `🔗 QA-probe:` the first line of an E division; the group's caption rule makes `**Name**: what this shows.` the first line of any division. Writing both puts the pointer on the page twice, so the pointer rides inside each figure as its first row, keeping the contract's exact wording. ⚠️ REVERSED 260807: that put the pointer inside CODE, where it can never become a link, which is the defect the two rows at the top of this Log record. The collision is real and the resolution was wrong; the caption goes first and the pointer follows it ABOVE the fence.
- 260806 CC · E2 is left without its citation binding on purpose, and it is a DIFFERENT failure from the one `QBt5` leaves. That page leaves an answered question nobody consumes. This page leaves an answer whose consumer never wrote it down: the digest is here, the row is `⬜`, and `QBt3` §3 still argues its label from this paper's own design with no source beside it. On a real outward page this is the quiet one, because the page LOOKS answered and the manuscript is saying the right thing for the wrong reason.
