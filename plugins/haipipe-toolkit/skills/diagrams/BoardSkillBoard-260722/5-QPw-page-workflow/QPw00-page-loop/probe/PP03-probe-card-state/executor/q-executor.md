# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

Across this board, how many probe cards exist, how many bibex entries, how many display units, and how many of each have reached the state that means a person has read or accepted them?

**Where to look**
every <page>/probe/PP*/card.md (state: and read: lines), every <page>/bibex/*.bib, every <page>/display/*/README.md (accepted: line). haipipe-plugin-probe states the ladder; haipipe-plugin-display states the accept rule.

**What a complete answer contains**
three counts per lane, each split into created / landed / person-ticked, plus the count of cards still at `planned` and how long they have been there.

**How to return it**
Numbers with the file or command each came from. A count with no source is not an answer. If a number cannot be produced, say which input is missing rather than estimating it.
