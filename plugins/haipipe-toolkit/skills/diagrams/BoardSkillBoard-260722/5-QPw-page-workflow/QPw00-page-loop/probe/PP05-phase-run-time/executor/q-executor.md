# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

How long does each phase take, measured from the timestamps of stored receipts rather than estimated?

**Where to look**
_runs/page/**.json receipt timestamps. The four phases with no receipt anywhere (OUTLINE, DRAFT, PROBE, EVIDENCE) must each be run once before this can be answered at all.

**What a complete answer contains**
one measured duration per phase, with the run and receipt each came from, and an explicit NOT MEASURED for any phase with no receipt.

**How to return it**
Numbers with the file or command each came from. A count with no source is not an answer. If a number cannot be produced, say which input is missing rather than estimating it.
