# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

What does the deterministic lifecycle auditor return for each stored run, and which fault codes does it emit?

**Where to look**
haipipe-board/src/page_lifecycle.py is the auditor; run it over every file under _runs/page/. Its codes are defined in that module.

**What a complete answer contains**
one verdict per run, PASS or FAIL, with each emitted fault code and the receipt index that produced it, quoted from the auditor's own output rather than summarised.

**How to return it**
Numbers with the file or command each came from. A count with no source is not an answer. If a number cannot be produced, say which input is missing rather than estimating it.
