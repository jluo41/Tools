# Q-consumer · PP04-auditor-verdict

**The question, as this page needs it**
What does the deterministic lifecycle auditor return for each stored run, and which fault codes does it emit?

**What this page loses if it never comes back**
§14 claims the loop is auditable and that quality is evidenced rather than declared. An audit nobody has run over the board's own receipts leaves that claim self-referential; Aim A14.1 asks for both a passing and a failing test, and a real FAIL on our own run is the failing half.

**Where it is used**
`C14.P1.B5` in `outline/QPw00-page-loop-outline-v2.md`, and the sentence that bullet becomes.
