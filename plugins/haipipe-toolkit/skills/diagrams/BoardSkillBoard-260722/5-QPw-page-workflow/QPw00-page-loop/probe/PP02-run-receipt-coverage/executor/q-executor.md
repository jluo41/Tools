# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

Which runs are stored under the board's _runs/page/ tree, how many receipts does each hold, and which phases appear across all of them?

**Where to look**
BoardSkillBoard-260722/_runs/page/**.json — each file is one run; each receipt inside names its phase. Cross-read with page-workflows/haipipe-page-workflow/ref/page-run-contract.md for the receipt shape.

**What a complete answer contains**
one row per stored run: run id, receipt count, the ordered phase sequence, and the terminal route. Then the union of phases covered, and the complement against the seven the loop declares.

**How to return it**
Numbers with the file or command each came from. A count with no source is not an answer. If a number cannot be produced, say which input is missing rather than estimating it.
