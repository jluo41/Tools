# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

How many phases does the page workflow declare, how many of them ship a contract file, and how many person-reserved ticks does the set of contracts define?

**Where to look**
Tools/plugins/haipipe-toolkit/skills/board/page-workflows/ — one folder per contract, plus haipipe-page-workflow/ref/phase-cards.md which states every phase in six fields including its TICK row.

**What a complete answer contains**
four integers and the list behind each: the phase count, the contract count with their versions and line counts, the tick count with the artifact each tick sits on, and the run count with the phases those runs covered.

**How to return it**
Numbers with the file or command each came from. A count with no source is not an answer. If a number cannot be produced, say which input is missing rather than estimating it.
