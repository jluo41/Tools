# a-executor

Read from disk on 260819.

**Phases the loop declares: 7** — OUTLINE, DRAFT, PROBE, EVIDENCE, REVISE, COMPILE, CHECK.
`ref/phase-cards.md` carries six `##` headings, not seven, because ⑥ COMPILE shares ⑤ REVISE's heading. The phase is declared; it has no card of its own.

**Contracts that ship: 6**, one folder each under `page-workflows/`:

```
OUTLINE   haipipe-page-outline    0.1.2   116 lines
DRAFT     haipipe-page-draft      0.7.2   250
PROBE     haipipe-page-probe      0.4.2   252
EVIDENCE  haipipe-page-evidence   0.9.0   353
REVISE    haipipe-page-revise     0.5.0   164
CHECK     haipipe-page-check      0.6.1   211
COMPILE   —                       —       no contract file exists
```

`haipipe-page-workflow` 0.7.0, 302 lines, also lives there and is the ROUTER, not a phase, so it is not in the count.

**Person-reserved ticks: 5** — `approved:` on the outline file, `verified` on each bibex entry, `read:` on each probe card, `accepted: ✅` on each display README, and the Page Type's RULING. Two revert when their inputs change: `read:` and `accepted:`.

**Runs executed: not answered here.** That question is `PP02-run-receipt-coverage`'s, and answering it in this card would put one number in two places.
