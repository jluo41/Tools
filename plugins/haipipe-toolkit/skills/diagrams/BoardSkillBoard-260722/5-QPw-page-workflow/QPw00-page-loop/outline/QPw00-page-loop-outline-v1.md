# QPw00-page-loop · outline v1
outline-version: v1
supersedes: —
date: 260818
approved: ✅ JL 260818 1543, "ok, approved" · ⬜ auto 260818 · R3 fails: 6 bullets carry no mark (C1.P1.B3, C1.P1.B4, C2.P1.B3, C2.P2.B4, C3.P1.B3, C4.P1.B3)

Written for the REVISE round the page's own `state:` line already names ("§1-§4
hand off to QPw1-QPw6") and the Opening's own 260817 warning ("§1 to §4 are
owed a REVISE round").
Purpose and Aims stay fixed: this plan only shrinks the REALIZATION of
Content divisions 1 to 4, so it is REVISE material, not a reopened round.
UNAPPROVED, so it is a working document: rewrite it, delete what is wrong.

Out of scope, and untouched by this plan: divisions 5, 6, 8, 9, 10, which the
assignment names explicitly as staying; divisions 7, 11, 12, which nobody
asked to touch this round, including §7's own note that it is superseded by
the 260818 restructuring (a separate open question); the page's Aims and
States ids, which keep A1.1, A2.1, A2.2, A2.3, A3.1, A4.1 exactly as
numbered; and the open Decision Now row on Q-consumer ownership transfer
(A2.2), which this plan does not resolve.

Read against QPw2-draft, QPw3-probe, QPw4-evidence, QPw5-revise and
QPw6-check: each already carries a fuller, more current version of what
divisions 1, 3 and 4 argue, and of most of what division 2 argued before the
260817 PROBE/EVIDENCE split. That is what makes shrinking safe rather than
lossy, except for §2.4 to §2.6, which no QPw page yet restates.

## C1 · DRAFT gives the Page a promise

### C1.P1 · one short paragraph, pointing at QPw2-draft
- B1 · keep the existing face-card (✍️ DRAFT enters · owns · writes · names ·
  exits) as the division's compact summary; its wording still matches
  DRAFT's current meaning and needs no correction.                    ✅ have it
- B2 · replace #### 1.1, 1.2 and 1.3's full argument (design authority over
  first typing, every editing operation, exposing uncertainty) with one
  short paragraph naming that DRAFT's full contract, its worked
  Point-to-scaffold example, and its Aim-is-the-stake rule now live at
  QPw2-draft.                                                          🎯 A1.1
- B3 · name, in that same paragraph, that OUTLINE (QPw1-outline) is the
  phase now immediately upstream of DRAFT, since §1 predates the 260817
  split and never had to say so.
- B4 · delete #### 1.1, 1.2 and 1.3 as separate paragraphs once the pointer
  paragraph is written; nothing here needs re-arguing, because QPw2's own
  §§1-4 carry the same authority test on its own worked example.

## C2 · PROBE resolves what the Page cannot know

### C2.P1 · one short paragraph, pointing at QPw3-probe and QPw4-evidence
- B1 · rewrite the existing face-card (currently `🔎 PROBE` enters
  question id/stake/source route, writes QA-probe, RETURNS result/source/
  limits/status): that mixes PROBE's post-260817 job with EVIDENCE's, so it
  is stale and needs correcting, not just keeping.                     🎯 A2.1
- B2 · replace #### 2.1 (PROBE begins with a consequential unknown),
  #### 2.2 (PROBE writes beside the target Page), and #### 2.3 (PROBE is
  optional and repeatable) with one short paragraph: the trigger, the
  write-surface split, and the optional/repeatable rule now live at
  QPw3-probe and QPw4-evidence respectively.                           🎯 A2.1
- B3 · state plainly, in that same paragraph, that PROBE's own meaning
  changed in 260817: it now only raises the card and asks, and landing the
  answer is EVIDENCE, so a reader is not left holding this page's older
  260804 sense of the word (the Opening banner already says this once; the
  division should not make a reader learn it twice in two different words).

### C2.P2 · keep #### 2.4, 2.5 and 2.6 in place, unshortened
- B1 · #### 2.4 "Filing a QA-probe does not identify the target Page"
  stays: no QPw page yet states the origin-owned Q-consumer rule against
  the evidence-route filing case.                                      ✅ have it
- B2 · #### 2.5 "One active Page bounds the consumer write" stays for the
  same reason: the sibling-handoff boundary is argued only here.       ✅ have it
- B3 · #### 2.6 "A projection can show the full chain" stays for the same
  reason: it is the only place the read-only-projection requirement is
  argued, and Aim A2.3 depends on it.                                  ✅ have it
- B4 · do not resolve the open Decision Now row on Q-consumer ownership
  transfer (A2.2) this round; leave it exactly as written, and leave Aims
  A2.2 and A2.3's States rows citing "sections 2.4 to 2.6" unchanged.

## C3 · REVISE makes the current promise work

### C3.P1 · one short paragraph, pointing at QPw5-revise
- B1 · keep the existing face-card (🧵 REVISE enters · holds · owns ·
  updates · exits) as the division's compact summary; REVISE's meaning did
  not change in the 260817 split and needs no correction.              ✅ have it
- B2 · replace #### 3.1 (execution under a fixed promise), #### 3.2 (the
  DRAFT/REVISE test), and #### 3.3 (REVISE may discover a gap but may not
  invent its answer) with one short paragraph: the fixed-promise test, the
  five-step LAND/ARGUE/SOUND/RENDER/BUILD order, and the display-walk split
  now live at QPw5-revise, which also carries COMPILE.                 🎯 A3.1
- B3 · name, in that same paragraph, that §6 on this page (Operations route
  by reason) still carries the shared DRAFT-versus-REVISE test table, so a
  reader is pointed sideways as well as forward.

## C4 · CHECK decides where the current version goes next

### C4.P1 · one short paragraph, pointing at QPw6-check
- B1 · keep the existing face-card (🧑 CHECK enters · owns · writes ·
  routes · avoids) as the division's compact summary; CHECK's meaning did
  not change in the 260817 split and needs no correction.              ✅ have it
- B2 · replace #### 4.1 (CHECK tests a version, not an abstract Page),
  #### 4.2 (CHECK has five meaningful outcomes), and #### 4.3 (CHECK does
  not become a hidden revision phase) with one short paragraph: the
  judge/repair separation, the three independent built-artifact counts,
  and the accept-biased human gate now live at QPw6-check.             🎯 A4.1
- B3 · name, in that same paragraph, that §10 on this page (Audit proves
  process claims) still carries the receipt-and-invariant material CHECK is
  measured against, so nothing here is left an orphaned reference.
