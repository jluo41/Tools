# QPw00-Display7-point-to-sentence

- claim: DRAFT's whole job is one conversion a reader can walk in BOTH directions: one approved outline POINT (`C3.P1.B4`) becomes one or more sentence scaffolds (`C3.P1.S1`-`S3`) whose holes sit exactly where their answers will sit, and the join survives edits because it is carried twice, in the shared `C3.P1` address prefix (only the last token differs, B for a point in the plan, S for a sentence on the page) and in the machine-readable `<!-- realizes: C3.P1.B4 -->` comment on each scaffold line, which is what lets REVISE split or merge sentences without losing the point
- kind: tex, TeX-native (a hand-authored `tikz` block; the writer is a person, the recipe IS the figure)
- caption-job: let a reader DO the conversion, both ways round, without opening either contract: given a bullet, write its scaffold with the address carried across; given a scaffold sentence, recover its point from the `realizes:` comment; and say why B and S never collide (two owners, one shared prefix)
- intake: intake/manifest.yaml freezes 2 files as a LISTING (concept unit, no probe card serves it and none is needed): `board/page-workflows/haipipe-page-draft/SKILL.md` 0.8.2 sha256 fab3b386... (the worked example C3.P1.B4 -> S1/S2/S3, the hole markers, the realizes: comment, the never-invent rule, all from its "Point -> sentence scaffold" section) and `board/page-plugins/haipipe-plugin-outline/SKILL.md` 0.12.0 sha256 94e84996... (one POINT becomes ONE OR MORE sentences, the shared-prefix join, B owned by haipipe-plugin-outline and S by haipipe-sentence)
- fragility: fully transcription-bound. If the draft contract's worked example changes its addresses or hole names, if the realizes: comment moves off the scaffold line, if the B/S ownership moves (haipipe-sentence losing S, or the plan letter changing), or if the join stops being the address prefix, this figure is wrong; the two sha256 in intake/manifest.yaml are what make that computable. A known tension it inherits rather than resolves: the frozen worked example is hole-heavy while the same contract's 260819 ruling (and this page's C3.P1.B1) says DRAFT now enters on landed evidence so a hole is the EXCEPTION; the contract itself kept the example unchanged after the ruling, so the figure transcribes it verbatim and leaves the tension to the source
- renderer: CC as the TeX hand via haipipe-display-tex, `tikz` (arrows.meta, positioning); rebuild = `cd recipe && pdflatex asset.tex && cp asset.pdf ../assets/figure.pdf`, then `pdflatex preview.tex` at the unit root
- picked: 260819 CC · one candidate, plan box over page box with the two arrows in the gap (solid down = the conversion, dashed up = the recovery) and the address-join law as its own third box, because "both ways round" is the bullet's own ask and two opposed arrows are the only layout that shows it without prose. Two layout passes were wrong before it: pass 1 (side-by-side boxes) clipped the page box at the paper edge and ran both arrow labels through each other and through the join box; pass 2 (vertical) still collided the two gap labels at center and wrapped S2's realizes: comment onto its own orphan line; both defects were invisible in the source and obvious in the rendered PNG. Fixed by widening to 140mm text width, pushing the arrows to +-66mm, and shortening the page-box header
- accepted: ⬜  a person has not looked at it yet; the tick is ⑦ CHECK's, never this producer's
- serves: C5.P2.B1
- history: born 260819 in the EVIDENCE phase of the PREPARE round on QPw00-page-loop, the last owed 🖼 mark on outline v3. The mark rename in the plan (`🖼 owed · tex` -> `🖼 Display7 · tex` on C3.P2.B1) is NOT done: the dispatching instruction forbade outline edits for this build (Display5 was being rebuilt concurrently), so the one-line rename is owed by the caller
