% One row per step of the page workflow, in the order the loop runs it since
% 260819: the PREPARE loop 1 OUTLINE / 2 PROBE / 3 EVIDENCE first, then 4 DRAFT,
% 5 REVISE (6 COMPILE folded in) and 7 CHECK. Every cell is transcribed from a
% file frozen in intake/inputs/; the TIME column is the only estimate and is
% labelled as one.
\newcommand{\band}[1]{%
  \midrule
  \multicolumn{6}{l}{\cellcolor{black!7}\footnotesize\itshape\bfseries #1}\\
  \midrule}
\newcommand{\you}{\textbf{YOU}~}
\newcommand{\rr}{\raggedright}
\newcommand{\loopy}{$\circlearrowleft$~}
\newcommand{\cost}[1]{{\footnotesize$\leftarrow$ \itshape #1}}
\newcommand{\note}[1]{%
  \multicolumn{6}{@{}p{122mm}}{\footnotesize\itshape\quad #1}\\}
\renewcommand{\arraystretch}{1.12}
\footnotesize
\begin{minipage}{124mm}
\begin{tabular}{@{}c@{~} l@{~} p{31mm} p{28mm} r@{~~} l@{}}
\toprule
\textbf{\#} & \textbf{PHASE} & \textbf{WHAT HAPPENS} & \textbf{YOUR JOB} &
\textbf{TIME} & \textbf{HOW OFTEN}\\

\band{once per page}
0 & OPEN & \rr \texttt{haipipe-page} CREATE copies the page template and registers
    the page in \texttt{board.md} (a VERB, not a phase) &
    \rr \you write the title so it states the page's PURPOSE & 15 min & once\\

\band{the loop --- CHECK may send you back to rows 1, 2, 3, 4 or 5}
1 & OUTLINE & \rr a machine writes the SHAPE only, into
    \texttt{outline/...-v<N>.md} and nothing in the page itself; then FOUR checks
    run: coverage, address, value, shape &
    \rr \you judge the plan's DIRECTION, never its arithmetic, then tick
    \texttt{approved:} \newline \cost{the four run BEFORE you are asked; this is
    the loop's one gate} & 10 min & \loopy each pass\\

2 & PROBE & \rr MATCH first (this page, then PageX, then the QA bank), and only then
    a card at \texttt{probe/PP<NN>-<slug>/}; the stripped Q-executor is the only
    thing that crosses & \rr nothing & 0 & \loopy each pass\\

3c & CITATION & \rr a machine may SUBSET or TRANSCRIBE a real record and may never
    COMPOSE one &
    \rr \you land the entry verbatim in \texttt{bibex/}, so it carries
    \texttt{verified = \{WHO YYMMDD\}} & 20 min & \loopy per entry\\

3v & VALUE & \rr the bank answers into its own QA file, \texttt{target:} names that
    file BY PATH, and \texttt{checks/values.py} re-runs the number &
    \rr \you tick \texttt{read:}, which now means ``I agree with the judgment inside
    the question'' and not ``I checked the arithmetic'' \newline \cost{it reverts
    when target or proof moves} & 30 min & \loopy per card\\

3d & DISPLAY & \rr this lane DRAWS since 260819: intake, recipe, the pick, then
    \texttt{assets/} and \texttt{preview.pdf}. It returns a previewable unit
    rather than a folder &
    \rr \you rule \texttt{intake/} and choose among \texttt{candidates/} \newline
    \cost{no tick here: \texttt{accepted:} is row 7a's} & ? & \loopy per unit\\

\note{Rows 1, 2 and 3 REPEAT AS ONE UNIT until the plan and its evidence agree,
    and 3c, 3v and 3d run at the SAME TIME. Three of the five person-reserved
    ticks are in this band; the other two are 7a and 7b.}

4 & DRAFT & \rr it now ENTERS on evidence that already landed, so it writes the
    NUMBER; a hole is the EXCEPTION and must name the input that is missing &
    \rr \rr nothing & 0 & per round\\

5 & REVISE & \rr the sentence that cites each drawn unit by id, the caption for THIS
    page's claim, and the \texttt{latex/} and \texttt{word/} rebuild. It no
    longer draws & \rr nothing & 0 & \loopy each round\\

6 & COMPILE & \rr folded into 5 since 260819 and holding no contract of its own;
    whether the fold is permanent is \texttt{QPw5-revise}'s open ruling &
    \rr \rr nothing & 0 & with 5\\

\note{4, 5 and 6 run END TO END and stop for nobody: a run that pauses for a
    person between DRAFT and CHECK is stopping in the wrong place.}

7 & CHECK & \rr a DIFFERENT actor judges the RENDERED page and the BUILT artifact,
    and may not repair its own finding in the same pass &
    \rr \you take CLOSE, or name where to go back \newline
    {\footnotesize$\hookrightarrow$ REVISE, DRAFT, EVIDENCE, PROBE or OUTLINE} &
    15 min & \loopy each round\\

\band{what ends it --- both ticks below are row 7 CHECK's own}
7a & ACCEPT & \rr you are shown this gate only once the page's mechanical errors are
    ZERO, so it is a confirmation and not an inspection &
    \rr \you tick \texttt{accepted:} in each unit's README \newline \cost{a changed
    \texttt{intake/} drops it back to $\square$} & 10 min & per unit\\

7b & CLOSE & \rr CLOSE is a ROUTE and not a phase, and only \texttt{verdict: pass}
    may take it &
    \rr \you sign the Page Type's declared RULING \newline \cost{silence is not
    consent} & 5 min & per round\\
\bottomrule
\end{tabular}

\par\vspace{1.5mm}
\parbox{124mm}{\scriptsize\itshape TIME is an ESTIMATE and nothing else in this
table is. The receipt shape carries no start/end pair, so no receipt can time a
phase and not one phase here has ever been timed. Across this board's four
stored runs the receipts cover OUTLINE, PROBE, DRAFT, REVISE and CHECK (PROBE's
first two landed only on 260819), and EVIDENCE and COMPILE had none when this
table was frozen that evening. Row 3d carries no estimate at all, because it
took over three of REVISE's steps on 260819 and nobody has priced the lane
since. Every other cell is transcribed from a file frozen in
\texttt{intake/inputs/}.}
\end{minipage}
