# QPw00-Display1-run-loop

- claim: RUN is a bounded router, not a conveyor belt — it takes one packet, alternates a producer, a mechanical builder, and a fresh judge, follows only the route each returns, refuses a route outside $R(\varphi)$, and stops at CLOSE, HOLD, or a limit that never means the Page passed
- kind: tex, TeX-native (an `algorithm2e` block; the writer is a person, the recipe IS the figure)
- caption-job: let a reader see the loop's control flow and its three separate actors at once, so §9's prose has something to point at instead of being read four times
- fragility: none data-bound; the block states no number. What it does bind is transcribed text, and `intake/manifest.yaml` names the three files every phase name, route, and stop condition was copied from
- renderer: CC as the TeX hand; rebuild = `pdflatex preview.tex`, then `recipe/asset.tex` for `assets/figure.pdf`
- picked: 260816 CC · one candidate; the first compile wrapped four comments and left a third of the page blank, so the geometry went 185x175mm to 224x132mm and two comments were shortened
- accepted: ⬜ auto 260818 · display-rules R7 fails: `preview.pdf` (260816 1543) and `assets/figure.pdf` are older than `recipe/run-loop.tex` (260818 1038); the edit is comment-only (`QPw1 §9.1` → `QPw00 §9.1`), so `pdflatex preview.tex` then `recipe/asset.tex` clears it. R10 unevaluated, not applicable to this kind: an `algorithm2e` block has no numeric axis. R1-R6, R8, R9, R11-R14 pass
- history: born 260816 in the Page-Workflow session, when JL asked for the loop to be illustrated on the page that defines it
