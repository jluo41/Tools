# QPw00-Display1-run-loop

- claim: RUN is a bounded router, not a conveyor belt: it takes one packet, alternates a producer, a mechanical builder, and a fresh judge, follows only the route each returns, refuses a route outside $R(\varphi)$, and stops at CLOSE, HOLD, or a limit that never means the Page passed
- kind: tex, TeX-native (an `algorithm2e` block; the writer is a person, the recipe IS the figure)
- caption-job: let a reader see the loop's control flow and its three separate actors at once, so §12's prose has something to point at instead of being read four times. §12 was §9 until the page was re-cut on 260819; the citing sentence is now the last line of §12's Overview block
- intake: `intake/manifest.yaml`, frozen 260819 (re-frozen the same evening after the PREPARE-loop route rewrite) in the `file:` + `source:` + `sha256:` shape · five LIVE files hashed: `board/haipipe-board/ref/page-lifecycle.workflow.js` `c91c028023`, `board/haipipe-board/src/page_lifecycle.py` `9465025ae5`, `board/page-workflows/haipipe-page-workflow/SKILL.md` `9b87038910`, `.../ref/page-run-contract.md` `59e77e9d14`, and the page itself `6126f576dd` · from no probe card: a concept unit freezes the LISTINGS it asserts, not a `proof/`
- fragility: none data-bound; the block states no number. What it does bind is transcribed text, so it goes wrong when an ACTOR, a STOP, or an ENTRY DEFAULT moves, never when the phase order does: the figure names one phase, CHECK, and reads every route out of $R(\varphi)$ without enumerating it. `intake/manifest.yaml` names the five live files those facts live in and hashes each one, so `checks/intake.py` can say whether any of them has moved since
- renderer: CC as the TeX hand; rebuild = `pdflatex preview.tex` at the unit root, then `cd recipe && pdflatex asset.tex && cp asset.pdf ../assets/figure.pdf`
- picked: 260816 CC · one candidate; the first compile wrapped four comments and left a third of the page blank, so the geometry went 185x175mm to 224x132mm and two comments were shortened. 260819 re-verified rather than redrawn: the loop's own re-cut, not a new design, is what changed two lines
- looked at: yes, 260819 evening, after the rebuild below. `preview.pdf` renders 15 numbered lines with no wrap, no clipping and no collision, and line 1 now reads OUTLINE. `assets/figure.pdf` is the `standalone` crop, so its narrower measure soft-wraps trailing comments with a hanging indent; line 1's comment now joins that wrapped set because OUTLINE is two characters longer than DRAFT. Same behavior class as the accepted 260816 build, left alone rather than re-geometried
- accepted: ⬜ nobody has accepted the CORRECTED drawing. The previous note here was a 260818 R7 staleness report (`preview.pdf` and `assets/figure.pdf` older than `recipe/run-loop.tex`, believed comment-only); that is now resolved, but not the way the note predicted. Both artifacts were rebuilt 260819 and the recipe edit was NOT comment-only: line 5 of the block asserted the wrong actor. What a person must judge is the three changed lines: 5 and 13 (morning pass) and line 1's entry comment, DRAFT → OUTLINE (evening refresh)
- contradictions: recorded, not resolved (260819 evening refresh):
  - `QPw00-page-loop.md` §12.2 (lines 622-623) still says "A new Page is first created and registered, then RUN starts it at DRAFT", while `ref/page-run-contract.md` says RUN "begins at OUTLINE, which has been phase ① since 260817" and `SKILL.md` §🔁 step 1 says "start at OUTLINE, phase ① since 260817". The figure follows the contract, which the manifest names as the authority for the entry sentence; the page's stale sentence is the page's to fix, not this unit's
  - `page-lifecycle.workflow.js` meta `phases[0].detail` still enumerates three producer phases ("a phase-scoped producer performs DRAFT, EVIDENCE, or REVISE"), contradicting its own `if (current === 'CHECK')` dispatch and SKILL.md §🔁 step 2's six-producer list. Pre-existing (identical in the morning freeze); the figure sides with the executable test, which is why it prints $\varphi \neq$ CHECK and no list. RESOLVED upstream at the late-evening freeze: the detail now reads "a phase-scoped producer performs any phase except CHECK", matching its own dispatch test
- serves: C12.P2.B1
- history:
  - 260816 · born in the Page-Workflow session, when JL asked for the loop to be illustrated on the page that defines it
  - 260818 · `sources:` turned into real frozen files, four inputs with sha256, in the `path:` + `sha256` shape
  - 260819 · re-frozen and MINIMALLY CORRECTED. `checks/intake.py` reported `haipipe-page-workflow.SKILL.md` and `page_lifecycle.py` CHANGED and two more inputs `unresolved`. Verdict on the drawing: **the figure changed, but not because the phase order did.** The 260819 reorder (① OUTLINE ⇄ ② PROBE ⇄ ③ EVIDENCE, then ④ DRAFT → ⑤ REVISE → ⑦ CHECK) does not enter this algorithm at all, because the algorithm reads its next phase out of $R(\varphi)$ and never draws the order. What DID make it false was the 260817 four-to-seven phase split, which nobody re-checked the drawing against:
    - line 5 said `if $\varphi \in \{$DRAFT, PROBE, REVISE$\}$ then Producer, else Judge`. With seven phases that sends OUTLINE, EVIDENCE and COMPILE to the fresh read-only judge. The controller tests `if (current === 'CHECK')`, and SKILL.md §🔁 step 2 names six producer phases. Now `if $\varphi \neq$ CHECK`, which is what the code tests and cannot go stale when the inventory is re-cut again
    - line 13 counted a round on any DRAFT route with `reopens`. The controller excludes `current !== 'DRAFT' && current !== 'OUTLINE'`, and after 260819 OUTLINE → DRAFT is the NORMAL forward edge out of the PREPARE loop, so the old line burned a round on every ordinary run and would have hit `max_rounds=3` early. Now `$\varphi \notin \{$OUTLINE, DRAFT$\}$` as well
    - manifest converted to `file:` + `source:` + `sha256:` against the LIVE files, with `page-run-contract.md` added as a fifth input because the `\Input` packet line is transcribed from it, and the `takes:` notes kept as comments
    - NOT changed: the entry comment "an existing Page enters at CHECK, a new one at DRAFT". Both `ref/page-run-contract.md` and the page's own §12.2 still say exactly that, so the figure matches its authority. ⚠️ Whether that default should now be OUTLINE is a question for the contract, not for this unit, and it is outside this folder
  - 260819 late evening · RE-FROZEN at EVIDENCE with NOTHING redrawn, after the per-phase producer-agent roster landed in three of the five inputs (`SKILL.md` 0.15.0 §👷, `page-run-contract.md`, `page-lifecycle.workflow.js` `PRODUCER_AGENTS` and the ⑦ judge's rename to `haipipe-page-check-agent`). The roster never enters this drawing: the block names \Producer, \Builder and \Judge generically, its branch tests $\varphi \neq$ CHECK, which is byte-for-byte what the controller still tests, and the entry comment (an existing Page at CHECK, a new one at OUTLINE) was re-read against the contract and still holds. Three hashes re-pinned; one recorded contradiction (the js meta `phases[0].detail`) dissolved upstream, noted above
  - 260819 evening · REFRESHED after the PREPARE-loop route rewrite landed in all five inputs (`checks/intake.py` flagged every input CHANGED against the morning freeze). One transcribed fact had gone false: the entry comment on the $\varphi \leftarrow \varphi_0$ line said "a new one at DRAFT" and both `ref/page-run-contract.md` and SKILL.md §🔁 step 1 now say a new Page begins at OUTLINE, so the comment now says OUTLINE, and the morning row's "NOT changed" verdict on that line is superseded. Everything else was re-verified unchanged, exactly because line 5 and line 13 were future-proofed in the morning pass: the LEGAL_ROUTES rewrite (OUTLINE→PROBE, PROBE→OUTLINE, EVIDENCE→OUTLINE legal; EVIDENCE→DRAFT gone) never enters a figure that reads $R(\varphi)$ without enumerating it, the $\varphi \neq$ CHECK test is still what the controller tests, and the round guard $\varphi \notin \{$OUTLINE, DRAFT$\}$ still matches the controller and the auditor's `should_increment`. Both artifacts rebuilt (`pdflatex preview.tex`; `recipe/asset.tex` → `assets/figure.pdf`), intake re-frozen against the live files, and this unit is off the `checks/intake.py` report. Two contradictions recorded above, neither resolved here
