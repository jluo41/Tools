# QPw00-Display5-evidence-logic

- claim: the evidence a page lands is HEADED by the probe card — a bullet of the approved plan ASKS, ② PROBE raises the card whose `serves:` points back at that bullet, and the two-stage (MAKE, BIND) three-lane wall draws on it unevenly: the value lane fills the card itself and each landed number gets its own id (PP01.v2), a DATA display freezes its intake FROM the card's proof/, and the citation lane is a person's and does not pass through the card
- kind: tex, TeX-native (a hand-authored `tikz` block; the writer is a person, the recipe IS the figure)
- caption-job: let a reader see the whole of §5 at once — where the question comes from and what folder holds it, the two stages, which tick belongs to which lane, which two ticks revert, and the line the phase stops at — so the division's subsections have something to point at
- fragility: fully data-bound to the contracts. Every stage, lane, tick, revert rule and card field is transcribed, so `intake/manifest.yaml` freezes eight files with sha256; a moved tick, a renamed lane or a changed card field makes this figure wrong and computably so
- how it differs from the four units already here: Display1 is RUN's control flow, Display2 is the seven-row legal-route relation, Display3 prices the loop as work, Display4 orients a reader across all seven phases. This one opens ONE phase, with the card that feeds it
- renderer: CC as the TeX hand, `tikz`; rebuild = `cd recipe && pdflatex asset.tex` for `assets/figure.pdf`, then `pdflatex preview.tex` at the unit root
- picked: 260819 CC · one candidate, two columns for the two stages crossed with three rows for the three lanes, because the parallelism is the fact the figure exists to show
- picked: 260819 CC · the card sits LEFT of the wall as its head, not above it, because its three relations to the lanes are horizontal edges a reader can follow (fills the card · freezes from proof/ · no edge at all); an above-the-wall head would cross the citation lane to reach the other two
- accepted: ⬜ nobody has looked at it yet. Rendered 260819 with the intake frozen from 8 files
- serves: C4.P1.B1
- history: born 260819 in the EVIDENCE phase, after JL asked for the C5 logic to be drawn ("我觉得你应该在 C5 里面加 display，把整个 logic 都给画出来")
- history: refreshed 260819, same day, after two frozen inputs moved under it: `haipipe-page-evidence` 0.10.0 → 0.10.2 and `phase-cards.md` (EVIDENCE is now phase ③, routes to ① OUTLINE always, and ③d leaves no tick behind: its `accepted:` is ⑦ CHECK's). One cell changed in the drawing: the display lane's tick note now reads "reverts · ticked at (7) CHECK", and the caption says accepted is CHECK's. MAKE/BIND, all six stage-and-lane cells, the revert pair (`read:` · `accepted:`), and the ⑤ REVISE line below the wall were re-verified against the live contracts and are unchanged. Layout only: stage and lane boxes got fixed text widths and the preview page widened to 150mm, because the first look at the rebuilt preview.pdf showed the tick column clipped at the page edge and the two stage headers colliding (a birth defect, visible only in the picture). Intake re-frozen, all six sha256 current
- history: redrawn 260819, same day, after JL read it and named the hole ("I think for the display5, you didn't mention the probe itself. How could you put the probe there as well?"). The PROBE joins as the HEAD: the approved plan's bullet asks, ② PROBE raises the card (question, serves: C4.P1.B4 pointing BACK, consumer/, executor/, proof/, and the `## Values` ids PP01.v1.. written at ③ when the answer lands), and edges show which lanes draw on it — value fills the card itself, a DATA display freezes intake/ FROM proof/, citation is a person's and takes no card unless the key is unknown. One old cell corrected against the live contract: the display MAKE cell now lists `intake` among what is drawn (`haipipe-page-evidence` §🧱 lists it; the old drawing omitted it). The wall, ticks, reverts, stop line and OUTLINE loop re-verified and unchanged. Intake re-frozen at eight files (adding `haipipe-page-probe` 0.6.x and `haipipe-plugin-value` 0.1.1), all sha256 current. Three preview looks caught and fixed: the card overlapping the mark box and the stop line, the display-edge label colliding with the [D] cell, and the [D] box crossing the dashed line
