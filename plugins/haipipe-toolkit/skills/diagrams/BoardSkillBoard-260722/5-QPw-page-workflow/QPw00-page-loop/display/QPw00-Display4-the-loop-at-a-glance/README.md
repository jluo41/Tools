# QPw00-Display4-the-loop-at-a-glance

- claim: since 260819 the loop is not a line. ① OUTLINE, ② PROBE and ③ EVIDENCE repeat as one converging PREPARE loop that only a person's `approved:` tick (after four machine checks: coverage · address · value · shape) lets the page out of; ④ DRAFT and ⑤ REVISE (⑥ COMPILE folded in, no contract of its own) then run unattended end to end; and ⑦ CHECK, which a person judges, alone reaches CLOSE and may return the page to any earlier phase. Five stops on the whole loop are a person's and no machine may write one.
- kind: tex, a TikZ-native figure hand-written in `recipe/loop-at-a-glance.tex` (door: haipipe-display-tex); not a data plot and not a table
- caption-job: give §1 Overview the picture a first-time reader needs BEFORE any single phase is argued, so they can say what the seven phases are, which one they are in, and that their attention belongs to the PREPARE loop
- intake: 9 files under `intake/inputs/`, sha256 of the LIVE files in `intake/manifest.yaml`: `ref/phase-cards.md`, the six phase SKILL.md contracts (outline 0.5.0 · probe 0.6.0 · evidence 0.10.4 · draft 0.8.2 · revise 0.5.0 · check 0.6.1), and both legal-route tables (`ref/page-lifecycle.workflow.js` + `src/page_lifecycle.py`, each carrying the 260819 PREPARE edges). `checks/intake.py` audited this unit 260819 and again after the late-evening re-freeze: 0 CHANGED, 0 unresolved
- fragility: fully data-bound to the contracts. A renamed phase, a moved tick, a changed legal route (EVIDENCE gaining a route to DRAFT would break the "ONE gate out" panel outright), or COMPILE regaining its own contract makes this figure wrong, and the intake makes each of those computable. The attended/unattended zones hang on prose (`phase-cards.md` §🧑), so a re-ruling of where a person's attention sits flips the zones and only the frozen hash would say so
- how it differs from the other units here: Display1 draws RUN's control flow for an implementer, Display2 is the legal-route relation R row by row, Display3 prices the loop as a person's work, Display5 argues the evidence logic. This one is the ORIENTATION figure and carries no algorithm, no relation table and no cost
- renderer: CC as the TeX hand, tikz; rebuild = `cd recipe && pdflatex asset.tex && cp asset.pdf ../assets/figure.pdf`, then `pdflatex preview.tex` at the unit root. Natural width 151.5mm (429.5pt), so a page embedding it at `width=.85\linewidth` (140.3mm at 11pt, 1in margins) scales it 0.926x and the smallest 8pt type prints at 7.4pt
- picked: 260819 CC, two candidates across the day. The morning one (a left-to-right seven-box chain, two return arrows) was WRONG by evening: it drew the pre-260819 linear order, and JL caught it reading the page ("things here is still the old way"). The rebuilt drawing is two-part, loop over tail, per the same day's ruling. One wording fix on the rebuild's rebuild: PROBE's role row said "machine, unattended" inside the zone labeled "a person attends here", which no contract says; `phase-cards.md` gives PROBE `✋ TICK none`, so the row now reads "machine, no tick"
- contradictions: none among the nine frozen inputs. Three were found mid-rebuild and all three were fixed upstream BEFORE this freeze: `phase-cards.md` route rows and tick table still carried pre-260819 phase numbers (④ EVIDENCE · ③ PROBE · ④c/④v), its §🧑 counted "three ③ evidence ticks" against §🧾 placing `accepted:` at ⑦, and `haipipe-page-evidence` line 51 still recited the old order with DRAFT second. The frozen copies are the corrected versions (probe 0.5.1, evidence 0.10.2)
- accepted: ⬜ nobody has looked at it yet; a person still has to say whether it orients them. It awaits a person and this unit never ticks it
- serves: C1.P2.B1 — outline v3 §C1.P2.B1 asks for "the whole loop on one picture: seven phases, who acts at each, which five stops need a person, and where a round restarts", and is already marked `🖼 Display4 · the-loop-at-a-glance`
- history: RE-FROZEN 260819 late evening at EVIDENCE, nothing redrawn. `checks/intake.py` reported 6 of 9 inputs CHANGED; every delta was re-read against the drawing and none touches it: the mark-glyph renames (🔢 → 🧮 value, 📮 probe), outline 0.5.0's two-direction COVERAGE (the gate panel prints the four check NAMES, which did not change), and `page-lifecycle.workflow.js` gaining `PRODUCER_AGENTS` (the figure names no agent). The five ticks, the zones and every edge were re-verified against the moved files
- history: born 260819, when JL asked why the Overview had no picture ("overview 里面，不应该有一些图片或者 display 之类的吗？让我们看到它在 overall 上是一个什么样的东西？"). Redrawn later the same day after JL caught the first drawing still showing the pre-260819 linear loop; the redraw agent died mid-run at the API session limit with the tikz compiled but unchecked, and a second agent verified every fact against the live contracts, fixed PROBE's role row, rebuilt figure + preview (preview paper 160x60mm → 168x160mm), re-froze the intake at 9 inputs (adding `page_lifecycle.py`), and looked at the render
