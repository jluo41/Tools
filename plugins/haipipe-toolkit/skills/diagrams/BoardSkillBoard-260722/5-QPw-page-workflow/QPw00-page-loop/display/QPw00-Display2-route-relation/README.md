# QPw00-Display2-route-relation

- claim: the SEVEN-row legal-route table is not uniform, and reading it row by row yields the page's three central laws — CLOSE is reachable from CHECK alone, and CHECK is unreachable from CHECK, so no version is judged twice without a producer between
- kind: tex, TeX-native (a display equation; the writer is a person, the recipe IS the figure)
- caption-job: turn §9.1's sentence "only CHECK may CLOSE" into a statement a reader can check against the shipped table, and surface the second law that the prose never states
- fragility: none data-bound; every set is transcribed from `src/page_lifecycle.py`, named in `intake/manifest.yaml`. If `LEGAL_ROUTES` changes, this unit is wrong and must be rebuilt, not patched
- renderer: CC as the TeX hand; rebuild = `pdflatex preview.tex`, then `recipe/asset.tex` for `assets/figure.pdf`
- picked: 260816 CC · one candidate, clean on the first compile; only the paper height was trimmed
- accepted: ⬜ auto 260818 · display-rules R14 fails: the drawn `Φ = {DRAFT, PROBE, REVISE, CHECK}` and `R(φ) = Φ ∪ {HOLD}` appear in no intake input. `intake/inputs/page_lifecycle.py` (sha256 51448063…, the file the manifest names for PHASES/LEGAL_ROUTES) has SEVEN phases and a seven-row `LEGAL_ROUTES`: `R(PROBE)` omits DRAFT and CHECK, `R(REVISE)` omits PROBE, `R(CHECK)` omits COMPILE and adds OUTLINE and EVIDENCE. Only the two consequence lines still hold, and the README's own fragility row says a changed `LEGAL_ROUTES` means rebuild, not patch. R10 unevaluated, not applicable to this kind: a display equation has no axes. R1-R9, R11-R13 pass
- history: REBUILT 260818 against the frozen `intake/inputs/page_lifecycle.py`. The unit drew a FOUR-phase $\Phi$ and a uniform $R(\varphi)=\Phi\cup\{\textsc{Hold}\}$, true of the 260816 loop and false of the seven-phase loop the same file declares: $R(\textsc{Probe})$ omits DRAFT and CHECK, $R(\textsc{Revise})$ omits PROBE, and $R(\textsc{Check})$ omits COMPILE while adding OUTLINE and EVIDENCE. Caught by `haipipe-board-approver-agent` on display-rules R14, the no-invented-value rule, on its first run. The two consequence lines survived untouched because they were always derived from CHECK's row alone; a third, HOLD in every row, was added because the real table shows it.
- history: born 260816 beside Display1, from the observation that `R(CHECK)` omits CHECK — a law the page argues nowhere in prose
