# QPw00-Display2-route-relation

- claim: the legal-route relation is not uniform, and its shape is the 260819 two-part loop: PROBE and EVIDENCE route only sideways or BACK to OUTLINE, so DRAFT is unreachable from either and the PREPARE loop's single door out is OUTLINE's own gate; CLOSE is reachable from CHECK alone, CHECK is unreachable from CHECK, HOLD is reachable from every phase, and since the same-day 260819 fix the controller and the auditor encode this same table, keeping only a COMPILE row no contract names so stored receipts stay auditable
- kind: tex, TeX-native (a display equation; the writer is a person, the recipe IS the figure)
- caption-job: turn §9.1's sentence "only CHECK may CLOSE" into a statement a reader can check against the shipped table, and surface the three laws the prose never states: the loop's one door out, CHECK's unreachability from itself, and that contracts and code now assert the same relation
- fragility: none data-bound; every set is transcribed from the six phase contracts' §🔀 rows and `ref/phase-cards.md`'s ROUTES rows, all named in `intake/manifest.yaml` with the sha256 of the LIVE file. A phase renamed, a ROUTES row edited, or `LEGAL_ROUTES` diverging from the contracts again makes this unit wrong, and `checks/intake.py` says so the same day. The two JUDGMENT rows the morning rebuild carried are RESOLVED and no judgment remains: haipipe-page-evidence 0.10.2 deleted the stale `→ REVISE`/`→ DRAFT` half of its §🔀 (the §🔁 block this unit followed won), and haipipe-page-draft 0.8.1 removed EVIDENCE and COMPILE from its receipt route enum (the phase-cards ④ row this unit followed won)
- contradictions: one residue stands and is NOT resolved here: `src/page_lifecycle.py` LEGAL_ROUTES, `ref/page-lifecycle.workflow.js` LEGAL, and `haipipe-page-workflow/ref/page-run-contract.md`'s Legal-routes block all keep a COMPILE row and a REVISE→COMPILE edge that no phase contract's §🔀 names (phase-cards ⑤ folds COMPILE into REVISE and lists no such route). Both code files say in their own comments that the row is kept so an already-stored receipt naming COMPILE stays auditable, and phase-cards says whether the fold is permanent is `QPw5-revise`'s open ruling. The figure states this residue in its last block rather than hiding it
- renderer: CC as the TeX hand; rebuild = `cd recipe && pdflatex -interaction=nonstopmode asset.tex` for `assets/figure.pdf`, then `pdflatex -interaction=nonstopmode preview.tex` at the unit root
- picked: 260819 CC · one candidate again, the explicit set list, because a reviewer diffs this rebuild against the 260818 rows and a grid would hide which row moved. Three overfull boxes were fixed by shortening annotation text, never by shrinking the type; `preview.tex` moved to 10pt/135mm so the preview breaks lines exactly where the embedded asset does
- accepted: ⬜ nobody has looked at the refreshed render. Refreshed and re-frozen 260819 by the producer, who may not judge his own claim; the `contradictions:` row is what a person should read first
- serves: C12.P2.B2
- history: RE-FROZEN 260819 late evening at EVIDENCE, nothing redrawn. `checks/intake.py` reported 7 of 10 inputs CHANGED; every delta was re-read against the drawing and none is a route: the mark-glyph renames (🔢 → 🧮 value, 📮 probe) in `phase-cards.md`, probe 0.6.0, evidence 0.10.4 and draft 0.8.2, outline 0.5.0's two-direction COVERAGE, and the per-phase producer-agent roster in `haipipe-page-workflow` 0.15.0 §👷 + `page-lifecycle.workflow.js` `PRODUCER_AGENTS`. `LEGAL_ROUTES` (py) did not move at all and `LEGAL` (js) moved only around the roster, so the six R(φ) rows, the four laws and the last block's agreement claim were re-verified byte-true; the COMPILE residue in the `contradictions:` row also stands unchanged
- history: REFRESHED 260819, same day as the rebuild below, because the code caught up: `LEGAL_ROUTES` and `LEGAL` were brought into line with the contracts acting on this unit's own finding, so the figure's last block and the caption now record agreement instead of a lag, and both JUDGMENT rows dissolved when haipipe-page-evidence went to 0.10.2 and haipipe-page-draft to 0.8.1 (haipipe-page-workflow 0.9.2 aligned its ref too). The six R(φ) row-sets were re-derived from the live contracts and are UNCHANGED; seven of the ten intake hashes moved and the manifest was re-frozen; `checks/intake.py` reports this unit clean. The kept COMPILE row moved from the figure's disagreement note to the `contradictions:` row above
- history: REBUILT 260819. The loop became two parts that day (a converging PREPARE loop ① OUTLINE ⇄ ② PROBE ⇄ ③ EVIDENCE, then linear ④ DRAFT → ⑤ REVISE → ⑦ CHECK), which made the previous figure stale in five of its seven rows: PROBE and EVIDENCE stopped routing forward, OUTLINE gained PROBE, DRAFT and REVISE lost EVIDENCE's and COMPILE's edges, and COMPILE lost its row entirely. `checks/intake.py` is what caught it, reporting `page_lifecycle.py CHANGED`; the change in that file turned out to be unrelated (a moved-page fallback), and the real staleness was in the CONTRACTS, which the 260818 manifest did not name at all. Both remaining laws from 260818 survived, a fourth was added, and the intake was converted to the checkable `file:` + `source:` + `sha256` shape with ten inputs
- history: REBUILT 260818 against the frozen `intake/inputs/page_lifecycle.py`. The unit drew a FOUR-phase $\Phi$ and a uniform $R(\varphi)=\Phi\cup\{\textsc{Hold}\}$, true of the 260816 loop and false of the seven-phase loop the same file declares: $R(\textsc{Probe})$ omits DRAFT and CHECK, $R(\textsc{Revise})$ omits PROBE, and $R(\textsc{Check})$ omits COMPILE while adding OUTLINE and EVIDENCE. Caught by `haipipe-board-approver-agent` on display-rules R14, the no-invented-value rule, on its first run. The two consequence lines survived untouched because they were always derived from CHECK's row alone; a third, HOLD in every row, was added because the real table shows it.
- history: born 260816 beside Display1, from the observation that `R(CHECK)` omits CHECK — a law the page argues nowhere in prose
- history: redrawn 260819 night, one row: R(OUTLINE) gained EVIDENCE — after the 🧑 LOOK, ② and ③ dispatch in parallel (page-run-contract + LEGAL tables updated the same hour); every other row re-verified unchanged
