# QPw00-Display6-agents-and-skills

- claim: every phase of the loop is executed by its OWN agent, six files under page-workflows/agents/, five write-scoped producers plus (7)'s read-only judge, each walking one fixed six-layer stack whose shared law lives once in ref/producer-contract.md, so no producer reads another agent's file; the one standing exception is (7), whose stack still rests on another agent's file, its base haipipe-board-reviewer-agent, because the judge ref has not been carved out yet
- kind: tex, TeX-native (a hand-authored `tikz` block; the writer is a person, the recipe IS the figure)
- caption-job: let a reader answer "who runs this phase, and what does it load" for any phase without opening an agent file, and see in the same glance that the PREPARE trio (1)(2)(3) iterates as one converging bracket that stops at a person's `approved:` tick before (4)(5)(7) run
- fragility: fully data-bound to the ten frozen files. A renamed agent, a moved producer-contract.md, a changed load order, a new plugin in a load chain, or the judge ref finally being carved out makes this figure wrong, and `intake/manifest.yaml` (ten sha256) is what makes that computable. The printed version line (haipipe-page-workflow 0.15.0) moves with every SKILL.md release even when the roster does not; it did exactly that hours after the first freeze (0.14.2 → 0.15.0) and the line was redrawn
- how it differs from the five units already here: Display1 is RUN's control flow, Display2 is the seven-row legal-route relation, Display3 prices the loop as a person's work, Display4 orients a reader across all seven phases with the full route arrows and stop conditions, Display5 opens ONE phase (EVIDENCE's two stages and three lanes). This one names the EXECUTORS: which agent runs each phase and the exact stack it loads; its only share of the iteration is the PREPARE bracket and the one gate marker, so the routes stay Display4's and Display1's
- intake: intake/manifest.yaml freezes ten files, all under skills/: the six phase agent files (board/page-workflows/agents/), producer-contract.md and SKILL.md 0.15.0 (board/page-workflows/haipipe-page-workflow/), and the two base agents haipipe-page-creator-agent and haipipe-board-reviewer-agent (board/agents/)
- renderer: CC as the TeX hand, `tikz`; rebuild = `cd recipe && pdflatex asset.tex && cp asset.pdf ../assets/figure.pdf`, then `pdflatex preview.tex` at the unit root
- picked: 260819 CC · one candidate, a six-column roster in the order the loop runs the phases, crossed with the six load layers as rows, because spanning cells are what make the sharing visible: the shared law spans the five producers and STOPS at (7), whose row-2 cell is the different one. Two layout passes were wrong before it: pass 1 rendered black-on-black through sips (a rendering-tool artifact, not a figure defect; pdftoppm is the faithful renderer), pass 2 clipped every agent name at the box edge and drowned the rotated gate label behind row 1, both visible only in the picture
- contradictions: (a) C1.P2.B5 and producer-contract.md's carve-out rationale both say "no agent reads another agent's file", but haipipe-page-check-agent.md instructs reading haipipe-board-reviewer-agent.md FIRST and says itself the judge side has no carved-out ref yet; the universal clause is false for (7) today, the figure draws the exception. SOFTENED upstream 260819 late evening: producer-contract.md itself now records the exception ("⑦'s judge still reads its reviewer base until the judge ref is carved out"), so contract and figure agree; the carve-out itself is still owed. (b) haipipe-page-check-agent.md locates its base at plugins/haipipe-toolkit/agents/ while the roster's own files sit at skills/board/agents/; both paths exist and the two copies are byte-identical today, but nothing on disk keeps them identical. RESOLVED upstream 260819 late evening: haipipe-page-check-agent.md now names skills/board/agents/ as the canonical home and plugins/haipipe-toolkit/agents/ as symlinks only
- accepted: ⬜  a person has not looked at it yet; the tick is (7) CHECK's, never this producer's
- serves: C1.P2.B5
- history: RE-FROZEN and MINIMALLY REDRAWN 260819 late evening, at EVIDENCE. `checks/intake.py` reported 3 of 10 inputs CHANGED: SKILL.md 0.14.2 → 0.15.0 (the §👷 roster section landed; the roster it prints is what this figure already drew), producer-contract.md now recording the (7) judge-base exception in its own header, and haipipe-page-check-agent.md naming skills/board/agents/ as its base's canonical home. One drawn fact had gone false and it is the one the fragility row predicted: the printed version line. It now reads 0.15.0; every agent name, layer and span was re-verified unchanged. Asset and preview rebuilt, rendered through pdftoppm and looked at: nothing clipped, both contradictions updated above
- history: born 260819 in the EVIDENCE phase of the PREPARE round on QPw00-page-loop, after JL asked for the iteration itself to be drawn ("I think we should create the figure as well. Like the iteration between outline probe and evidence"); mid-build the value mark ruling landed (🔢 → 🧮, SKILL.md 0.14.1 → 0.14.2), the intake was re-frozen the same evening, the drawing prints no value mark, and only its printed version line followed
