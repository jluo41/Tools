# QPw00-Display3-who-does-what

- claim: the page workflow asks something of a person on SEVEN of its eleven rows and nothing at all on the other four; five of those seven carry a tick a machine may never write, and the remaining two (OPEN and CHECK) ask for a judgment that leaves no tick behind, so a reader who knows only this table knows what will be asked of them and when
- kind: tex, TeX-native (a `booktabs` table; the writer is a person, the recipe IS the figure)
- caption-job: let a reader see the whole loop as WORK rather than as authority, so the question "what will this cost me" is answered before the contracts are opened
- serves: QPw00-page-loop.md §12
- fragility: one column is data-bound and it is TIME, which is an ESTIMATE and says so on its own face. Every other cell is transcribed from a file frozen in `intake/inputs/`, and `intake/manifest.yaml` names the eight files and their sha256
- renderer: CC as the TeX hand; rebuild = `pdflatex preview.tex`, then `recipe/asset.tex` for `assets/figure.pdf`
- picked: 260818 CC · two candidates. The first put the footnote outside a minipage, which floated it to the RIGHT of the table in the cropped asset and left the figure 322mm wide; the second wraps the whole unit in `minipage{176mm}` so both hosts break the same way
- accepted: ⬜ auto 260818 · display-rules.md R1-R7, R9, R10, R13 pass · R8 fails: the page's built `.tex` embeds `assets/figure.pdf` at `width=.85\linewidth` (0.733x), so 8pt body text prints at 5.9pt and the 7pt estimate footnote at 5.1pt, both under 6pt · R11 fails: the caption says five of eleven rows need a person and six run machine-only, while the drawing shows seven YOU rows (0, 1, 4c, 4v, 6, 7, 8) and four `nothing` rows (2, 3, 4d, 5) · R12 fails: the claim spreads the five ticks over four rows; they sit on five (1, 4c, 4v, 7, 8), and this unit's own `intake/inputs/haipipe-page-check.SKILL.md:153` says that count "said FOUR until 260818 and it was wrong" · R14 fails: the footnote states every other cell is transcribed from `intake/inputs/`, but row 0 OPEN matches none of the eight frozen files, which run ① OUTLINE to ⑦ CHECK with no ⓪
- history: CORRECTED 260818 by `haipipe-board-approver-agent` on its first run, which failed R11, R12 and R14. The caption and this claim both said "five of the eleven rows need a person" and "the other six run machine-only"; the drawing shows SEVEN rows with a job and FOUR reading `nothing`. The claim also said the five ticks sit on FOUR rows, which reproduces the exact miscount this unit's own frozen `haipipe-page-check.SKILL.md:153` corrects ("This said FOUR until 260818 and it was wrong"). R14 caught row 0 OPEN tracing to none of the eight frozen inputs; `haipipe-page.SKILL.md` is now frozen as the ninth and the row names its `CREATE` verb. R8 caught the embed: at `width=.85\linewidth` the 176mm original printed at 0.733x, so 8pt body reached the reader at 5.9pt; the natural width is now 124mm and the same embed prints it at 0.9x.
- history: born 260818, when JL handed over a labeling protocol written as `# · PHASE · WHAT HAPPENS · YOUR JOB · TIME · HOW OFTEN` and asked for the page workflow in the same shape. The band structure is his: once / the loop / what ends it
