# QPw00-Display3-who-does-what

- claim: the page workflow asks a person for something on EIGHT of its twelve rows, and every one of those eight is either the OPEN that starts the page, a step of the PREPARE loop, or CHECK; the three rows that sit between the plan and the judgment (4 DRAFT, 5 REVISE, 6 COMPILE) ask for nothing at all, so the loop's whole human cost is paid BEFORE DRAFT and AFTER REVISE and never inside them
- kind: tex, TeX-native (a `booktabs` table; the writer is a person, the recipe IS the figure)
- caption-job: let a reader see the whole loop as WORK rather than as authority, so the question "what will this cost me, and when" is answered before the contracts are opened
- serves: QPw00-page-loop.md §14 · outline v3 bullet C14.P1.B1
- counts on its own face: 12 rows · 8 ask a person · 4 read `nothing` (2, 4, 5, 6) · 5 person-reserved ticks on 5 rows (1 `approved:`, 3c `verified`, 3v `read:`, 7a `accepted:`, 7b the RULING) · 3 rows ask a judgment that leaves no tick behind (0 OPEN, 3d DISPLAY, 7 CHECK)
- row order: 0 OPEN · 1 OUTLINE · 2 PROBE · 3c CITATION · 3v VALUE · 3d DISPLAY · 4 DRAFT · 5 REVISE · 6 COMPILE · 7 CHECK · 7a ACCEPT · 7b CLOSE. Rows 1, 2 and 3 repeat AS ONE UNIT; the three ③ lanes run at the same time; rows 4, 5 and 6 run end to end and stop for nobody
- intake: `intake/manifest.yaml`, 15 inputs, listing frozen 260819, re-frozen the same afternoon after sibling agents edited three of them, and re-frozen again late that evening at EVIDENCE (the fourth stored run became input 15 and the footer was redrawn; see history) (`page-run-contract.md`, `haipipe-page-evidence/SKILL.md`, `haipipe-page-draft/SKILL.md`); the manifest header records why none of those edits changes a cell. Every row names the LIVE `source:` as well as the local copy, so `checks/intake.py` can re-hash the original and report CHANGED. No probe card serves this unit and none needs to: it is a CONCEPT unit, whose intake freezes a listing of source files rather than a `proof/` (`haipipe-page-evidence` §🖼, "concept kind ... nothing to wait for")
- fragility: one column is data-bound and it is TIME, which is an ESTIMATE and says so on its own face. Every other cell is transcribed from a file frozen in `intake/inputs/`. What makes this picture WRONG: a renamed phase, a moved tick, a re-ordered loop, or a phase gaining its first receipt. Disk WOULD say so for all four, because `checks/intake.py` re-hashes every `source:` row; that is exactly how the 260819 order change was caught, six inputs CHANGED at once
- renderer: CC as the TeX hand; rebuild = `pdflatex preview.tex` at the unit root for `preview.pdf`, then `cd recipe && pdflatex asset.tex` and move `asset.pdf` to `assets/figure.pdf`
- looked at: yes, 260819. `assets/figure.pdf` rendered at 150 dpi and read row by row: nothing clipped, no cell overlapping its neighbour, and the TIME and HOW OFTEN headers no longer touch (they did at the first compile of this rebuild, which is why TIME is now right-aligned with a wider gap). Looked again 260819 late evening after the footer redraw: `assets/figure.pdf` rendered through pdftoppm and read row by row, nothing clipped, no cell overlapping, and the corrected four-run footer prints clean with zero overfull boxes
- measured widths, because the previous README stated one that was never true: the cropped asset is 400.2pt = 141.1mm wide; the previous asset in git was 393.6pt = 138.8mm, so this rebuild is 1.7% wider. `124mm` is the `minipage` DECLARATION inside `recipe/who-does-what.tex` and has never been the asset's width. The page embeds it at `width=.85\linewidth` in `latex/QPw00-page-loop.tex:522`
- picked: 260819 CC · three candidates on the TIME/HOW OFTEN boundary, all rejected by looking at the render. `l@{~~}` (the pre-260819 spec) printed the headers as one word, `TIME HOW OFTEN`; `l@{~}` was worse; `r@{~~}` with the two `p` columns trimmed to 31mm and 28mm separates them and holds the asset near its old width. The row for 3d DISPLAY was also picked twice: `0` was rejected because the lane took over three of REVISE's steps on 260819 and no longer costs a person nothing, and no substitute number was invented
- accepted: ⬜ NOT judged, and not judgeable here. A producer asked "is my table right" says yes; the row order, the eight-of-twelve split and the five ticks must be read against `intake/inputs/phase-cards.md` by `haipipe-board-reviewer-agent`, and the tick itself belongs to a person at ⑦ CHECK, which is row 7a of this very table
- contradictions: ALL FIVE below were RESOLVED UPSTREAM on 260819, the same afternoon they were reported: the main session edited the contracts toward this drawing (phase-cards.md now says two ③ lane ticks with ③d tickless and numbers the lanes ③c/③v; `haipipe-page-probe` 0.5.1 calls itself phase ②; `haipipe-page-evidence` 0.10.2's kinds table says RENDER · PICK · BUILD are here; `page-run-contract.md` says five ticks and its legal-routes block matches the PREPARE loop). The intake was re-frozen against the fixed files and ZERO cells changed. The list is kept as found, because finding them is what this unit's transcription rule is for: ① `phase-cards.md` §🧑 says "the three ③ evidence ticks", but its own ③d card says `accepted: ✅` "is CHECK's, not this phase's" and its gathered-ticks table places `accepted:` at ⑦. The table follows the ③d card and the gathered table, so 3d shows a job with no tick. ② `phase-cards.md`'s ⑦ ROUTES row and its gathered-ticks table still use the PRE-260819 numbers (④ EVIDENCE, ③ PROBE, ②/④c/④v); the table therefore prints route names, never those numbers. ③ `haipipe-page-probe`'s frontmatter and footer still call themselves "phase ③" and point at `phase-cards.md §③`, and `haipipe-page-evidence`'s footer points at `§④`; both are one place behind the headings in the file they cite. ④ `haipipe-page-evidence`'s three-kinds table still says a display's renderer "draws it in REVISE", contradicting its own §🖼 twelve sections later. ⑤ `page-run-contract.md`'s legal-routes block still allows `from OUTLINE → DRAFT` directly and says "The four ticks"
- history: RE-FROZEN and MINIMALLY REDRAWN 260819 late evening, at EVIDENCE. `checks/intake.py` reported 9 of 14 inputs CHANGED; eight of the nine deltas (mark-glyph renames 🔢 → 🧮 value and 📮 probe, outline 0.5.0's two-direction COVERAGE, cosmetic agent and tab wording in `haipipe-page.SKILL.md` and the two plugins) change no cell and were re-pinned only. What DID go false was the footer, exactly the failure the fragility row names as "a phase gaining its first receipt": the board's fourth stored run (`260819-1813-QPw00.json`) holds PROBE's first two receipts, so "three runs" and "PROBE ... no receipt anywhere" were both wrong. The footer now counts four runs, lists PROBE as covered, and dates the EVIDENCE/COMPILE gap to the freeze, because this same pass appends the board's first EVIDENCE receipt right after it: that append will flag input 15 CHANGED on the next `checks/intake.py` run, expected and recorded here rather than hidden. No table cell changed; asset and preview rebuilt
- history: RE-BUILT 260819 because the loop's phase ORDER changed that day and this table drew the old one. `checks/intake.py` reported 6 of the 9 frozen inputs CHANGED. Four rows moved and three changed meaning: PROBE and the three EVIDENCE lanes now come BEFORE DRAFT rather than after it; DRAFT's cell now says it enters on landed evidence and writes the NUMBER, so a hole is the exception; REVISE's cell no longer claims it draws, because RENDER, PICK and BUILD moved to EVIDENCE and only ACCEPT stayed at CHECK; 3d DISPLAY gained a job and lost its `0`; COMPILE gained its own row saying it is folded into 5; and OUTLINE's cell now names the FOUR machine checks that run before the person is asked, which is new work for that row because the person now judges DIRECTION and not arithmetic. The manifest was converted from the `path:` + `sha256` shape, which proves only that a copy has not rotted, to `file:` + `source:` + `sha256`, and grew from 9 inputs to 14: `page-run-contract.md`, the two page plugins, and the board's two QPw00 run receipts, because "which phases have ever produced a receipt" became a transcribed cell
- history: CORRECTED 260818 by `haipipe-board-approver-agent` on its first run, which failed R11, R12 and R14. The caption and claim both said "five of the eleven rows need a person" and "the other six run machine-only" while the drawing showed SEVEN rows with a job and FOUR reading `nothing`; the claim also spread the five ticks over FOUR rows, reproducing the exact miscount `haipipe-page-check.SKILL.md:153` corrects. R14 caught row 0 OPEN tracing to none of the frozen inputs, which is why `haipipe-page.SKILL.md` is an input
- history: born 260818, when JL handed over a labeling protocol written as `# · PHASE · WHAT HAPPENS · YOUR JOB · TIME · HOW OFTEN` and asked for the page workflow in the same shape. The band structure is his: once / the loop / what ends it
