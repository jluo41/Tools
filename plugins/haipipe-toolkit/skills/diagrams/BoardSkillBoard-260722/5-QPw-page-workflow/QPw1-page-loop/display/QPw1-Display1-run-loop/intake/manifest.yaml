# A concept unit states no number, so it declares no values intake.
# What it DOES bind is the text it transcribes: every phase name, route, and
# stop condition below is copied from a shipped file, never from memory.
kind: concept
values: none
sources:
  - path: plugins/haipipe-toolkit/skills/board/haipipe-board/src/page_lifecycle.py
    takes: PHASES, LEGAL_ROUTES, TERMINAL_ROUTES
  - path: plugins/haipipe-toolkit/skills/board/page-workflows/haipipe-page-workflow/SKILL.md
    takes: the six RUN steps, the producer/judge separation, the stop list
  - path: QPw1-page-loop.md
    takes: "sections 9.1 to 9.4"
