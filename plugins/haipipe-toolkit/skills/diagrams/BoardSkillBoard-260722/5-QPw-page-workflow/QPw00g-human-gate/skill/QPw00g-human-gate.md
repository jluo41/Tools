# QPw00g-human-gate · skills

- haipipe-board · note: the engine whose computed findings must all be zero before this gate opens, which is the accept-bias rule's mechanism
- haipipe-page-check · note: reserves two of the four ticks, `accepted: ✅` and the Page Type's ruling, and forbids a machine claiming either
- haipipe-page-outline · note: reserves `approved:`, the only exit its phase has, and states why no machine may write it
- haipipe-plugin-bibex · note: the bibex law that puts `verified` in a person's hands, since a machine may subset or transcribe bibtex and never compose it
