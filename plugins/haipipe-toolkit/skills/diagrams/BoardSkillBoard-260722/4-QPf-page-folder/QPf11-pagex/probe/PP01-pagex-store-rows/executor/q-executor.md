# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

Read the markdown list at
/Users/jluo41/Desktop/Physician-SPACE/Tools/plugins/haipipe-toolkit/skills/diagrams/BoardSkillBoard-260722/4-QPf-page-folder/QPf11-pagex/pagex/QPf11-pagex.md

Report, in the file's own top-to-bottom order and without sorting:
① how many list rows it holds, counting rows that end ` · removed` separately
   from live rows
② each row's target path, exactly as written
③ each row's note, quoted verbatim, and the number inside any note of the form
   `scan-seeded — this page names <id> N×`

Do not resolve, tidy or shorten a path. If a row carries no note, write none.
If the file is not there, say so; do not report a similarly named file instead.
Aggregate output only; no row-level record, beneficiary id or physician id.
Deliverable: an ordered list of path · note · seed number, plus the row total.
Accepted: an exact count with the ordered list | the file does not exist.
