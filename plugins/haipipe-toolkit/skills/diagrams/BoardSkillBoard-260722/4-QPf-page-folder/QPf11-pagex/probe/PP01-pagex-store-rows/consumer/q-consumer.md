# PP01-pagex-store-rows · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: division 7 is the page's own specimen, captioned "Read off the disk, not
the design". Two of its sentences quote numbers: C7.P1.B1 says the list holds
four lines, and C7.P1.B3 says one row was suggested first on 16 mentions and now
sits second because a person moved it. A4.1 is the Aim that pays, since its Done
when is that one click fills the list IN THAT ORDER with nobody reading the page
by hand, and A1.2's Done when is that a rebuild leaves a hand-set order alone.
Both are read off this file or off nothing. A specimen division that quotes a
count from memory is the exact failure its own caption was written against, and
the numbers have already rotted once here: the Log records a depth claim in this
same division that was false and backwards.

- `### 7 · The four borrows this page actually has` · needs the row total and
  the top-to-bottom order (C7.P1.B1), and each row's scan-seeded count so the
  gap between the machine's suggestion and the person's rank is a number rather
  than a story (C7.P1.B3).
