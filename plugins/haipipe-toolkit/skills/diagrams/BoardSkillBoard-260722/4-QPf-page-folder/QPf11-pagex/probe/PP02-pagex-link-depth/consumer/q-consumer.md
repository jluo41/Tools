# PP02-pagex-link-depth · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: C7.P1.B5 states a RULE about how far a shortcut climbs, three steps up
when the source shares this page's group folder and four when it does not, and
C7.P1.B1's second half counts the shortcuts on disk. This exact sentence has
already been wrong once: the Log's 260816 truth pass records that the division
had claimed a single depth of four for every link, "false and backwards", and it
was corrected only because someone opened the folder. A rule restated from the
last correction is not a reading. A2.1 is the Aim that pays: its Done when is
that a rebuild creates one shortcut per live line and leaves every other file
alone, which is a statement about what is in this folder and nothing else.

- `### 7 · The four borrows this page actually has` · needs the shortcut total
  (C7.P1.B1) and the raw `../` step count of every link, so the depth rule is
  read off the targets instead of restated (C7.P1.B5).
