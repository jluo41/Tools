# q-executor
🧱 STAKE FORBIDDEN · this is the ONLY file that is dispatched.

Walk the tree at
/Users/jluo41/Desktop/Physician-SPACE/Tools/plugins/haipipe-toolkit/skills/diagrams/BoardSkillBoard-260722

Find every file whose name ends `-deck.html`, at any depth, including inside
folders whose names begin with an underscore.

For each one report:
① its path relative to that root
② its size in bytes
③ its modification time, to the minute

Then give the total, and a second total counting only the files whose
modification date falls on 2026-08-15.
List them oldest first.
If none is found, say none; do not report the nearest similar file name.
Aggregate output only; no row-level record, beneficiary id or physician id.
Deliverable: the path list with sizes and times, plus both totals. Accepted: an
exact count with the list | none.
