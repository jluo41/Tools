# Concept-driven intake: narrative sources only, no values (the figure states no number).
unit: QPf5-Display1-pipeline-tikz
kind: diagram-tikz
values_sources: []
narrative_sources:
  - role: concept
    ref: ../QPf5-display.md
    note: Content §4, the five-step pipeline and the three-hands rule this figure draws
facts_the_image_may_state:
  - the pipeline is intake, render, pick, build, accept, in that order
  - a person owns steps 1, 3, 5; machinery owns steps 2 and 4
  - a changed intake flows forward and the acceptance falls back to open

# FROZEN 260818. This manifest named its sources by REF and froze no bytes,
# so `cli/check.py` reported `display-intake-unfrozen` and nothing could be
# rechecked: a ref points at a file that keeps changing. These are the bytes
# the figure was drawn from.
# CONVERTED 260819 to the checkable shape: `source:` names the LIVE file
# relative to the skills/ root and `sha256` hashes THAT file, so
# checks/intake.py can compute staleness. The live file matched the 260818
# copy byte-for-byte at conversion; nothing was redrawn.
frozen: 260819
inputs:
  - file: intake/inputs/QPf5-display.md
    source: diagrams/BoardSkillBoard-260722/4-QPf-page-folder/QPf5-display/QPf5-display.md
    sha256: e251fdcac0d6a9ac94d8e2db8b4b066d6c954d4581289da80c89b7b76b615620
    bytes: 28558
    takes: "Content section 4: the five-step pipeline and the three-hands rule this figure draws"

# RE-FROZEN 260819: the source pages' Log rows were cleaned of profanity
# (JL: "please clean all the bad words from the log"); the content each
# figure takes is untouched, so nothing was redrawn.
