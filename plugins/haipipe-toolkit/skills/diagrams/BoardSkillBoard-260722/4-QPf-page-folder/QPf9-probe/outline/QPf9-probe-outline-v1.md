# QPf9-probe · outline v1
outline-version: v1
supersedes: —
date: 260817
approved: ⬜            🚧 a person ticks this. No machine may.

Generated 260817 from this page's own divisions, face-figure captions and
sentences, so no bullet claims anything the page does not already say.
UNAPPROVED, so it is a working document: rewrite it, delete what is wrong.

## C1 · One question, one folder, on the page that needs it

### C1.P1 · the field list this page's own `PP01` really carries
- B1 · The card keeps three things from the old `1-probes/` pool and changes two.
- B2 · It keeps the `PP<NN>-<slug>` name, the three bank-facing part names, and the rule that an answer is copied back from the file the bank wrote.
- B3 · The first thing it changes is the address, which is now the page's own `probe/` folder, so `PP` numbers start again on each page.
- B4 · A pool folder was one TOPIC, holding a `QXn_<slug>.md` file per question, and each of those files carried one `## QX<n>` entry split into four `###` parts.
- B5 · A card folder is one QUESTION, holding one `card.md`, whose head lines carry the question itself.
- B6 · That is why the card has three `##` parts where the old entry had four: it drops the review-only `q-consumer` map, because the head's own `question:` and `stake:` lines already say what the asking side wants.
- B7 · A card is small on purpose.
- B8 · ⚠️ 3 more sentences in this division are not planned here yet

## C2 · The answer is written once, and everyone points at it

### C2.P1 · the link lives in the card, and everyone else points at the card
- B1 · Before this plugin, a display figure's manifest held the answer's path itself.
- B2 · A second user of the same number would then have kept a copy of that path.
- B3 · Now the manifest names the card, and the card names the file that holds the answer.
- B4 · Nobody keeps a copy of the path, so a re-answered question changes one file and everyone follows.
- B5 · This page follows its own rule, with three real cards in its own `probe/` folder, all of them answered.
- B6 · They are the migration count (PP01), the landing address limit (PP02), and the family's own skill count (PP03).
- B7 · All three really went through the `haipipe-probe` loop.
- B8 · ⚠️ 4 more sentences in this division are not planned here yet

## C3 · Three states, and one owner for each move

### C3.P1 · the page asks, the runner claims, the collector writes the link
- B1 · A `working` card means someone is already on it.
- B2 · A second session reads that state line and waits, instead of asking the bank again.
- B3 · Why the page wants the number never crosses the wall.
- B4 · The card keeps that reason, and the bank sees only the question in plain, general words.
- B5 · A card may also close with a no.
- B6 · A bank answer of "not supported" is still an answer, and the sentence citing it must carry that no rather than hide it.

## C4 · A tab that shows every card, and changes nothing

### C4.P1 · the same layout the display tab uses, carrying cards instead of figures
- B1 · The layout is the display tab's, taken whole.
- B2 · It is a sideways strip of blocks, one block per card, with a row of chips naming every id.
- B3 · Each card has its own mark in the page, named after the card folder, so a citation lands on the card it names.
- B4 · What fills a block is probe's own: the state badge, the question, the link, and the card folder's file tree.
- B5 · The wiring is the same three-layer one the Slides tab uses.
- B6 · A tab plugin asks a `live/` endpoint, the endpoint scans the page's `probe/` folder, and each block is drawn from the card's own state line.
- B7 · It writes nothing, because §3 already gives every state change an owner.

## C5 · Evidence this page already carries

### C5.P1 · Cite each where it belongs, or drop it
- B1 · PP01 is on disk and no bullet cites it yet   🔢 PP01
- B2 · PP02 is on disk and no bullet cites it yet   🔢 PP02
- B3 · PP03 is on disk and no bullet cites it yet   🔢 PP03

