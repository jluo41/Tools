# PP02-landing-address · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: A1.1 needs the orchestrators to land in a page's `probe/` and nowhere else; whether that is a parameter change or a rewrite decides the plan.

- `### 1 · One question, one folder, on the page that needs it` · C1.P1.B3 says
  the first thing the card changes is the ADDRESS, from a shared pool to the
  page's own `probe/` folder. That is a claim about where a probe record is
  written, and it holds only if the old address is not nailed into the agents
  that write one. The size of what is nailed down is what turns this sentence
  from a design wish into a shipped change.
