# PP01-pool-census · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: A1.2 retires `1-probes/`; the migration cannot be scoped, sequenced, or declared done without this census.

- `### 1 · One question, one folder, on the page that needs it` · C1.P1.B4 says
  what a pool folder WAS: one topic, holding one `QXn_<slug>.md` file per
  question, each file carrying one entry split into four parts. That sentence
  is the load-bearing half of the before-and-after this division is built on,
  and until real pool folders have been opened and counted it is a memory of a
  shape rather than a reading of one.
