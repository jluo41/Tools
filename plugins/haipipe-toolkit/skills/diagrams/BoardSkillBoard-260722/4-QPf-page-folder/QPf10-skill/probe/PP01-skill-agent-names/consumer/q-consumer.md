# PP01-skill-agent-names · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: A1.1 promises that a page keeps its skills in its own `skill/<stem>.md`
and that a refresh only appends new names. The whole promise rests on the scan
matching each word it lifts against something real, which is what C1.P1.B2
claims and what C1.P1.B3 turns into "so a name is never invented". Neither
sentence can be checked while the set being matched against has no size and no
membership on disk. If the answer never comes, the page keeps a safety claim
nobody can test, and A1.1 keeps a Done when that reads as met because nothing
contradicts it.

- `### 1 · One file you own, and one view the machine rebuilds` · needs the
  count and the full name list of what the scan may match, skills and agent
  files together, so §1.1's two sentences rest on a set rather than a phrase.
