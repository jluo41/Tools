# PP01-bibex-row · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: C1.P1.B5 uses this page's own `bibex/` as the WORKED EXAMPLE of the rule
above it, that an empty rebuilt folder is never flagged. The rule and its
example stand or fall together: if the folder is no longer empty, the sentence
is teaching the rule with a case that does not show it. The Aim that pays is the
first one, that the folder shows its own contents before any other tab does, and
the Log already records this page dismissing a reviewer's `bibex/` row on the
strength of a figure redrawn from memory, then having to take the dismissal back
when someone looked. That is the same mistake this bullet is one step away from
repeating.

- `### 1 · You see what your folder holds, and what has gone out of date` ·
  needs the file count of that folder and the verdict the staleness test gives
  for it today, so C1.P1.B5's example is read rather than remembered.
