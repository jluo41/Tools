# One tab presents the evidence
state: 🟡 PARTIAL · tab shipped, browser-verified · open: Tasks/Pagex segments, probe retirement
owner: JL
method: merge the presentation of the four evidence lanes into one 🧾 surface while every lane keeps its storage, writer, wall and human gate

## Opening
Where does a reader stand to ask what this page still owes?
The evidence lived behind four strip tabs, so the question had no single surface.
JL ruled 260831: one evidence plugin presents bibex, display and the rest; the subfolders stay.
One 🧾 Evidence tab now shows the four lanes as segments.
Storage, writers and the three human ticks did not move.

**Covered elsewhere**: the lane contracts are `QPf8-bibex`, `QPf9-probe`, `QPf5-display` and the value surface ruling on `QPf9`; the outline tab's per-bullet join is `QPf12`; the plugin roster is `../../haipipe-plugin/ref/roster.md`.

## Diagram
**One surface, four lanes**: five segments, and the per-bullet join is the default.
```text
  🧾 Evidence  (live GET /_board/evidence · live/evidence.py · writes nothing)
  ├── ⧉ By bullet   outline/<stem>-evidence.md rendered · the DEFAULT
  ├── 📚 Citations  bibex/ saved workbench      gate: verified: (person)
  ├── 🚪 Cards      probe/ saved view           gate: read: (person)
  ├── 🧮 Values     /_board/value live route    rides read:
  └── 🖼 Displays   display/ saved view         gate: accepted: (person)
        a missing saved view is BUILT ON CLICK through that lane's own
        POST route, the same pen the old separate tab pressed
```

## Content
### 1 · What merged, and what may never merge
**Merged versus kept**: the surface and the declaration merged; the storage did not.
```text
  merged    the tab (one 🧾 row registers; 📚 🚪 🧮 🖼 rows folded)
            the declaration (the type registry's one evidence: field)
            the coordinator (phase ③ EVIDENCE was already one phase)
  kept      bibex/ probe/ display/ folders · their writers and pens
            the stake wall (consumer/ never crosses)
            three DISTINCT human gates: verified: · read: · accepted:
  ⚖️ law    a surface offering one "approve all" control would collapse
            three gates into one tick; this tab shows each gate beside
            its own thing and offers none
```
📌 Precedent: Probe is already a logical family over two rows, value a surface with no folder; this page is the same shape one level up.

### 2 · The pieces on disk
**The tab's anatomy**: one live view, one registration, three folds.
```text
  engine    live/evidence.py       GET /_board/evidence + POST twin
            cli/serve.py           routes + EvidenceTabMixin
  js        84-plugin-evidence.js  registers the ONE 🧾 row
            82-plugin-exports.js   bibex row folded out
            08-plugin-value.js     deleted; /_board/value stays as a segment
  contract  haipipe-plugin-evidence 0.1.0 · roster rows say
            "segment inside the 🧾 Evidence tab" · haipipe-board 0.152.0
```
The proof standard is a driven browser, not a returning route: the plugin menu opens the tab, every segment renders, and the Values segment loads the live view with its honest empty counts.

### 3 · The lane's next growth
**Two segments in, one retirement behind them**: the direction on record.
```text
  🗂 Tasks   segment six    a linked task folder IS an evidence backend;
                            the page's collection job ranks first
  🔗 Pagex   segment seven  the accepted-Page lane joins the same surface
  🚪 after   probe may RETIRE into the QA crossing + the collection job's
             values.yaml · HOLD until one real page binds end to end
```
The strip shrinks by two more rows when the segments land; the retirement needs its own design thread before any card moves.

## Aims
### A1 · 🧾 What merged, and what may never merge
- ✅ A1.1 · One Evidence tab replaces four strip rows, segments verified in a browser.
  **Done when:** the registry shows one `evidence` row, no bibex/value/display/probe rows, and a click path reaches every segment.
  **Now:** met 260831; CDP-driven on SM05-results, screenshot kept in the session scratchpad.
### A3 · 🗂 The lane's next growth
- ⬜ A3.1 · Tasks and Pagex land as segments six and seven.
  **Done when:** both render inside 🧾 and their strip rows fold.
  **Now:** ruled by JL 260831 ("for the task, they should also be one of the evidence"); not built.
- ⬜ A3.2 · Probe retirement gets its design.
  **Done when:** a D-thread names where the card's stake wall, states and proof live afterwards.
  **Now:** held until the first real collection job binds a page end to end.

## Log
- 🚢 260831 · [HAIPIPE-PAGE-SKILL] page born the day the tab shipped (JL: "not the evidence folder, the evidence PLUGIN"); design, browser proof and the open segment work recorded in one pass.
