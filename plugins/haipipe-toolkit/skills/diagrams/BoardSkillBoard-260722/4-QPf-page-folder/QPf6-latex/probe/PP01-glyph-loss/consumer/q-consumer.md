# PP01-glyph-loss · q-consumer
🧱 STAKE ALLOWED · this file never crosses the wall.
One block per consumer question that reduces to this one.

stake: A2.1 is the only Aim on this page whose Done when names a measurement:
one page full of ascii figures is exported, and the lost glyphs and lost lanes
are listed in States. That list is then what decides whether the master needs a
unicode font package or whether the losses are accepted, so the decision itself
waits on this number. C2.P1.B7 currently asserts the loss and the sentence after
it says A2 is where the gap gets measured instead of guessed, which is the page
promising a number it does not have. Without it, A2.1 cannot close, the font
question cannot be argued, and the head state keeps 🟡 PARTIAL on this account.

- `### 2 · What survives the trip to LaTeX, and what is lost` · needs the list
  of dropped characters and their count, taken from one real export, so
  C2.P1.B7 states a measured loss rather than a predicted one.
