# QPf5-Display2-small-paper-tikz

- claim: a page is a small paper: its probe plugin asks a question once, the bank answers behind the wall, and both a display intake and a Content sentence cite the same card by id
- kind: diagram, TeX-native (TikZ; the QPf5 §3 method whose recipe IS the figure)
- caption-job: show the ask-once-cite-twice mechanism so a cold reader sees why the binding has one home
- fragility: none data-bound; the figure states no number, so no values intake is declared
- renderer: CC as the TikZ hand, this unit being the plugin's own demo; rebuild = pdflatex preview.tex
- picked: 260815 CC · one candidate, v2 after one refinement (the v1 bank box clipped at the page edge)
- accepted: ⬜ · awaiting JL's read of preview.pdf, the row no machine may tick
