# Rebuild display05 table body

`_fixture/displays/display05-descriptives/assets/table-body.tex` is generated by a task, not authored here.

```
python3 examples/Project-Personality-OpioidRx/tasks/Z01_Display_PhyTraitOpioid/06_table1_descriptives/06_table1_descriptives.py
cp examples/Project-Personality-OpioidRx/tasks/Z01_Display_PhyTraitOpioid/06_table1_descriptives/results/table1_multicohort/table_body.tex \
   examples/Project-Personality-OpioidRx/papers/Paper-Personality2Opioid-MISQ2026/0-displays/display05-descriptives/assets/table-body.tex
```

## Provenance

① the producing run
`tasks/Z01_Display_PhyTraitOpioid/06_table1_descriptives/`, driven by
`configs/table1_multicohort.yaml` through `runs/table1_multicohort.sh`, gated by that folder's
`CODE_REVIEW.md` and `RUN_AUDIT.md`.

Outputs, all present in `results/table1_multicohort/`:

```
table_body.tex     what is copied into assets/
table1_wide.csv    the table as data
table1_long.csv    every cell traced to its describe.txt or reg-summarize log line
parse_report.txt   the parse record
```

PARSE-not-recompute over v0618 aggregated logs. No raw `.dta` and no micro-data are read, which
is what makes this runnable off the secure server.

## Known caveat, carried from the task

The osteoarthritis column is PARTIAL: it comes from regression-stage logs only, with no build
funnel behind it. That is recorded in the asset header and is not a rendering artifact.
