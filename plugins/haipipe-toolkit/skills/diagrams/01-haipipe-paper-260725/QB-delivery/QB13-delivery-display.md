# Delivery-Display: the float as an object, and only the half Paper owns

state: 🟡 PARTIAL · the float has its own series; the display-board overlap audit and QB13c are open
owner: JL
method: hold the float as its own unit, and keep only the half the paper owns rather than restating the Display layer

## Opening

What does the paper own about a float, between the renderer finishing and a sentence pointing at it?

A float is one figure or table as a delivered object: its identity, its files, its caption, and its label. A caption is an argument written in the author's voice. A label is the anchor every citing sentence depends on. Neither is produced by a renderer.

**Where this page sits**: QB5 Display holds the ownership seam against `/haipipe-display`, and this series holds the object itself.
Its three faces are QB13a what one unit is made of, QB13b who may invoke a renderer, and QB13c the caption and the label.
QB11c holds where the float lands, because that is decided by the first citing section.

**Why neither neighbouring series can hold it**: a float is not a section, since it has no outline and no paragraph order.
It is not a sentence either: QB12c and QB12d specify the POINTER, and a pointer says nothing about what it points at.
Between the two sits an object with its own identity, its own two filesystem roles, and its own authored text.

**The risk this page is carrying openly**: the display board may already own two of these three faces.
That is stated here rather than left for a reader to discover, and the audit is an Aim below.

## Writing Style

How this page must be written. Read it before editing, and edit to it.

**Inherited from `QB4`**: the page grammar, the section order, and the sentence rules come from `QB4-overall.md` and are not restated here.

**Specify owning, never making**: QB5's Law splits them, and this series is the easiest place on the board to cross that line.
A page here that says how a renderer works is a page in the wrong board, whatever it is titled.

**Keep the caption and the label apart**: they arrive together and fail differently.
A weak caption is a writing problem; a changed label silently breaks every sentence that cited it.

**State an unverified overlap as unverified**: while the audit is open, any claim that a face is this board's authority must say that only titles have been compared.

## Diagram

**The float between its neighbours**: what this series owns, and what the two beside it own.

```text
        ⚙️ Display layer MAKES it
                 │
                 ▼
   ┌───────────────────────────────┐
   │  🖼 THE FLOAT AS AN OBJECT     │
   │                               │
   │  🗂 identity + two file roles │ ← QB13a
   │  🎨 who may render it         │ ← QB13b
   │  ✍️ caption  · an argument    │ ← QB13c
   │  🔖 label    · a promise      │ ← QB13c
   └───────────────┬───────────────┘
                   │ the LABEL is what a sentence may point at
        ┌──────────┴──────────┐
        ▼                     ▼
   📊 QB12c / QB12d      📐 QB11c
   the pointer            where it lands

   ⚠️ UNVERIFIED: the display board may already own QB13a and QB13b
      (titles compared, bodies not)
```

## Content

### 1 · Why the float is its own unit

**Neither neighbour fits**: what each of the two adjacent series would have to pretend.

```text
   ❌ as a SECTION          ❌ as a SENTENCE         ✅ as its OWN unit
   ──────────────           ───────────────          ─────────────────
   no outline               the pointer is not       one identity
   no paragraph order       the thing pointed at     two file roles
                                                     authored caption
                                                     a label others depend on
```

🖼 Establishes why this series exists between QB11 and QB12 rather than inside either.

#### 1.1 · One unit, one identity, two filesystem roles
(the ruling QB13a already carries, and the reason the object needs a page at all)
Board authority and rebuild state stay numbered working machinery.
Only the selected `float.tex` and its assets enter the unnumbered submission half, so one object lives in two places on purpose.

#### 1.2 · The caption and the label fail differently
(they are authored in the same moment and are not the same kind of thing)
A caption is an argument in the author's voice, and a weak one costs a reader some effort.
A label is a promise, and changing one silently breaks every sentence that cited it, which is why QB13c cannot treat them as one topic.

### 2 · The overlap this series has not audited

**Title against title**: what the display board already runs, beside what this series claims.

```text
   📋 display board 01-haipipe-display-260727    📋 this series
   ──────────────────────────────────────────    ──────────────────────
   QB1 unit-contract                    ≟        QB13a display-folder
   QB3 recipe-and-render                ≟        QB13b requested-display
   QB4 candidates-and-promotion         ≟        QB13b requested-display
   QB5 wrapper-and-placement            ≟        QB11c display-placement

   ⚠️ only the TITLES were compared ── this is a RISK, not a finding
   🚫 until the audit runs, do not cite QB13a or QB13b as this board's authority
```

⚠️ Establishes the open duplication risk, so that a reader is never misled about which board rules a float.

#### 2.1 · The audit's outcome decides whether this series keeps three faces
(if the display board already owns making, what is left here is the authored text alone)
QB5's Law says Paper owns why a display exists, what it argues, where it lands, and whether it is accepted.
Anything in QB13a or QB13b that specifies how a render is produced fails that test and belongs on the display board, leaving QB13c as the clear survivor.

## Aims

### A1 · 🖼 Why the float is its own unit
- A1.1 · The float is filed as its own unit rather than as loose faces under the Display concern.
  **Done when:** the three float faces sit under this head, and QB5 lists no detail page of its own.
- A1.2 · The caption and the label are specified as two things.
  **Done when:** QB13c states separately what a caption owes a reader and what a label owes every citing sentence.

### A2 · ⚠️ The overlap this series has not audited
- A2.1 · The claimed overlap with the display board is resolved by reading, not by title.
  **Done when:** every face in this series is either confirmed as paper-owned or deleted in favour of a pointer to the display board page that owns it.

### P · 🏁 Page-level
- P1 · Nothing in this series specifies how a render is made.
  **Done when:** a reader applying QB5's Law to each face finds no sentence about recipes, renderers, or promotion.

## States

### A1 · 🖼 Why the float is its own unit
- ✅ A1.1 · Done 260802. QB5c, QB5d, and QB5e became QB13a to QB13c under this head, and QB5f left for QB11c.
- ⬜ A1.2 · Not started. QB13c is 🔴 OPEN and is where both are specified.

### A2 · ⚠️ The overlap this series has not audited
- ⬜ A2.1 · Not started, and it is the item that matters most here. Only titles have been compared, so §2 states a risk rather than a finding.

### P · 🏁 Page-level
- 🔨 P1 · Active and unverified. QB13a's own ruling is about file roles rather than rendering, which is the right side of the line; QB13b has not been read against QB5's Law.

## Files

- `QB13a-display-folder.md` · one identity, two filesystem roles
- `QB13b-requested-display.md` · who may invoke a renderer, and what may be overwritten
- `QB13c-display-caption.md` · the caption and the label, the only 🔴 face here
- `QB5-display.md` · the ownership seam this series must respect
- `QB11c-display-placement.md` · where the float lands, held by the section series

## Law

Paper owns why a float exists, what it argues, where it lands, and whether it is accepted.
Everything about HOW it is made belongs to the Display layer, and a page here that specifies making is a page in the wrong board.

## Glossary

- **Float**: one figure or table as a delivered object, distinct from the sentence that points at it and from the section that positions it.
- **Label**: the anchor a citing sentence depends on, which is a promise rather than a piece of text.

## Log

260802 · Migrated to the QB4 page contract: Writing Style added, Content numbered into two divisions with face figures and captions, Aims regrouped as A1/A2/P with `Done when`, States mirrored per Aim.
260802 · Opened as the head of the display series; QB5c, QB5d, and QB5e became faces QB13a through QB13c, and QB5f left for QB11c because placement is a section rule.
