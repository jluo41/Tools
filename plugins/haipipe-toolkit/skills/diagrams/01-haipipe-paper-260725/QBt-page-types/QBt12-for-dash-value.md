# QBt12 · page-type DASH · family VALUE · owns NO folder; holds the binding and staleness rules

state: 🔴 OPEN · specimen opened 260809 with the contract; nothing measured here yet
page-type: dash
dash_family: value
owner: JL
method: hold one specimen per paper-owned Page Type on the paper board, pointing at the live instance rather than simulating one

## Opening
What makes a number bound, what makes a bound number stale, and which sections owe numbers no topic covers?

The two rules live here because a rule stated on four topic pages is four rules that will disagree.
The venue blueprint decides which sections exist and therefore which numbers are owed at all, which turns the inventory into a coverage statement.

**Covered here**: what this Page Type owns on disk, and the live page that proves it.

**Covered elsewhere**: the contract itself is `../../paper/page-types/haipipe-page-for-dash-value`. The interlocked specimen set for the board-owned types stays on `BoardSkillBoard-260722`'s `QBt` group.

## Writing Style
Plain English for a reader who has never opened this paper. One sentence per line, and no em-dashes.

## Content
### 1 · What this type owns on disk
**The ownership row**: what the type reads, what it writes, and what folder it keeps.

```
📥 INPUT    every unit page in its family, plus S-Open-Venue's blueprint
📤 OUTPUT   no folder and no artifact · the output is a reading on this page
📂 OWNS     nothing on disk
```
📌 This is why the specimen is thin: there is no companion folder to inspect, unlike the display and venue specimens on the board-owned set.

### 2 · The live instance
**The live instance**: the page on the MISQ paper that this type governs, and how many units it holds.

```
0-lifecycle/S04-value/S-Value-Dash.md
scale   4 value topics on the MISQ paper, plus the resource pages this type absorbed on 260809.
```
This page proves the contract is real, and it is not a fixture.
📌 A real working page beats a synthetic specimen, because it cannot rot without somebody noticing.

## Aims
### A2 · 📄 The live instance
- A2.1 · The live instance above resolves and wears the right type key.
  **Done when:** the file exists and its frontmatter declares `page-type: dash`.
- A2.2 · The live instance declares `requires: S-Open-Venue`.
  **Done when:** the ruling of 260809 is on the real page, not only in the contract.

## States
### A2 · 📄 The live instance
- ⬜ A2.1 · Not started. The live page predates the contract and does not yet carry the key.
- ⬜ A2.2 · Not started, and the same sweep closes both.

## Files
- `../../paper/page-types/haipipe-page-for-dash-value/SKILL.md`
  The contract this page is a specimen of.
- `examples/Project-Personality-OpioidRx/papers/Paper-Personality2Opioid-MISQ2026/0-lifecycle/S04-value/S-Value-Dash.md`
  The live instance on the MISQ paper.

## Log
260809 2350 · CC · opened as the paper board's specimen for this type, on JL's 260809 ruling that a specimen lives with the skill set that owns the contract
