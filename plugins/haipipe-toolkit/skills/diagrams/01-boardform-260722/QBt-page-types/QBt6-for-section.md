# QBt6 · S-Main-4 §4 Results (FABRICATED): one reader-ordered section, bound to one venue allocation

state: 🔴 OPEN · gate is JL's and has not been asked for. 2 of 3 landing lanes paid · desk, style and override rows bind; the blueprint row cannot, because the venue chain is missing its middle link
page-type: section
section_kind: results
owner: JL
method: write §4 as real prose, land each record type on the sentence that owes it, take the desk's rules from the catalog only where no blueprint can supply them, and leave the binding row open
requires: S-Open-Venue · S-Work-C · S-Work-N (none of the three exists in this group)
style-from: S-Open-Pitch (does not exist in this group)
provides: sections/QBt6-for-section.tex
needs: QA-probe/QBt5-for-value/1-drift-counts, QBt3-for-display

## Opening

Does §4 report the drift gradient only as strongly as its design allows, and does every record this section owes reach a sentence?
A section page is where other pages' records turn into prose: a number, a figure reference, a citation.
Two of those three land here; the third has an answer one folder away that nobody wrote onto this page's row.
The desk is known and its one binding rule is obeyed, but nothing has ever allocated this section a size, because the venue chain is missing its middle link.

🚫 **This is a fabricated section of a fabricated paper.** The paper is "Do typed pages reduce contract drift?", the desk is the "Journal of Imaginary Systems (JIS)", and neither exists. Every count, rate and interval below is invented and none of it may be cited. What is NOT fabricated is the shape: this is a real `for-section` page, and its two paid bindings resolve to files that really sit on disk in this group.

**Where its things are**: the group's one naming rule is that a page's companion is `<type-plural>/<page name>/`, and a section is the type that gets no companion FOLDER. Its product is one flat generated file, `sections/QBt6-for-section.tex`, named after this page exactly but sitting IN `sections/` rather than under a folder of its own. That is what the real stage does: `S06-main/section-edit/stage.md` declares `output: sections/*.tex`, generated from the .md by sync and never hand-authored, and the live MISQ paper's §3 lands at `sections/03_theoretical_framework.tex`. So the naming rule survives and the folder does not. Compare `QBt3`, whose unit IS a folder, `displays/QBt3-for-display/`.

**Why this page is also the instruction**: it is the specimen for its type. `QB4` teaches the page grammar by being a page that obeys it; this page teaches the section form the same way. The rules stay in `haipipe-board-page-for-section` and `haipipe-board-page-for-stage`, which this page never restates.

**Covered here**: the venue contract block at unit grain, the section kind as a join key, the landing surface where three record types reach prose, and what a retarget rewrites.

**Covered elsewhere**: `QB6` owns which types exist. The chain, the managed contract span, the transfer tiers and the human gate are `haipipe-board-page-for-stage`'s. The catalog side is `QBt2-for-venue`, which arrived in this group at 0100 on the day this page was drafted and changed four rows here; what that desk demands stays on that page and is pointed at, never copied.

## Writing Style

⚠️ HAND-WRITTEN, and on a real paper it is not. `stage.py sync` materializes this section from the page named in `style-from:` and rewrites it between `<!-- haipipe:style:start sha256=… -->` markers. This group has no paper root, no row in `stages/index.yml` and no generator run, so the span below was typed. No marker is written here, because a marker carrying an invented hash claims a run that never happened. This is the one place the specimen cannot be letter for letter real, and it is marked rather than forged.

**What this page actually writes under**, since `style-from:` resolves to nothing: `S06-main/section-edit/stage.md`'s `prose_rule`, which is family-level and venue-free, so it binds even with no desk picked.

Real academic sentences the user can read as a paper, one sentence per line, blank line between paragraphs.

`\citep{key}` only for a key that already greps in the paper's `.bib`. An unverified number is `{VAL:? <what>} [Q-<Stage>-<n>]` and a citation slot with no key is `\cite{TOADD} [Q-<Stage>-<n>]`; placeholder and bracket sit side by side, never fused. Never invent a key or a number to avoid a placeholder.

A finished sentence carrying a number keeps its bracket and gains a `> Value:` lane naming the atom and its state. The bracket comes off for a citation, because `\citep{key}` checks itself against the `.bib`, and never for a value.

English only, no em-dashes, and no `%%` markers: citation commands are the only LaTeX allowed in the .md.

## Stage Contract

⚠️ HAND-WRITTEN, same reason as `## Writing Style`. On a real paper everything down to `### Venue contract` sits between `<!-- haipipe:contract:start -->` markers that `stage.py sync` owns, and author prose begins after the end marker. Nothing generated it here.

### Required Inputs

- [ ] `S-Open-Venue` · the venue decision and this paper's per-section blueprint.
      Does not exist in this group. **Would provide:** the blueprint block this section's `### Venue contract` binds to.
- [ ] `S-Work-C` · the claims ledger.
      Does not exist in this group. **Would provide:** which claim each reported number serves, and the associational language boundary.
- [ ] `S-Work-N` · the narrative.
      Does not exist in this group. **Would provide:** the reader order that makes this unit `4`, and the display requests this section owes.

A real section page may not open until those three gates pass. This one opened anyway, because the group builds one specimen per type and no stage page of any kind exists here yet. That inversion is the single cause of the one UNBOUND row below, and it is recorded rather than papered over. Note which of the three matters most: `S-Open-Venue` is not just an input, it is the hop that turns a catalog into an allocation, and its absence is why the binding row is empty even though the desk is fully known.

### Stage Record

Lifecycle: Main manuscript of the fabricated paper "Do typed pages reduce contract drift?"
Rollup: none. This group has no submission page, so no float number and no page count is reconciled anywhere.
Unit: 4 of an unset reader order. `units_from` names `S-Work-N-narrative.md`, which is absent, and the fallback is the folders scaffolded under `S06-main/`, of which there are none.
Gate: 🔴 not asked for.
Hands downstream: the §4 results prose, the drift gradient at its honest label, and the two record bindings it lands.
⚠️ That last line is what `for-stage` means by `provides:`, and it is NOT what the head's `provides:` says. See `## States`.

### Venue contract

venue: Journal of Imaginary Systems (JIS) · section_kind: results · the desk prints this block as "Findings"
desk: `QBt2-for-venue`, the CATALOG, with its pack at `venue/QBt2-for-venue/`. The Findings block is that page's `### 7`.
blueprint: UNBOUND  <- this is the row that BINDS this unit, and there is nothing in it
  The blueprint is the venue STAGE's output, not the venue page's, and this group has no venue stage page. The catalog arrived and the middle hop did not, so the desk's 3,500-word total has never been divided into per-section allocations.
  binds:   nothing. The subsection count, the claim each subsection carries and the displays this section owes would all be allocated here.
  reports: nothing of its own. The pack numbers below were read from the catalog directly, so they carry one hop less authority than a blueprint row and are reported as the pack's, never as this paper's.
style: `venue/QBt2-for-venue/style-profile.md` · REFERENCE ONLY. Arc, slot patterns and refusals for Findings are indexed at `QBt2` §7; the band is 700 to 1,100 words and the pack marks it a PACK PRESCRIPTION, which is weaker than an observation and much weaker than a rule.
override: YES, and it outranks every row above. `QBt2` §9 step ④ is the desk's own binding content rule: every table names the command that regenerates it, in the sentence that introduces it. P3.S1 carries it. The desk's other length rule is a 3,500-word TOTAL, which defers to the deliver gate rather than binding a section, and the desk does not publish what the 3,500 counts, which is `QBt2`'s own open row and not this page's to close.
supersedes: nothing. No generated `### Venue` heading exists on this page to collide with, because no sync ran.

⚠️ DEVIATION, recorded rather than hidden. A section page may not read the catalog: the venue stage reads it once and every later page reads the blueprint. This page read `QBt2` directly, because the stage that would have read it for me does not exist. Every fact above is stamped with the catalog division it came from, so a later blueprint can be checked against what was taken. The moment a venue stage page lands, three rows here are re-derived from its blueprint and this note is deleted. That three-line width is the retarget surface at unit grain, and it is the reason this type exists rather than folding into `for-stage`.

## Diagram

**The join at unit grain**: which of this section's three venue joins resolve today, and which are open.

```text
🎚 section_kind: results   ← the join key, one name lining up three things
     three owners wrote at three different times

  ① the venue page's matching division   ✅ QBt2-for-venue · ### 7
       read ONCE, by the venue stage         "Sec-3-Results", printed
       ⚠️ read HERE instead, by this          by the desk as "Findings"
          page, because there is no
          venue stage in this group

  ② the blueprint's per-kind block       🚫 THE MISSING LINK
       BINDING when it exists                no venue STAGE page exists,
       nothing else on this page             and the blueprint is the
       binds a size or a shape               stage's output, not ①'s

  ③ the (venue x kind) template          ⚠️ pack has style, no template
       reference only, always                style-profile.md ✅
                                             template.md ❌ · fell back
                                             to section-edit/template.md

  📄 THE HOP IS TWO DEEP, and here it is one: ① ─╳─ ② ──▶ this page
  🚫 FABRICATED desk. "Journal of Imaginary Systems" is not a venue.
```

📌 Establishes that this type exists to consume a join, and that the join's binding arm is the one that is missing.

**The landing surface**: the three record types, and whether each reached a sentence.

```text
📥 WHAT LANDS HERE        FROM                        SENTENCE     STATE
─────────────────────────────────────────────────────────────────────────
🔢 value binding          QA-probe/QBt5-for-value/    §4.1 P1.S2   ✅ paid
                          1-drift-counts              §4.2 P3      ✅ paid
                                                      §4.2 P4      ✅ paid
🖼 display acceptance     QBt3-for-display            §4.2 P3.S1   ✅ paid
                          at rung ④ ACCEPTED
📚 citation binding       QBt4-for-literature E1      §4.1 P2.S3   ⬜ open
                          the ANSWER is there and                    TOADD
                          names nowhere2030drift ·
                          the BINDING row for this
                          consumer was never written
─────────────────────────────────────────────────────────────────────────
  both paid lanes bind BY ID, in this page's head. no prose types a path.
  the open lane cannot be paid from here: a manuscript leans on a citation
  BINDING written on the asking row, never on a digest, and that row is
  QBt4's to write. 🚫 every number and every key involved is invented.
```

📥 Establishes where other pages' records become sentences, which is the job a section page does and no other type does.

**The form**: what the section is, and what the pack would have it be.

```text
§4.1 The corpus and the drift measure            2 P
  P1. what was counted, and over what window      4 sentences
  P2. what one drift event is                     4 sentences  ← TOADD here
§4.2 Drift by type-key tenure                    2 P
  P3. the gradient, and the figure that draws it  4 sentences
  P4. what the intervals separate                 4 sentences
§4.3 What the gradient does not establish        1 P
  P5. the association ceiling                     4 sentences
────────────────────────────────────────────────────────────────────────
  MEASURED   3 subsections · 5 P · 20 sentences · 373 prose words
  260806     1 display reference · 1 open citation slot · 0 tables set
             here (the count table is QBt3's float)
             counted by sections/source/build.py, off the generated .tex

  AGAINST    words          373 vs 700-1,100     ❌ 327 short of the floor
  QBt2 §7    band order     definition order      ✅ no effect-size sort
  a PACK     displays owed  1 count table         ✅ QBt3, cited at P3.S1
  PRESCRIP-  regen command  named at P3.S1        ✅ the DESK's rule ④
  TION, not  close on       CONTAINS the Limits   ⚠️ §4.3 is that block
  a desk     Limits         block instead            see the note there
  rule       verb vs the    unavailable           ⬜ no abstract exists
             abstract's                              in this group
             citation and  not recorded by the    ⬜ nothing to report
             value density  pack at all              against
────────────────────────────────────────────────────────────────────────
  ⚠️ every row here REPORTS and none binds, except the regen command,
     which is the desk's own and binds. A pack prescription is a target
     somebody wrote down, and no blueprint has ever allocated this
     section anything. 🚫 these count the section, not any corpus.
```

📐 Establishes the section's own shape against the only numbers available, and marks which of them could ever fail a gate.

## Content

### §4.1 The corpus and the drift measure

**Landing lanes, §4.1**: which sentence here owes a record, and whether it is paid.

```text
  P1.S2  🔢 400 pages           ← DERIVED: the five band counts summed, from
                                  QA-probe/QBt5-for-value/1-drift-counts
                                  deliberately NOT from that page's E2
  P2.S3  📚 drift-measure       ← \cite{TOADD}. QBt4-for-literature E1 has
         precedent                the answer and names nowhere2030drift,
                                  and no citation binding was written for
                                  this consumer, so the slot stays open.
                                  THE deliberate hole on this page.
  🚫 the 400 is invented, and so is every key just named.
```

🔢 Establishes the analysis set and the outcome definition, and marks the one citation this section cannot pay.

#### P1. What was counted, and over what window

(the analysis set is the five tenure bands, and its total is derived rather than taken from the census record)

The analysis set is every page in the Fabricated Corpus that carries a resolvable type key, or a filename from which a type can be inferred.
That set is 400 pages [Q-Sec4Results-2], the sum of the five tenure bands reported in the next subsection.
Each page contributes one observation per release window, and the window is the last release of the fabricated cycle.
Pages that were archived, generated, or empty at the window's start are outside the set.

> Value: 400 · DERIVED, the `pages` column summed over the five rows of `QA-probe/QBt5-for-value/1-drift-counts` (214 + 61 + 48 + 44 + 33) · resolved by id from this page's `needs:` line · state=verified against that atom's `counts.csv`
> Value: the same total is the answer `QBt5`'s E2 division already holds, and this sentence deliberately does NOT bind E2. That division is left with no consumer on purpose, to show what an answered question nobody uses looks like. Binding it from here would close another page's open row from the outside, so it is routed to `QBt5` and not taken.

#### P2. What one drift event is

(the outcome definition, and the one citation slot no binding row backs)

A drift event is one release in which a page's rendered sections stop matching the sections its contract declares.
The measure is binary per page per window, so a page that drifts in three sections at once counts once.
This follows the section-level definition used in earlier contract-conformance work \cite{TOADD} [Q-Sec4Results-3].
No severity weight is applied, so a missing section counts the same as a renamed one.

> Citation: `\cite{TOADD}` is correct here and is not a shortcut, and the reason changed at 0100 on the day this page was drafted. `QBt4-for-literature` arrived, and its E1 answer names three neighbouring keys, of which `nowhere2030drift` is the one that measures drift without typing a page. That is the precedent this sentence wants. It still may not be cited, because that page's own rule is that a manuscript leans on the citation BINDING written on the asking row, which is a key plus one positioning sentence plus a verdict, and never on a digest. No such row exists for this consumer, writing it is `QBt4`'s work and not this page's, and lifting the key out of a digest is exactly the shortcut that rule exists to stop. The placeholder and its bracket sit side by side, which is what makes this a hole a question can fill rather than a hole nothing will ever find.

### §4.2 Drift by type-key tenure

**Landing lanes, §4.2**: the same question, for the subsection that carries the result.

```text
  P3.S1  🖼 the figure          ← QBt3-for-display · rung ④ ACCEPTED 260806
                                  its §4 Placement quotes THIS sentence's first clause
  P3     🔢 17.3 → 3.0          ← QA-probe/QBt5-for-value/1-drift-counts
                                  DERIVED there as drift_events / pages
  P4     🔢 the interval bounds ← same atom, ci_low and ci_high, carried by
                                  the atom and never drawn on the figure
  🚫 every rate and bound above is invented.
```

📉 Establishes the gradient, and closes the placement row that `QBt3` opened against this page.

#### P3. The gradient, and the figure that draws it

(the one result the section exists to report, and the sentence QBt3's placement record quotes)

Drift falls across all five bands (Figure~\ref{fig:drift-tenure}), a count table that `python3 unit.py build` regenerates from its counts atom.
Pages whose type is inferred from a filename drift at 17.3 percent, 37 events over 214 pages [Q-Sec4Results-1], while pages that have declared a type key for thirteen months or more drift at 3.0 percent, 1 event over 33 pages [Q-Sec4Results-1].
The three intermediate bands fall in order between those two, at 14.8, 8.3 and 4.5 percent [Q-Sec4Results-1].
The ordering is monotone across all five bands.

> Value: 17.3 · 14.8 · 8.3 · 4.5 · 3.0 · DERIVED in that display unit's `source/build.py` as `drift_events / pages` · atom `QA-probe/QBt5-for-value/1-drift-counts` · state=verified against `counts.csv`
> Display: `QBt3-for-display` · `\ref{fig:drift-tenure}` · that unit is at rung ④ ACCEPTED and its `§4 Placement` division names `S-Main-4-results §2` as this consumer, quoting the sentence as "Drift falls across all five bands (Figure 1)." Two differences from the quote, and neither is a disagreement. The quoted form says `Figure 1` where this prose writes `\ref{fig:drift-tenure}`, because the printed float number belongs to the submission stage and no section page may hard-code one. And the sentence continues past the quote, because `QBt2` §9 step ④ is a DESK rule: every table names the command that regenerates it, in the sentence that introduces it. The quoted form is this sentence's opening clause, word for word.
> Display: the second row of that same placement record, `S-Open-Pitch §1`, is still `⬜ named, not cited`, which is why the unit sits at rung ④ rather than ⑤. That row is not this page's to close, and no page in this group can close it, because `S-Open-Pitch` does not exist here either.

#### P4. What the intervals separate

(the honest reading of five overlapping intervals)

No adjacent pair of bands is separated by its 95 percent intervals.
The band inferred from a filename spans 14.2 to 20.4 percent and the 0 to 3 month band spans 10.1 to 19.4 percent [Q-Sec4Results-1], and every neighbouring pair overlaps in the same way down the ladder.
What the intervals do separate is the two ends, 14.2 to 20.4 percent against 1.1 to 7.2 percent.
The gradient is therefore read as a whole and never rung by rung.

> Value: the four bounds are the `ci_low` and `ci_high` columns of the same atom · state=verified against `counts.csv` · the figure carries these columns without drawing them, which is recorded in `QBt3` §1 as a thing the render deliberately leaves out

### §4.3 What the gradient does not establish

**Landing lanes, §4.3**: the subsection that states no number, and the block it turns out to be.

```text
  P5     🔢 no number           ← this subsection prints none, on purpose
  P5     📥 the LIMITS lane     ← copied WHOLE from that atom's ## Caveats,
                                  never digested, per that record's own rule
  ⚠️ THIS SUBSECTION IS THE DESK'S Sec-?-Limits BLOCK, misfiled
     QBt2 §8: the desk requires Limits, reads it SECOND, and the
     resolver declares NO `limits` kind, so no S-Main page can join it.
     Its two slots are "X was not assigned, so this paper reports
     association only" and "Y is not controlled": both are filled by
     P5 below, in this section, because there is nowhere else to put them.
     The desk's Findings arc asks this block to CLOSE by pointing at
     Limits. It closes by CONTAINING it. Unruled; see Decision Now.
  🚫 the limits are real rules about a fabricated count at a fake desk.
```

⚠️ Establishes the ceiling the design puts on the whole section, and that the block carrying it has no home of its own.

#### P5. The association ceiling

(what the design forbids this section to say)

Tenure is not assigned.
A page declared its type key whenever its author reached it, so pages that declared early may differ from pages that declared late in ways this count cannot see.
Page size is not controlled either, and a longer page has more sections available to drift.
This section therefore reports an association and never a reduction, and every later sentence that leans on it inherits that ceiling.

> Value: both limits are copied WHOLE from `QA-probe/QBt5-for-value/1-drift-counts`'s `## Caveats` rather than digested, which is that record's own rule. A digest of an ANSWER is a convenience; a digest of a LIMIT is how a paper ends up claiming more than its design supports.
> Citation: `QBt4-for-literature` E2 answers the outside half of this ceiling, that no published design assigns a type key either, and its own row for that answer is still `⬜`. This subsection does NOT need it and does not open a second hole: P5 states only what THIS design cannot support, which is a fact about this paper. The key would be owed the moment a sentence here said the field has done no better, and no sentence here says that.
> Route: this ceiling contradicts the paper's own title. `QBt2` §2 records that "Do typed pages reduce contract drift?" puts a causal verb over a design where tenure was never assigned, and that this is the desk's most common desk-reject, caught at the title. A section page cannot rename the paper. Routed to whoever owns the seed, and named here so it is not discovered at the portal.

## Aims

### §4.1 · The corpus and the drift measure
- Q-Sec4Results-2 · The analysis set is derived from a bound atom, never typed.
  **Done when:** P1's total resolves through this page's `needs:` line and `python3 unit.py check` reports that atom built.
- Q-Sec4Results-3 · The drift-measure citation slot is filled, or the section states on the page that it cannot be.
  **Done when:** `QBt4-for-literature` E1 carries a citation binding row naming this page as its consumer, and P2.S3 writes that row's key in place of `\cite{TOADD}`; or this Aim is retired with a dated line saying the section ships with one open slot.

### §4.2 · Drift by type-key tenure
- Q-Sec4Results-1 · Every number §4 prints traces to one atom by id.
  **Done when:** each `[Q-Sec4Results-1]` bracket has a `> Value:` lane naming the atom, and `unit.py check` resolves it.
- Q-Sec4Results-4 · The display reference resolves to an accepted unit.
  **Done when:** `needs:` lists `QBt3-for-display`, that page's state line reads rung ④ or higher, and check resolves its product.

### §4.3 · What the gradient does not establish
- Q-Sec4Results-5 · No sentence in §4 outruns the atom's stated limits.
  **Done when:** the strongest verb in the section is "is associated with", and §4.3 carries both limits in the atom's own words.
- Q-Sec4Results-6 · The Limits block sits where the desk reads it, or the deviation is ruled.
  **Done when:** either a `limits` section kind exists and this content moves to its own page, or a dated ruling on this page says Findings carries it and the desk's "close by pointing at Limits" move is dropped with a reason.

### P · page-level
- P1 · The venue contract block binds its blueprint row instead of reporting a hole.
  **Done when:** a venue STAGE page exists in this group, its blueprint allocates the Findings block a size and a shape, and this page's blueprint, style and override rows are re-derived from it rather than from the catalog.
- P2 · The `.tex` this page provides is generated and never hand-edited.
  **Done when:** `python3 unit.py build` reproduces `sections/QBt6-for-section.tex` from this page's Content, byte for byte.

## States

- ✅ Q-Sec4Results-1 · Five rates, two numerator and denominator pairs and four interval bounds print in §4, and every one has a `> Value:` lane naming the atom. Nothing in Content types a path.
- ✅ Q-Sec4Results-2 · 400 is derived and marked derived. It is not taken from E2, which stays unconsumed.
- ⬜ Q-Sec4Results-3 · One `\cite{TOADD}` stands at P2.S3. The answer exists at `QBt4` E1 and the binding row does not. This is the deliberate hole; see the record below.
- ✅ Q-Sec4Results-4 · `QBt3-for-display` is at rung ④ and its product is built, so the reference resolves. The desk's regeneration rule is carried in the same sentence.
- ✅ Q-Sec4Results-5 · §4.3 carries both limits verbatim and the section's strongest verb is "falls", used of the sample and never of a cause.
- 🧠 Q-Sec4Results-6 · §4.3 IS the desk's Limits block, sitting inside Findings because the resolver declares no `limits` kind. Nobody has ruled it.
- ⬜ P1 · The blueprint row is open, and it is the row that binds. Blocked on a venue stage page that does not exist.
- ✅ P2 · The `.tex` is produced by `sections/source/build.py` and reproduces from this page's Content.

- 260806 CC · Written as the `for-section` specimen: a real §4, not an essay about §4s. It is `S-Main-4-results`, which is the consumer `QBt3`'s placement record already names, so writing this page closed a row that was open on a sibling. That record quotes P3.S1, and P3.S1's opening clause is that quote word for word.
- 260806 CC · This page owns no companion FOLDER, and that is the type's answer rather than an omission. The group's naming rule gives the companion the page's name, and a section's output is one flat generated file, `sections/QBt6-for-section.tex`, because `section-edit/stage.md` declares `output: sections/*.tex`. The generator sits at `sections/source/build.py` and is ONE script for every section page in the group, not one per page, which is the shape of the real `stage.py sync`. `unit.py`'s walk-up search for `source/build.py` accommodated both companion shapes without being told about either.
- 260806 CC · ⚠️ `provides:` means two different things and one line cannot hold both. `for-stage` says `provides:` is what this page hands downstream "in one line", and the live `S-Main-3-theory` writes prose there: `provides: accommodation mechanism, discretion boundary, and theory-derived H1-H3`. `unit.py` reads `provides:` as exactly one path and fails the page when the product does not exist, so a real section page's `provides:` line makes `check` report a missing product. The head here carries the PATH, because the engine is what runs, and the prose sentence moved into `### Stage Record` as a `Hands downstream:` line. Unruled, and in Decision Now.
- 260806 CC · ⚠️ `P<n>` is ambiguous on every section page and nobody has noticed. `for-stage` assigns page-level Aims the id `P<n>`, and the same contract numbers Content paragraphs `#### P<n>. <its job>`. So this page has an Aim `P1` and a paragraph `P1`, and they are different things. Written to the contract anyway rather than fixed locally, because a specimen that quietly renames an id teaches the wrong id. In Decision Now.
- 260806 CC · ⚠️ The per-division figure rule and the manuscript-section exemption disagree, and this page satisfies the group. Every Content division here opens with a caption, a figure and an emoji line, which is the base's rule and this group's house shape. `for-stage` records the opposite for exactly this kind of page: a manuscript section has no ASCII figure, `check.py` warns `division-no-figure` on a journal section's subsections, and "the page must NOT satisfy it", with no way for a variant to suppress a base rule. The figures here were made to earn their place rather than to satisfy a count: each one is that subsection's LANDING LANES, which is a section-page fact and not decoration, and the section's own form block sits once in `## Diagram` where the real page keeps it. Recorded, not settled.
- 260806 CC · Aims carry ids and States mirror every one of them exactly once, which the live `S-Main-3-theory` does not do: it runs 21 un-ided Aims with no `Done when` and no mirroring State row, so the render's `met/total` counter reports nothing about it. `for-stage` calls that the expensive half of conformance. A specimen that skipped it would teach the cheap half only.
- 260806 CC · The `state:` line is a gate position and the base's four health words fit it without a fight, which is worth recording because `QBt3` hit the opposite. A display page's contract asks its state line to carry a ladder RUNG, and `cli/check.py` rejects any state line not opening on a health word. A section page has no ladder: its state IS `🔴 OPEN` until a person passes CHECK, so the health word and the gate position are the same token.
- 260806 CC · REBOUND at 0100. This page was drafted venue-unbound, with all four rows reading UNBOUND and `override:` reading UNKNOWN, because `QBt2-for-venue.md` and `venue/QBt2-for-venue/` were both absent at 0054. They landed six minutes later and three rows changed: `desk:` names the catalog, `style:` names `style-profile.md` as reference only, and `override:` turned from UNKNOWN into a real YES, because `QBt2` §9 step ④ publishes a per-section content rule and a published per-section rule outranks any blueprint. The Findings block's arc, its 700 to 1,100 word band, its owed display and its two refusals all came into view at the same moment.
- 260806 CC · ⚠️ The blueprint row did NOT bind, and that is the finding rather than the leftover. A section page reads the BLUEPRINT and never the catalog: the venue stage reads the catalog once and turns the desk's total into per-section allocations. This group has a catalog and no venue stage, so the chain is `① ─╳─ ② ──▶ page` and the arm that binds is the missing one. This page read the catalog itself to fill three rows, which is a rule break, so every fact taken carries the catalog division it came from and the deviation is written into `### Venue contract` where a reader meets it before the facts.
- 260806 CC · The word band is a REPORTING row and it is missed: 373 prose words against 700 to 1,100. Per `for-stage`, a missed reporting row is logged with its reason and never treated as a failed gate, and there are two reasons here. The pack marks its own band a PACK PRESCRIPTION, weaker than a measurement, because no JIS paper exists to measure. And no blueprint ever allocated this section anything, so there is no number that this paper agreed to.
- 260806 CC · The desk's binding content rule changed one sentence of prose, which is what a per-section desk rule is supposed to do. `QBt2` §9 step ④ demands that every table name the command that regenerates it in the sentence that introduces it, so P3.S1 now ends "a count table that `python3 unit.py build` regenerates from its counts atom". `QBt3`'s placement record quotes that sentence, and the quote is now its opening clause rather than the whole of it. Recorded in the `> Display:` lane so the two pages do not read as disagreeing.
- 260806 CC · ⚠️ §4.3 is the desk's Sec-?-Limits block wearing a Findings subsection number. `QBt2` §8 records that the desk requires Limits, reads it second, and that the resolver declares no `limits` kind, so no S-Main page can join it and the block's index prints as a visible hole. Its two slots are already filled by `QBt5`'s E1 caveats, which are exactly P5's two sentences. The content has to live somewhere, so it lives here, and the desk's Findings arc asks Findings to close by POINTING at Limits rather than by containing it. That is a deviation on a shape row, and it is in Decision Now rather than fixed, because fixing it means either adding a section kind or dropping a desk move.
- 260806 CC · The citation hole survived the arrival of `QBt4-for-literature` and got a better reason. Before 0058 the reason was that no literature page existed. Now the answer exists, E1 names `nowhere2030drift` as the neighbour that measures drift without typing a page, and the slot still cannot be filled, because a manuscript leans on the citation BINDING written on the asking row and never on a digest. That binding row is `QBt4`'s to write. Lifting the key out of the digest would have closed the hole and broken the rule the sibling page exists to teach.

### Decision Now
- 📍 `provides:` collides. **A ·** the head carries the path `unit.py` needs and the downstream sentence lives in `### Stage Contract`, which is what this page does. **B ·** `for-stage` keeps the prose line and the engine grows a second key such as `product:`. Until this is ruled, a real section page cannot pass `unit.py check` and a specimen cannot match a real page.
- 📍 `P<n>` means both a page-level Aim and a Content paragraph on the same page. **A ·** rename the page-level Aim id in `for-stage`. **B ·** rename the paragraph marker, which is 17 live pages and the tex generator. This page runs the collision rather than dodging it.
- 📍 A manuscript section's Content divisions: figure or no figure? `for-stage` says the base's rule must NOT be satisfied here and gives no way to suppress it; the group's house rule says every division opens with one. Ruling either way is cheap now and expensive after ten section pages exist.
- 📍 The blueprint hop is missing and this page read the catalog instead. **A ·** build a venue stage page for this group, re-derive the three rows from its blueprint, and delete the deviation note. **B ·** rule that a group with one desk and one paper may let a section read the catalog directly, and say so in `for-section` so it stops being a break. This is the row that keeps the page at 🔴.
- 📍 §4.3 is the desk's Limits block with nowhere to live. **A ·** add `limits` to the closed kind set in `section-kinds.yml` and give it its own page, which makes a join key that resolves for this desk and for no other. **B ·** rule that Findings carries Limits at this desk, and drop the arc's "close by pointing at Limits" move as unreachable. `QBt2` §8 already prints the hole rather than filling it, so the two pages are waiting on one ruling.

## Files

- `sections/QBt6-for-section.tex`
  The one artifact this page provides, and the whole of its companion. GENERATED; never hand-edited.
- `sections/source/build.py`
  The generator. ONE script for every section page in the group, not one per page, because a paper's section sync is one engine and not a per-unit recipe. It reads only `#### P<n>` prose and drops the board-side apparatus: captions, figures, emoji lines and every `> Value:` or `> Display:` lane.
- `QBt5-for-value.md`
  The evidence page whose E1 division owns the atom every number here traces to. Its E2 is deliberately left unconsumed and this page keeps it that way.
- `QBt3-for-display.md`
  The display page whose `§4 Placement` record names this page as a consumer and quotes P3's first clause.
- `QBt2-for-venue.md`
  The catalog. Its `### 7` is this section's matching division, its `### 8` is the Limits block with no kind, and its `### 9` step ④ is the one desk rule that binds a sentence here.
- `venue/QBt2-for-venue/style-profile.md`
  The pack. Reference only, and read directly by this page only because no venue stage exists to resolve it. Line 22 carries the 700 to 1,100 band, lines 68 to 74 the Findings arc and its two refusals.
- `QBt4-for-literature.md`
  The outward evidence page holding the answer P2.S3 wants, and the page that owns the citation binding this page cannot write for itself.
- `unit.py`
  The resolver. `check` verifies that both declared needs resolve and that this page's product exists.
- `../../board/page-types/haipipe-board-page-for-section/SKILL.md`
  The contract this page is an instance of. If the two disagree, the contract wins and this page is the defect.
- `../../board/page-types/haipipe-board-page-for-stage/SKILL.md`
  The contract the one above loads first: the chain, the managed span, the venue tiers, the gate, and the Aim id scheme this page uses.
- `../../paper/S06-main/section-edit/stage.md`
  The stage that produces pages of this kind, and the source of the `output:`, `prose_rule` and `section_kind` facts this page states.
- `../../paper/haipipe-paper/stages/section-kinds.yml`
  The closed set `section_kind: results` is drawn from.
- `QB-delivery/QB4-overall.md`
  The page frame this page sits in, including the caption rule every Content division above obeys.

## Log

- 260806 0054 · [DRAFT-CC] written as a real `for-section` page rather than an essay about one, on JL's ruling that a specimen BE its type. Unit chosen as `S-Main-4-results` so the page closes the consumer row `QBt3`'s placement record already opened, and P3.S1 opens on the clause that record quotes. Both bindings are by id; no prose types a path; every number is invented.
- 260806 0054 · [DRAFT-CC] drafted venue-unbound. `QBt2-for-venue.md` and `venue/QBt2-for-venue/` were checked for at 0054 and were absent, so all four venue rows reported holes and `override:` read UNKNOWN rather than `none`, because `none` is a fact about a desk somebody read.
- 260806 0100 · [REVISE-CC] rebound against `QBt2-for-venue`, which landed at 0100, and against `QBt4-for-literature`, which landed at 0058. Three venue rows now bind and one still cannot: the blueprint is the venue STAGE's output and this group has no venue stage, so the chain runs `catalog ─╳─ blueprint ──▶ page` and this page read the catalog itself, which is a recorded deviation and not a licence. The desk's binding rule ④ rewrote P3.S1 to name the command that regenerates the count table. The section's 373 words are reported against the pack's 700 to 1,100 band as a miss with two reasons. `§4.3` was identified as the desk's Sec-?-Limits block living inside Findings because the resolver declares no `limits` kind, which is now a Decision Now row shared with `QBt2` §8.
- 260806 0100 · [REVISE-CC] the citation hole kept its `\cite{TOADD}` and changed its reason. `QBt4` E1 names `nowhere2030drift`, which is the precedent P2.S3 wants, and no citation binding row exists for this consumer. A manuscript may lean on a binding and never on a digest, so the slot stays open and the work is routed to `QBt4` rather than taken.
- 260806 0100 · [PROBE-CC] four conflicts surfaced by building the specimen and all four are in `### Decision Now`: `provides:` means a path to the engine and a sentence to the contract, `P<n>` names both an Aim and a paragraph, the per-division figure rule contradicts the manuscript-section exemption, and the Limits block has no section kind to live in. None was settled here.
