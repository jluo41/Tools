# This board's map
state: 🟡 PARTIAL · the convention is JL's (260729); its graduation into the skill is open
owner: JL
method: one face that says what this board's groups and folders are, so a newcomer places anything in one read

## Question
What is on this board, group by group, and where does each kind of thing live?
Every board is to open with a `QA0` like this one (JL 260729): the board's own structure information, so a reader who knows nothing can place a page, a group, or a folder without reading the skill.
It is the instance-level half of `QA1`: QA1 maps the family's two folders; QA0 maps THIS board.

## Boundary
- ✅ Covered here
  This board's groups, their folders, and the 260729 restructure; the convention that every board carries a QA0.
- ↪ Covered elsewhere
  The family map (skill set vs board folder, crossings, standing): `QA1`.
  What the words mean: `QA1a`. What the index shows and how the surface looks: `QA10`. How pages and groups are proposed: `QC4`.

## Diagram

```
   01-boardform-260722/            spine: pin down what a board IS
   │                               40 pages · 7 group folders
   ├── board.md                    the source: spine · close · Pages · Links
   ├── board.html                  generated by build.py, never hand-edited
   ├── board.excalidraw            ONE scene, one frame per page, at the ROOT
   │                               as a first-class citizen (JL 260729)
   ├── fig/                        images only, 7 of them
   ├── _archive/                   4 retired pages, moved and never deleted
   ├── meeting/                    one dated meeting note
   │
   ├── QA-defining-a-board/     6  the board itself
   │     QA0 this map · QA1 the two folders + where a board lives
   │     QA1a the vocabulary · QC3 page folder management
   │     QA9 what is checked · QA10 the index page + visual design
   ├── QAa-the-page/            8  the page, section by section (JL 260729)
   │     QAa0 overall + the source template · QAa1 opening
   │     QAa2 diagram + the canvas · QAa3 content + markers · QAa4 items
   │     QAa5 where-we-are + 🧩Skills · QAa6 folds · QC4 proposals
   ├── QAb-the-sentence/        5  the sentence and what attaches to it
   │     QAb0 overview · QAb1 evidence card · QA6 comments
   │     QAb2 editing · QAb3 agent visibility
   ├── QB-shipping-the-skill/   7  SKILL.md · acceptance · migration · splits
   │                               QB7 holds the sub-skill roster
   ├── QD-working-on-the-board/ 6  the live layer: chat · terminal · updates
   ├── QE-sharing-the-board/    6  hosting · mounting · console · bind address
   └── Q-Skill/                 2  roster rows, generated by skillpage.py
```

/_excalidraw/?board=Tools/plugins/haipipe-toolkit/skills/diagrams/01-boardform-260722/board.excalidraw&frame=QA0

## Content
### 1 · The seven groups, and what each owns
`QA` defines the board itself: its folder shape and family map (`QA1`), its vocabulary (`QA1a`), page folder management (`QC3`), what is checked after a change (`QA9`), and its index page plus the board's visual design (`QA10`, which absorbed QC2 on 260729).
`QAa` owns the page, one face per section, each face owning both projections, the source you write and the render you read; the page/group proposal rules (`QC4`) sit beside them.
`QAb` owns the sentence: the unit (`QAb0`), its evidence card (`QAb1`), comments pinned to it (`QA6`), editing it (`QAb2`), and whether agents see what it carries (`QAb3`).
`QB` ships the skill, `QD` is the live layer, `QE` is sharing, and `Q-Skill` is the generated roster of what shipped.

### 2 · The 260729 restructure, in one paragraph
The former QC group (Index and structure) dissolved: QC1 merged into `QA1`, `QC2` merged into `QA10`, `QC3` joined QA and was renamed to page folder management, `QC4` joined QAa.
The former `QA4` became `QAa0` and was carved into one face per section; `QA4a` became `QAa2`; `QA8` split into `QAb0`+`QAb1`; `QA8a` became `QAb3`; `QA6` moved groups with its id kept; `QA2` moved, then merged into the QAa faces on 260729, because the source and the render are one subject per face.
Ids were kept wherever a page was cited from the skill or another board; only pages whose subject changed were renamed.

### 3 · The QA0 convention
Every board opens with a `QA0` carrying its own structure information (JL 260729).
It graduates into the skill as one line in the `open` verb (create QA0 with the board) and a guide block in `ref/q-template.md`; until then it binds only here.

## Items to Finish
- [x] 🗺 This board's QA0 exists and matches the tree on disk
      Written 260729, the same round as the restructure it records.
- [ ] 📐 The convention graduates: `open` creates a QA0, and the template carries its guide text
      One line in `SKILL.md`'s open verb, one block in `ref/q-template.md`; owned jointly with `QAa0`, which owns the template since QA2 merged into the QAa faces (260729).
- [ ] 🗺 The other four design boards gain a QA0
      paper · task · display · probe-qa; each is one page, written by its own board's session, not by this one.

## Where we are
This board has its QA0; the convention is decided and not yet graduated, so no other board has one yet.

- Measured against disk on 260729, after the second restructure round
  40 pages in 7 group folders, of which 38 are faces and 2 are generated roster rows.
  `_archive/` holds 4 retired pages: QA2, QC1, QC2 and QD5, each reachable through a `## Links` row.
- The scene is a root file, not a figure (JL 260729)
  `board.excalidraw` sits beside `board.md` and `board.html` rather than under `fig/`, which now holds images only.
  This was found the same round as a defect rather than a preference: 35 faces were pointing at `fig/board.excalidraw`, which returned 404, while the real scene had been at the root since 260729 1211.

## Files
- `board.md`
  The `## Pages` groups this face describes.
- `SKILL.md`
  The open verb, where the convention lands.
- `ref/q-template.md`
  Where the QA0 guide text lands.

## Log
260729 · Second restructure round recorded: QC2 merged into QA10 (the index page and the board's visual design are one face now), QC3 kept its id and was retitled to page folder management, and the QC group is fully dissolved. The tree in `## Diagram` and the group descriptions in §1 and §2 were re-measured against disk rather than edited from memory
260729 · Opened on JL's call ("every board should have it, for the board structure information"), written to match the tree after the same day's restructure
