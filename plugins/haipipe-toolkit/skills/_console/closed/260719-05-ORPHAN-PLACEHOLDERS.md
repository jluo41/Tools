# 260719 · 无主占位符(论文侧 19 处)

**话题**:正文里的 `{VAL:?}` / `\cite{TOADD}` 待填标记,没有任何 Q-consumer 认领
——没人负责把它填上,所以它永远填不上,而 checker 的 PASS 4 会 FAIL。

来源:`260719-02-PHASE-BOUNDARY-REFACTOR.md` 第 ② 行(JL 在该板 D4 裁「再开一个吧」)。
那块板是「相位边界重构」的话题,这件事不属于它。

```
  D<n>  DECISION   等你在 `> JL:` 里拍板
  {VAL:?}      待填的数值占位符
  \cite{TOADD} 待填的引用占位符
  Q-consumer   stage doc 里带 stake 的问题;它认领一个占位符,就等于承诺去问
  PASS 4       check-probe-cards.sh 里查占位符所有权的那一关
```

---

## 📍 现状

```
  4-llmtrait     3 处
  5-empirical    4 处
  6-results     10 处   ← 全是数字,最硬:每个都要一次真 probe 才能落地
  7-discussion   2 处
  ─────────────
  合计          19 处

  机制上为什么会漏成这样(D9 的背景):
    {VAL:?} 此前【没有任何机制】管 —— checks.sh 和 check-probe-cards.sh 都不查,
    而唯一被查的 `% TODO` 早已退役。checker 在守一个死标记,放着两个活的不管。
    PASS 4 是 260719-02-PHASE-BOUNDARY-REFACTOR D9 新建的,它把缺口关上了,
    于是这 19 处一次性浮出来。
```

## ⚖️ 裁决账本(append-only · 你的原话逐字保留)

```
D1 · 先做哪一节?
     每个占位符要变成一个 Q-consumer,而「这个数字该由哪个问题去问」
     取决于你想让这一节说什么 —— 这是我代不了的部分。
     我能代的:列全该节占位符 → 每个起草一个 Q-consumer(Description/Reason/Answer)
              → 你过目改词 → 我写进 1-probes/ 并跑 PROBE。
     建议从 4-llmtrait(3 处)起步,最小,跑通一遍我们就都知道流程对不对;
     6-results(10 处)最后做,它最硬。
> JL:
     >> CC:

D2 · 有没有「不该存在」的占位符?
     19 处里可能有一部分不是"待填",而是当初写飘了、根本不需要那个数字。
     那种应该【删句子】,不是【开 probe】。
     要我先做一轮分类(该填 / 该删 / 该改写成定性表述)再给你看吗?
> JL:
     >> CC:
```

## 🧾 清账表

```
  编号  项目                       规模   等谁   状态
  ──────────────────────────────────────────────────
  O-1   4-llmtrait 占位符           3 处   D1     ⬜
  O-2   5-empirical 占位符          4 处   D1     ⬜
  O-3   6-results 占位符           10 处   D1     ⬜  最硬,建议最后
  O-4   7-discussion 占位符         2 处   D1     ⬜
  O-5   分类:该填 / 该删 / 该改写   19 处  D2     ⬜  可先跑,不等 D1
  ──────────────────────────────────────────────────
  0 / 5
```
