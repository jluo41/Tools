# The thin application: execution plan (JL approved the shape 260805)

Ruled context: QC6's application row (corrected reading: the recipe is shared, the roster is not).
Sequenced STRICTLY behind: ① paper phase-1 agent report (running), ② paper door fold proven on one live RUN.

## Target tree (~23 skills → ~11, one router)

```
application/
├── haipipe-application/     door + packet compiler (enter + lifecycle fold IN)
├── S01-opening/             seed · venue · pitch           stage.md pairs
├── S02-ladder/              2a-descriptions → 2b-themes → 2c-claims → 2d-advice
│                            ONE group, four rungs, each stage.md `requires:` the
│                            previous rung's gate. The family's own shape.
├── S03-literature/          → for-literature   (shared type, thin stage.md)
├── S04-value/               → for-value        (shared type, thin stage.md)
├── S05-display/             → for-display
├── S06-sections/            section-edit → for-section; narrative folds in as the section map
├── S07-deliver/             artifact · review · claim-audit · deploy   (deploy = own gate)
├── S08-iterate/             rounds fed by deployment feedback           (own gate)
├── workers/                 flat, no hubs: application-artifact knowledge only
└── venue/                   channel packs (sms · email · dashboard · report)
```

## Steps

1. MOVE (mv, retire to application/_old/, never delete): bucket dirs → SNN- dirs per the tree;
   the four phase hubs (draft/probe/revise/check... probe moves INTACT to workers/ if it carries
   real deltas — read it first, same judgment as paper's) → _old/ or workers/.
2. NEW CONTRACT FIELD: `fires-per-venue:` in each stage.md — the channel gates which stages fire
   (today prose in the door's description; becomes a declared line the packet compiler reads).
   This is application's first contribution TO the shared stage grammar; add the field's rule to
   haipipe-board-page-for-stage (one paragraph, versioned).
3. ROSTER: write stages/index.yml (paper's shape) with the ladder rungs as ordered rows.
4. REFERENCE SWEEP: exact path strings only, verify target exists before writing, changelogs stay.
5. REWRITE SOP-paper-alignment.md: the shared thing is the RECIPE (door + roster + workers on the
   board), not folder names. Cite the C ruling (QC6 A4.1) and this plan.
6. VERIFY: install.sh --global · any application check scripts · both design boards rebuild at
   baseline · a fresh-context agent resolves one application stage end to end (CLAUDE.md rule).

## Do-not

- No hub retirement before paper phase 2 lands (fallback rule).
- No bulk renames of bare names. No commits without JL's go.
