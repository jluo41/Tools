3 — THE DEFECTS
================

  Concrete, verified-on-disk gaps between skills/probe and its spec.
  Each has a grep behind it, not an opinion. Newest concern first.


D1 — THE DEAD SCRIPT AN AGENT IS STILL TOLD TO RUN   ✅ FIXED BY THE DROP
------------------------------------------------------------------------

```text
   haipipe-probe-review/SKILL.md:123   ⛔ "DO NOT invoke g2_integrity_check.py …
                                          handed a QA file it exits early, audits nothing"
        ✗ DIRECT CONTRADICTION ✗
   agents/haipipe-probe-reviewer-agent.md:43  "G2 integrity (deterministic
                                               g2_integrity_check.py)"
   agents/README.md:83                        "G2 integrity computation → …g2_…py"
```

An agent following line 43 runs a 781-line script whose CLI is folder-era
(probe.yaml + evidence.md, retired 2026-07-05). On every shape v3 produces it
exits early and audits nothing — then the process can report a "G2 ✅" that
never happened. That is worse than no gate.

  >> CC 23:41: JL's ruling (02) retires the whole judge, so this contradiction
     evaporates — no agent left to mis-instruct. Left recorded because it is WHY
     the "keep as-is" option was wrong: the gate was already half-broken.


D2 — THE RULING COUNT DISAGREES WITH ITSELF
-------------------------------------------

```text
   haipipe-probe/SKILL.md:15         "…probe-qa/ (v3, approved JL, R1-R21)"
   skills/probe/CHANGELOG.md [6.0.0]  "…R1–R18"
   haipipe-probe-review + reviewer-agent summaries   "…R1–R18"
```

Three rulings (R19/R20/R21 — the state-line contract) exist in the SKILL but
not in the changelog's headline count. Either the changelog is stale or the
count is loose. This is the exact drift a `reconcile` pass is meant to catch.


D3 — THE VERSION IS TWO MAJORS AHEAD OF ITS OWN CHANGELOG
--------------------------------------------------------

```text
   haipipe-probe/SKILL.md metadata     version: "8.3.0"
   skills/probe/CHANGELOG.md newest     ## [6.0.0]
```

The SKILL claims 8.3; its changelog's newest entry is 6.0. Nothing records what
7.x or 8.x changed. A version number nobody can trace to a changelog line is a
number that means nothing.


D4 — THE SPEC PATH IS BAKED IN BY ITS DATE, IN 86 FILES
-------------------------------------------------------

```text
   grep -rl 260714-probe-qa  →  86 files
   haipipe-probe/SKILL.md:15  hardcodes the DATED path as "spec of record".
```

`260714-` is a snapshot name on a LIVING spec. Revise the design tomorrow →
260715-*/ → all 86 pointers break at once. probe-qa's own 05 (#4) already
flagged this.
   ⇒ recommend: rename to undated  diagram/probe/  and repoint once. Dated
     copies can be snapshots; the path people CITE must never move.


D5 — THE CONSTITUTION IS 517 LINES, ALWAYS LOADED
-------------------------------------------------

Same bloat pattern just fixed in haipipe-task. haipipe-probe/SKILL.md is 517
lines with a ~90-word description leaking ~12 trigger keywords (state, working,
claim, superseded, …), re-deriving in full what probe-qa/ and (until the drop)
probe-review/ already own. The state-line bash block + R19/R20/R21 prose +
tables are reference material a pointer could carry.
   ⇒ after the drop lands, a conciseness pass (levers proven on haipipe-task:
     move threads to a log, collapse repeated tellings, point at probe-qa/).


D6 — THE MACHINE HAS NEVER RUN
------------------------------

From probe-qa/05, still true on disk:

```text
   ✅ VERIFIED BY EXECUTION          ❌ NEVER RUN, NOT ONCE
   ─────────────────────────         ────────────────────────
   the checker (10 fixtures)         the qa verb itself
   set -C claim (loser defers)       the five-step loop
   staleness / TTL                   the probe → orchestrator dispatch
   registration (282 resolve)        the harvest loop (working→answered→read)

   QA/ folders on disk:  0            new-shape probe files:  0
```

The gate is real; the engine is a spec. The drop-the-gate ruling does not touch
this — it is a transport question, and transport is the untested part.
