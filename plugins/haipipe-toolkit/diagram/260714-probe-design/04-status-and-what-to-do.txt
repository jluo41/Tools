4 — STATUS AND WHAT TO DO
==========================

  The one file to read if you read only one.
  What this session settled · what is owed · what is still yours to decide.


WHAT THIS SESSION SETTLED
=========================

```text
   ✅ JL RULED — 2026-07-14
   ─────────────────────────────────────────────────────────────────────
   R-AUDIT-1  DROP THE GATE.  A probe is COMMUNICATION between consumer and
              executor. The G1/G2/G3 review (haipipe-probe-review + its agent +
              g2 script + caveats checklist) is JUDGMENT, not communication —
              retire it. The consumer authors the claim status by hand from the
              answered QA file. Full ruling + reasoning: 02.
              CC flagged one loss (the causal/overclaim guard); JL accepted. 02.

   R-AUDIT-2  KEEP THIS FOLDER as a separate AUDIT (distinct from the probe-qa
              SPEC). Cross-linked; not a second spec. 01.
```


WHAT IS OWED (execute the ruling — NOT yet done)
================================================

All of this is EDITS to skills/, and none of it is done. Recommended order:

```text
   OWED-1  RETIRE THE JUDGE.  ⬅ the ruling, made real.
           · git mv the 4 files → skills/probe/_archive/ (review SKILL,
             reviewer agent, g2 script, caveats checklist)
           · de-register the reviewer agent
           · strip the ⑤ INTERPRET dispatch from the ~5 consumer/probe files
             (paper-probe, application-probe, paper-claims, application-claims,
             haipipe-probe SKILL "Live instruments" line)
           · add PART 9 (retired machinery) entries in haipipe-probe/SKILL.md
           · redefine mode:full = "author writes status from the QA file",
             no review skill invoked
           ⇒ this ALSO closes D1 (the dead-script contradiction) for free.

   OWED-2  FIX THE DRIFT (D2 + D3).  reconcile R1-R21 vs R1-R18; reconcile
           version 8.3.0 vs changelog 6.0.0. One pass, no logic change.

   OWED-3  RENAME THE SPEC PATH (D4).  diagram/260714-probe-qa/ → diagram/probe/
           and repoint 86 citations. Cheap now, 86-way breakage later.

   OWED-4  CONCISENESS PASS on haipipe-probe/SKILL.md (D5). AFTER OWED-1, since
           the drop removes a chunk of it. Reuse the haipipe-task levers.
```


WHAT IS STILL YOURS TO DECIDE
=============================

```text
   Q1  The causal/overclaim guard (02, the accepted risk).
       Accept the loss as-is, or fold ONLY the claim_type reminder into
       haipipe-paper-claims (no gate, no skill — a one-paragraph check)?
       👤 JL   📄 OPEN

   Q2  Execute OWED-1 now, or record-only for this session?
       The retirement touches ~9 files across probe + paper + application.
       CC will not run it without an explicit go.
       👤 JL   📄 OPEN

   Q3  The untested machine (D6). Before ANY of the above touches live data:
       run the smallest real exercise — probe-qa/05 STEP 4 already spec'd it:
          /haipipe-task qa "Does WellDoc record any menstrual/cycle indicator?" \
                           tasks/A03_welldoc_cycle_check
       one new file, no dispatch, exercises the qa verb + state line. Do it?
       👤 JL   📄 OPEN
```


COMMENT LEDGER (JL threads moved here once their edit lands)
============================================================

  How it works: JL leaves `> JL:` in a file body; CC answers inline with
  `>> CC HH:MM`; when the edit lands, the whole thread moves HERE, verbatim,
  stamped, tagged. Newest at the bottom. (Mirrors probe-qa's convention.)

```text
   (none yet — 01 and 02 each hold one OPEN `> JL:` prompt awaiting your reply.)
```


IS IT OK TO PROCEED?
====================

```text
   ✅ SAFE NOW                          ⛔ NOT UNTIL YOU SAY GO
   ─────────────────────────            ────────────────────────────
   this audit (docs only)               OWED-1 retirement (9 files)
   reading probe-qa / skills/probe      OWED-3 the 86-citation rename
                                        anything touching examples/ (Q3)
```

Nothing here has changed a single skill file. This folder is words about the
code, not the code. The next move is yours (Q2).
