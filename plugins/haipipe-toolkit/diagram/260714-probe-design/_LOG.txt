Change log — what this session actually did to skills/probe (and its consumers)
===============================================================================
Part of: diagram/260714-probe-design/  ·  the audit is files 01-04; THIS is the ledger
of the edits that executed the audit's ruling.

How it works: files 01-04 hold the AUDIT and JL's ruling (drop the gate).
This file records each CHANGE as it SHIPPED — timestamped, with the JL ruling behind
it and a one-line note of what moved on disk. Newest block at the bottom.
JL comments (`> JL:`) that land in 01-04 move HERE once their edit is applied, verbatim.


✅ SHIPPED — 2026-07-14 / 2026-07-15
====================================

[2026-07-14 23:43 · SHIPPED] the audit itself
  Files 01-04 written; committed as the record of the ruling. Two decisions taken:
  JL: keep 260714-probe-design/ as a SEPARATE audit (not folded into probe-qa).
  JL: the audit covers BOTH the design question AND the concrete defects.

[2026-07-15 00:05 · SHIPPED] DROP THE GATE — the judge is gone
  > JL: a probe is COMMUNICATION between consumer and executor, not judgment —
        drop the review/gate entirely.
  >> CC: executed. A probe is now pure transport; the consumer authors the claim's
     status (supported | refuted | inconclusive + confidence + claim_type) by hand
     from the answered QA file, in its own 1-claims.md. What went:
       · DELETED  skills/probe/haipipe-probe-review/  (SKILL + CHANGELOG +
                  g2_integrity_check.py + probe-caveats-checklist.txt)
       · DELETED  skills/probe/agents/haipipe-probe-reviewer-agent.md
       · de-registered the reviewer agent (~/.claude/agents symlink removed)
     Safe because examples/ is empty → 0 live 1-claims.md on disk → doc-only,
     fully git-reversible; nothing broke. CC's flagged loss (the independent
     causal/overclaim guard) was ACCEPTED by JL; the claim_type check survives as
     the claims skill's OWN author check (never "causes" from associational evidence).

[2026-07-15 00:05 · SHIPPED] the wiring, swept clean across both consumer families
  Removed every reference to the deleted judge — DISPATCH calls and G1/G2/G3
  status-format mentions — so nothing dangles. Files touched:
       probe        agents/README.md · agents/_archive/README.md · haipipe-probe/SKILL.md
       paper        paper-probe/SKILL.md · paper-probe/ref/per-stage-dispatch.md ·
                    paper-probe-citation/SKILL.md · paper-claims/SKILL.md ·
                    paper/haipipe-paper/fn/probes.md · paper-narrative/SKILL.md ·
                    paper/wiki/00-evidence-principles.md · paper/wiki/11-delivery-need.md
       application  application-probe/SKILL.md · application-probe/ref/per-stage-dispatch.md ·
                    application-claims/SKILL.md · application/haipipe-application/fn/probes.md ·
                    application/README.md · application/wiki/03-intervention-lifecycle.md ·
                    check/attendance-modes.md · check/gate-persona.md
       shared       skills/STRUCTURE.md (probe bucket = ONE skill, no agents)
  Verified: 0 live refs to haipipe-probe-review / -reviewer-agent / g2_integrity_check
  remain (only a PART-9 one-liner saying the gate is gone, + one dated history line).

[2026-07-15 00:05 · SHIPPED] no tombstones — deleted, not archived
  > JL: always make the skills clean, no tomb things — 这个不是垃圾桶，啥都往里面塞.
  >> CC: reversed course. First pass ARCHIVED the judge to _archive/ with tombstone
     prose; JL rejected that. Redid it: DELETE cleanly (git history is the record),
     collapsed the agents/README "Retired" graveyard, cut the verbose PART-9 entry to
     one line. The RATIONALE lives HERE in diagram/, not stuffed into the skill.
     Saved as a standing preference (probe/haipipe-probe/PREFERENCES.md).

[2026-07-15 00:08 · SHIPPED] metadata deflation — the real "how could it be that large"
  > JL: so many rubbish!!! how could it be that large? refer to haipipe-task.
  >> CC: the two probe WORKERS each carried a ~6 KB CHANGELOG crammed into the
     always-loaded `summary:` YAML field (paper-probe 5,986 ch · application-probe
     6,720 ch), duplicating their own CHANGELOG.md. Gutted both to a one-sentence
     summary + History/Contract pointers, matching haipipe-task (summary 483 ch).
     Trimmed the two oversized descriptions too. Net: -12.7 KB always-loaded text.
       paper-probe        desc 936→551 · summary 5,986→307
       application-probe  desc 1,000→584 · summary 6,720→346

[2026-07-15 00:11 · SHIPPED] one-sentence-per-line saved as a rule
  > JL: another important thing — one sentence one line, no break lines.
  >> CC: saved to PREFERENCES.md (mirrors haipipe-task). Held all NEW prose to it.

[2026-07-15 00:40 · SHIPPED] haipipe-probe/SKILL.md — COMPLETE REWRITE to an operational skill
  > JL: I feel this is not the real skill, what there are so many R1, R2, .... It is not
        the real skill file. Please think about how to rewrite it completely.
  >> CC: right — it was a RULING LEDGER (R1-R21, PART 0-9, "where the closure could be
     wrong"), not a skill. The rulings + rationale already live in the spec of record,
     diagram/260714-probe-qa/. Rewrote the SKILL as an OPERATIONAL file (JL chose full
     rewrite): what a probe is · probe-file anatomy · the five-step loop + cost ladder ·
     the QA state-line contract · the two LAWS · derived state + checker codes · verbs ·
     pointers. 517 → 228 lines. 0 R-numbers, one sentence per line, consumer-level
     throughout, no claims/verdict machinery. Repointed 5 dangling "PART n" refs
     (fn/probes ×2, check-probe-cards ×2, _archive README) to durable section names.

  The FOUR inline JL comments the rewrite subsumed (moved here verbatim):
  > JL: not just paper, can also be application, so it will be Consumer level document.
    >> CC [SOLVED]: the rewrite says "consumer" (paper OR application) throughout.
  > JL: is this rule worth to put here? If dead, remove them, don't need to put them here.
    >> CC [SOLVED]: R7 (the Verdict ruling) is gone; every R-number was dropped.
  > JL: I think probe should have the ability to collect the Q-paper from draft.md, and then
        make group them, and then make them into the Q-task, and then send to the executor
        side, do we have it?
    >> CC [SOLVED]: YES — that IS the five-step loop. ① ORGANIZE collects the DRAFT's
       questions and groups them by topic; the `commission` is the "Q-task" (the question in
       general language); ③ DISPATCH sends it to the executor orchestrator. It runs in the
       PROBE-phase worker (haipipe-paper-probe / haipipe-application-probe), and the rewrite
       states it plainly under "The five-step loop".
  > JL: delete it. Not even mention it. So in probes, there are not claims. Just the
        comminucations.
    >> CC [SOLVED]: the rewrite never mentions the claim ledger / verdict / judgment; it
       states the boundary positively — "a probe is communication, not judgment".


[2026-07-15 01:05 · SHIPPED] the two WORKERS rewritten to CALL the constitution
  > JL: yes, please do it. So they are more like to call the haipipe-probe, right?
        Please try to remove the duplicates.
  >> CC: right — the workers were ~50% duplicated constitution (a full copy of PART 2
     the QA file, PART 3 the qa verb, PART 5 the state table, plus the cost-ladder and
     state-line tables and the two LAWS). Rewrote both to POINT at the constitution for
     the model and carry ONLY the worker-side deltas + the operational runbook:
       haipipe-paper-probe        702 → 156 L  (paper deltas: paper_root · the RESOURCE
                                  intake + write-back · 3 harvest lanes as SUB-SKILLS)
       haipipe-application-probe  670 → 153 L  (app deltas: intervention_root · no
                                  resource stage · venue-scaled harvest HOOKS)
     Both keep: the five-step loop as the worker runs it, the PROOF-per-step discipline,
     project_root resolution, the T1 local whitelist, the dispatch spelling, the async
     path, the checker invocation. 0 R-numbers; each opens with "⭐ THE MODEL IS NOT THIS
     FILE'S — it is the constitution's". −1063 lines across the two.


[2026-07-15 00:50 · SHIPPED] 3 JL comments on the rewritten constitution
  > JL: what does the "commission" mean? I don't understand it, could we have some
        simple version?
    >> CC [SOLVED]: added a plain-language block to "What a probe is" — YOUR question
       carries the stake ("C6 dies if…"); the executor must not see it; so the probe
       writes a COMMISSION = the same question in general language, stake stripped =
       the executor-facing question, the only thing that crosses to the bank.
  > JL: where is the part for convert the Q-consumer to Q-executor? and the probe should
        scan the existing ones, try not to create a new Q-executor if one is already in
        the BANK. Have you mentioned this?
    >> CC [SOLVED]: made both explicit in the five-step loop. ① ORGANIZE now says "write
       each question's COMMISSION — translate it to the executor-facing form" (the
       conversion); ② MATCH now says "SCAN the bank FIRST; if one already answers it,
       REUSE it (skip ③); only an unanswered question creates new work" — ② always
       precedes ③.
  > JL: [harvest lanes] probe just needs the PROBE file ready + the QA complete, and
        later revise harvests? I don't understand it. After probes are done, revise
        will help this.
    >> CC [SOLVED for the constitution; DESIGN Q flagged]: you're right the probe's core
       is "probe file ready + QA files complete". Harvest (pulling a value/citation/figure
       OUT of the answer into _VALUES_/.bib/0-displays) is NOT the model — removed the
       HARVEST-LANES paragraph from the constitution; it lives in the WORKERS (their ⑤).
       WHERE it should run — the probe worker's ⑤ (today) vs the REVISE phase (your
       instinct) — is a real lifecycle question I did NOT decide. Flagged below.


[2026-07-15 02:15 · SHIPPED] VOCABULARY: commission→q-executor, reading→a-consumer, the 2×2
  > JL: commission is confusing — rename to something like Q-executor. Do we have
        Q-consumer, A-consumer, A-executor as well?
  >> CC: YES — there is a full 2×2, now documented in the constitution ("THE FOUR FORMS"):
       QUESTION  Q-consumer (## Why + ## Q)  ──T1──▶  Q-executor  = `q-executor:` field
       ANSWER    A-consumer = `a-consumer:` field  ◀─T2──  A-executor = the QA file's ## Answer
     The probe file holds only the two BRIDGE ends; the far corners live in ## Why / the QA
     Answer. T1 = ① ORGANIZE, T2 = ⑤ INTERPRET.
     JL chose to rename BOTH fields (keep the `commissioned` STATE + the verb "to commission"):
       commission: → q-executor:   (the noun/field; verb & state kept)   ~35 field sites
       reading:    → a-consumer:   (its mirror)                          ~69 field sites
     Renamed across ~57 files: fields, field-lists (serves/target/state/q-executor/a-consumer),
     backtick noun-refs, both checkers (grep + FAIL codes: LAW2-q-executor-leak,
     read-with-empty-a-consumer), descriptions + trigger keywords. KEPT: "commissioned"
     (state), "commissions/commissioning" (verb), "reading" (verb "reading the file").
     ⚠️ BUG THE FIXTURE CAUGHT: the new field names have a HYPHEN; the checker's field-reset
     alternation was `(serves|target|state|commission|reading|…):` — after rename the hyphen
     broke the match, so `- q-executor:` never reset the block scalar and a valid `read`
     section FAILed (read-with-empty-a-consumer). Fixed the alternation → q-executor|a-consumer.
     VERIFIED: original checker PASSes the fixture, my first cut FAILed, post-fix PASSes again.
     This is the FIRST real end-to-end exercise of the checker on the new field shape.


[2026-07-15 02:05 · SHIPPED] a probe AGENT — question-level, stake-free, shared
  > JL: where is the agent? we want an agent for it. But the probe will only handle
        the question level, right? Q-executor.
  >> CC: right. Created haipipe-probe-worker-agent — the probe layer's ONE live agent
     (gateway + judge stay retired). It is the SCOPING JL nailed: because a q-executor
     has the stake ALREADY stripped, an agent at the q-executor level never sees the
     ## Why, so it is a CLEAN-CONTEXT agent (like the executor orchestrators) and can be
     SHARED across paper + application. It runs the stake-free middle of the loop —
     ② MATCH (bank + dedup) → ③ DISPATCH unmatched to the task/discovery orchestrators →
     ④ POINT — in an isolated context, and returns {q-executor → answered QA path}. The
     stake-AWARE halves stay with the stage: ① writing the q-executor (T1) and ⑤ harvest
     (T2). NOT the retired gateway (that was a 1:1 hop; this MATCHes the whole batch first,
     so an answered question is REUSED not re-run). Registered (~/.claude/agents symlink);
     rostered in agents/README + the constitution Pointers block.


⏳ STILL OWED
=============

```text
  1. The audit's other defects (03): the dated spec path cited by ~86 files
     (rename → diagram/probe/), the R21↔R18 + version drift (the constitution is now
     9.0.0, which resets that particular drift).
  2. Q1 (02): fold the claim_type overclaim reminder deeper into the claims skills,
     or leave it as the one line added this session. JL to decide.
  3. Optional: the two workers are near-identical twins — the shared runbook could move
     to one ref both cite. Not done (they live in separate consumer trees); flag only.
  3b. VOCAB residual: ~37 bare-noun "commission" refs remain in PERIPHERAL files (claims,
     orchestrator-agent descriptions, wikis), MIXED with the legit verb "to commission"
     ("commission the work") and the executor-side "a commission" (the order it receives).
     A blanket sweep would corrupt the verbs, so these need per-occurrence review. The
     PROBE-LAYER core (fields, both checkers, constitution, workers, descriptions) is fully
     q-executor/a-consumer and checker-verified; the residual is cosmetic drift in prose.
  4. WIRE THE PROBE AGENT IN: haipipe-probe-worker-agent EXISTS + is registered, but nothing
     dispatches it yet — the two worker skills still run ②③④ inline. To USE it, their ②③④
     steps delegate to Agent(haipipe-probe-worker-agent) (the stage keeps ① + ⑤). Mechanical
     follow-up across the 2 workers. JL to say go.
  (nothing structural owed on harvest — resolved below.)
```


✅ SHIPPED (harvest, cont.) — 2026-07-15
========================================

[2026-07-15 01:20 · SHIPPED] harvest placement settled + the boundary made explicit
  Q chain: JL first thought harvest belonged in REVISE, then refined to "haipipe-probe
  collects, haipipe-paper-probe harvests". CC read 2-phase/ and grounded it:
  · harvest STAYS at PROBE — the evidence stages claims + resource + seed SKIP REVISE
    (haipipe-paper-revise/SKILL.md:124), yet claims files every number backing every
    claim into _VALUES_; so harvest must ride with PROBE, the universal intake phase.
  · the clean split is WITHIN the ONE worker run, not two skills: ①–④ COLLECT the answer
    (the shared mechanism the haipipe-probe CONSTITUTION governs) · ⑤ HARVEST (the
    consumer's OWN, not in the constitution). haipipe-probe is the RULEBOOK, not a
    runtime step; haipipe-paper-probe executes collection AND harvest.
  · JL: "yes, make it explicit." DONE — both workers now carry, in the five-step loop:
      a "TWO HALVES" framing line under STEP 0, and a
      "════ COLLECTION (①–④) ends here — the answer is banked. HARVEST (⑤) begins ════"
      divider before ⑤, whose heading now reads "…and HARVEST (the <consumer>'s own,
      not the constitution's)".
