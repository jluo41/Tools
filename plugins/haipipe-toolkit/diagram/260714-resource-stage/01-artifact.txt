The artifact — two sections, and the wire
=========================================
Part of: diagram/260714-resource-stage/  ·  6 files, one theme each, no index.
SPEC OF RECORD — the resource stage. C1-C9 closed JL 2026-07-14. SHIPPED.
Format: JL comments are `> JL:` · CC replies are `>> CC{MMDD}:` + a diagram.


THE STAGE IN ONE LINE
=====================

  seed (0) → 🧱 RESOURCE (1) → claims (1) → [venue] → pitch → narrative → display

  ❓ the ONE question:  "What must EXIST for this paper to be testable,
                         does it exist, and can it CARRY the claim?"

  venue-FREE · shares its number with claims (as 2-venue / 2-pitch already do)
  skill: haipipe-paper-resource   ·   artifact: 0-lifecycle/1-resource/1-resource.md


① THE ARTIFACT
===============

  📄 0-lifecycle/1-resource/1-resource.md   +   _LOG_1-resource.md

     ┌──────────────────────────────────────────────────────────────────────┐
     │ ## Demand      what we MIGHT NEED   (one N<n> per H, from the seed)  │
     │ ## Questions   what we need to KNOW (one Q<n> — and its A when it    │
     │                                      lands)                          │
     └──────────────────────────────────────────────────────────────────────┘

        "what we might NEED"    →  📋 Demand
        "what we can ASK"       →  ❓ a Q<n>
        "what we already HAVE"  →  ✅ its A

  🔑 "do we have it?" AND "does it WORK?" are BOTH the ANSWER.
     → no ledger · no existence axis · no fitness axis · no binding table.

  ✂️  CUT (JL, 07-14): 💀 Kill Conditions · 🔒 Setup Contract · 📦 Resource Ledger ·
                       🔗 Binding table · ALL sidecars.
     > JL: "Make it as concise as possible." / "just keep Demand, and then Probe and
       Probes with QAs. No others."

  🔑 DEMAND IS KEYED ON H<n>, NOT C<n>.
     Resource runs BEFORE claims. C-ids do not exist yet. A Demand row that cites a
     claim id is citing something unwritten — that is the tell that the row belongs
     in claims, not here.


② THE Q-NOT-PP RULING  (JL, 07-14)
==================================

  > JL: "we should not design the probes, but design the paper-questions. And then,
    in the probe stages, the probe stages will pick them up, and think what type of
    probe-topic should these questions be assigned and answered."
  > JL: "So here PP02 etc, they are more like the Q1-xx, Q2-xxx. In the draft, I will
    determine whether we want to ask these questions."

  🔑 THE STAGE ASKS. THE PROBE LAYER ROUTES.


③ THE WIRE — end to end
========================

```text
   🧑 SEED            H1 · H2 · H3                        the claim shape
                            │
   🧱 RESOURCE        ## Demand    N1..N4   (one N per H)
      DRAFT           ## Questions Q1..Q5   ← the stage ASKS. It mints NO PP id.
                            │
                      ⛔ GATE 1   👈 the human picks WHICH Q's are worth asking
                            │        (a declined Q is LOGGED, never deleted)
                            ▼
   🛰️  PROBE WORKER    opens ONE question SECTION per approved Q
                      in 1-probes/PPNN_<topic>.md
                      …and writes the `-> PP<NN>` BACKLINK into 1-resource.md
                            │
                      ② MATCH   ── names the `cross-project:` reuse candidate
                            │
                      ⛔ GATE 1b  💰 the SPEND is authorized INFORMED, not blind
                            │
                      ③ DISPATCH ──▶ 🤖 Agent(haipipe-task-orchestrator-agent)
                                      🤖 Agent(haipipe-discovery-orchestrator-agent)
                                      💀 no gateway.  🧱 clean context = the wall
                            │
                      📄 tasks/<leaf>/QA/<n>-<slug>.md    ← the EXECUTOR authors it
                            │
                      ⑤ INTERPRET ──▶ writes the A back into Q<n>
```

  TWO WRITERS on 1-resource.md — same consumer, no wall crossed:

```text
   🧱 the RESOURCE stage (DRAFT)   writes  ## Demand N<n>  ·  ## Questions Q<n>
   🛰️  the PROBE WORKER             writes  the `-> PP<NN>` backlink  ·  the `A:`
   ⇒ the stage still never MINTS a PP. The FILE carries one, put there by the probe
     worker at the moment it opens the question's section. A backlink, not a mint.
```


④ THE NAMESPACE WORRY IS DEAD
==============================

  >> CC0714: and for a BETTER reason than the one we had.

  The original note said: "the stage cannot collide with a sibling paper's PP ids,
  because it no longer mints any. The gateway owns the namespace."
  Both halves are now WRONG, and the conclusion is now STRONGER:

```text
   ❌ WHAT WE THOUGHT               ✅ WHAT probe v8.1 RULED (R1: BINDING BY PATH)
   ─────────────────────────        ──────────────────────────────────────────────
   PP ids are a shared              PP numbers are PAPER-LOCAL FOOTNOTE NUMBERS.
   namespace, so someone            Two papers may both carry a PP04, the way two
   must own it → the gateway.       books both have a footnote 4.

   the stage must not mint,         there IS NO NAMESPACE. No ledger. Nothing to
   or it will collide.              renumber. No PP id ever crosses to the bank —
                                    a section binds by `target: <path to QA file>`.

   ⇒ the RULING survives untouched (the stage asks, the probe layer routes).
     Its JUSTIFICATION is obsolete: collision was never possible to begin with.
     💀 there is no gateway. It was RETIRED. Dispatch goes DIRECT.
```

  ⇒ this stage is a CONSUMER of diagram/260714-probe-qa/ (the probe spec of record).
    It redefines nothing there. See 06-shipped-owed.txt for the full reconciliation.
