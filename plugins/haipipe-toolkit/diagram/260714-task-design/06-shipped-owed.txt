Shipped and owed — the reconciliation
=====================================
Part of: diagram/260714-task-design/  ·  6 files, one theme each, no index.
STATUS: 🟡 DRAFT   ·   flip to → SPEC OF RECORD — R1-Rn closed JL <date>. SHIPPED.
Theme: what is ON DISK vs what is still OWED. 🔒 MANDATORY — the honesty ledger.


  🔒 ✅ = VERIFIED ON DISK. 🟠 = NOT DONE (a design is not a ship). A later
     session flips 🟠 → ✅ here IN PLACE.


✅ SHIPPED — this session
=========================

  Only this topic note. NO task-skill file was changed — this was review + analysis.

  📁 diagram/260714-task-design/   6 files (01-the-thing … 06-shipped-owed)
     ⬅ fills a folder that was scaffolded EMPTY at 19:38 and never populated.


✅ SHIPPED — 2026-07-14 (session 2 · the rename)
================================================

  JL ruled R2 closed → the three fn files were RENAMED on disk (git mv, history kept):
     ✅ fn/workflow-plan.md   → fn/stage-plan.md
     ✅ fn/workflow-report.md → fn/stage-report.md
     ✅ fn/workflow-audit.md  → fn/audit.md
  ✅ live references repointed: SKILL.md (dispatch table + audit cmd), DESIGN.md,
     USAGE.md, ref/task-structure.md, agents/…orchestrator-agent.md (body),
     8_stata/…/fn/audit-stata.md, and the fn files' own titles + cross-refs.
  ✅ this diagram folder swept to the new names (01·02·03·04·06); R7 logged in 05.
  🟠 NOT touched (historical, by design): CHANGELOG.md files, SKILLSET_REVIEW.md,
     the orchestrator-agent version-history line, and `workflow-plan-sample.yaml`
     (a DIFFERENT file — the type-specialist IPO template, not renamed).


🟠 STILL OWED — the review findings (nothing below is fixed)
============================================================

  H1 · stage-report.md evidence table   🔴 DIRECTLY about these 4 files.
     fn/stage-report.md:49-56 tells the reporter to gather from
     results/<run>/manifest.json + config_snapshot.yaml + log/*.txt, and OMITS
     metrics.json + runtime.yaml. But run-sh-template.sh writes runtime.yaml,
     metrics-json-schema.md calls metrics.json "the file every later reader
     resolves a number out of", and authoring-conventions.md §1 + task-structure.md
     agree the contract is metrics.json + runtime.yaml. The reporter scans the
     WRONG files. FIX: rewrite the Step-2 table to lead with metrics.json +
     runtime.yaml; move manifest/config_snapshot under a stata-only note.
     ⚠️  this is the concrete face of R5 (prose drift). Fix it and cite R5.

  ── adjacent task-skill review findings (raised same session, full detail in the
     review turn; not workflow-specific, logged here so the task-design folder
     carries them) ─────────────────────────────────────────────────────────────

  M1 · ref/hierarchy.md:36,86,251   the Step-0 "read first" doc says the group
       letter "signals the dominant task-type" + regresses to "(A/B/C/D)",
       undercutting SKILL.md's ⛔ "NEVER infer type from the letter". Prior H7
       claimed fixed; the leading sentence + residue survived.

  M2 · ref/authoring-conventions.md:9,85 + ref/invocation-modes.md   name retired
       agents (code-creator-for-<type>-agent, haipipe-task-builder-agent); real set
       is the triad creator/reviewer/orchestrator (grep-confirmed no such files).

  M3 · SKILL.md:130 + CHANGELOG 6.1.0   cite "fn/qa.md (Step 3a/3b)" — no such
       labels in qa.md; SKILL's own Step 3a is the unrelated type cascade.

  M4 · SKILL.md:212   "three defects" — the checker (check-probe-cards.sh) has FOUR
       task-side qa codes; qa-no-state is undocumented in SKILL/fn though 6.2.0
       says it was added.

  L1 · TTL constant named two ways: spec + checker use QA_WORKING_TTL_HOURS; all 5
       ecosystem CHANGELOGs say QA_CLAIM_TTL_HOURS. Checker+spec win.
  L2 · QA "constitution" lives in probe/haipipe-probe/SKILL.md (a CONSUMER layer)
       while task is "consumer-unaware" + restates it inline — dual source.
  L3 · check-probe-cards.sh lives ONLY in paper/ + application/ (duped), not task/;
       SKILL.md:212 calls it "the checker" as if task owned it.
  L4 · 4 path-root spellings for the same tree in SKILL.md + fn/ (Tools/plugins/
       ×2, skills/task/ ×9, bare task/ ×2, probe/ ×1).
  L5 · nits: scan-status.md:6 "Three artefacts" lists two; CHANGELOG 6.0.2 cites
       diagram/01-architecture.txt (no such file in the task tree).

  R1 · R3   →  05-rulings.txt. OPEN, wait on JL. Not code.
  R2        →  CLOSED 2026-07-14 by R7 (the rename above).

  COMMIT STATE: the task skill files are UNCHANGED by this session (clean). The
  only new files are this note's 6 .txt under diagram/260714-task-design/.


🤝 RECONCILIATION WITH the concurrent v6.x QA landing
=====================================================

```text
   📌 the ground under this topic MOVED today. The v6.x consumer-unaware / QA-file
      machinery landed 2026-07-14 and swept SKILL.md + fn/qa.md, but left the
      SPOKE files (ref/hierarchy.md, ref/authoring-conventions.md,
      ref/invocation-modes.md, fn/stage-report.md) behind. Every owed finding
      above is a spoke the QA landing did not sweep — the same "搬家没改地址"
      (moved house, didn't update the address) root cause the 2026-07-04
      SKILLSET_REVIEW named. It recurred.

   ✅ SURVIVED: the schema single-sourcing (R4), the shared IPO vocabulary.
   🟠 NOT SWEPT: the four spoke files above.
```
