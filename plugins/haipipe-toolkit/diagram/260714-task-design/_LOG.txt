Comment log — JL review annotations on the task skill, moved out of the body
=============================================================================
Part of: diagram/260714-task-design/  ·  the comment ledger for the SKILL.md review.

How it works: JL leaves `> JL:` comments in a doc body; CC answers inline with
`>> CC HH:MM:`; the moment the body edit is APPLIED, the whole thread is moved
HERE — the `> JL:` line VERBATIM (never paraphrased), stamped, tagged SOLVED, with
a one-line note of what changed. A PENDING thread stays inline in the doc until JL
decides, then moves here too. Newest block at the bottom.


⏳ OPEN — still inline in SKILL.md, waiting on JL
=================================================

  NOTHING OPEN — all three were approved by JL and applied 2026-07-14; the threads
  moved to the MOVED section below. (The only OPEN items left are RULINGS R1 + R3,
  tracked in 05-rulings.txt — those are rulings, not inline comments.)


✅ MOVED — 2026-07-14, edit applied in SKILL.md
===============================================

[2026-07-14 21:50 · SOLVED] type-table — group-letter NOTE
  > JL: 这个没有必要说太多吧，感觉不是很重要，有必要说这么多吗？
  >> CC: Rule is load-bearing (never infer type from letter), but the defaults list
     duplicates ref/hierarchy.md — trimmed to the rule + a pointer.

[2026-07-14 21:50 · SOLVED] Stata specialist block
  > JL: How to make this to be concise? Don't need to mention too much here.
  >> CC: Cut to one line — only "detect Stata → hand off WHOLESALE" is load-bearing;
     the {LNN} alphabet + dialect are the specialist's.

[2026-07-14 21:50 · SOLVED] process — reply-and-mark protocol
  > JL: 以后每一次，比如说我的问题解决了之后，你都要在我的这个上面回复啊，一定要回复。 然后 mark 一下，说这个是 solved，要不然还以为我们下一次以为是没有 solved
  >> CC: Adopted — reply under every `> JL:` with `>> CC HH:MM:` + [SOLVED]/[PENDING],
     never delete the JL line. Saved to PREFERENCES.md (survives sessions).

[2026-07-14 21:50 · SOLVED] routing-principle — migration breadcrumb
  > JL: 这句话啥意思？感觉很突兀，要不然删掉吧
  >> CC: Agreed — dated 2026-07-03 migration note, CHANGELOG material. Sentence deleted
     from the body; the fact stays in CHANGELOG.

[2026-07-14 21:50 · SOLVED] authoring convention — one sentence per line
  > JL: 在正文中，我们做到每一句话是一行.
  >> CC: Noted as standing convention (body already mostly follows it). Saved to
     PREFERENCES.md; I hold new prose to it and fix stragglers on touch.

[2026-07-14 21:50 · SOLVED] Stage-4 — QA "the ONE exception" sentence
  > JL: 这句话不知道啥意思，我感觉比较奇怪。那 QA 跟这四个 stage 什么关系呢？
  >> CC: Rewrote it. QA is NOT a 5th stage — it rides ON TOP: this layer writes the QA
     file a `working` CLAIM at entry (before Plan) and the answer at Report. Dropped a
     dangling "Step 3a" ref.

[2026-07-14 21:50 · SOLVED] Stage-4 — "a task ends at Report" line
  > JL: 这里的话是不是有点老旧了？其实我们就是从 QA 来做 consume，对吧？我觉得这个 QA 是来 consume 这个 result 的？
  >> CC: Right, it was stale. Fixed: the qa verb consumes results/ → writes QA/<n>.md;
     a consumer reads THAT digest, never results/ directly (probe LAW 1).

[2026-07-14 21:50 · SOLVED] qa-verb — QA readability
  > JL: 然后还有一个问题：一定要把 Q&A 写的 readable，就是要让 human 容易去读它，不要写得非常难读
  >> CC: Affirmed as a hard requirement — QA/<n>.md is the HUMAN answer: plain prose +
     [→ results/…] anchors, no YAML/jargon. Enforced in fn/qa.md (reviewer lints it).

[2026-07-14 22:05 · SOLVED] fn/ docs — one sentence per line
  > JL: fn 里面的 skills 都换成一行、一句话的，不要有 line break。
  >> CC 22:10: Done — 5 files (qa, run, workflow-audit/plan/report) were ALREADY
     one-per-line (your pass); CC reflowed the remaining 4 (feedback, digest,
     scan-status, task-group). Caught + fixed a bug on the way: the first pass
     mangled feedback.md's YAML frontmatter (the token check passed but structure
     broke) — added frontmatter protection, re-ran. Verified per file: token stream
     identical to HEAD, code fences balanced, ASCII/frontmatter intact.

[2026-07-14 22:20 · SOLVED] type-specialist docs — one sentence per line (sweep)
  > JL: 1_data..9_agent 这些 folder 里面是不是也有同样的问题?都统一一下吧。
  >> CC 22:30: Done, with the REAL numbers (the "176" above was optimistic): SCANNED
     177 .md across the 9 folders, REFLOWED the 35 that had wrapped prose, 142 were
     ALREADY one-per-line, 0 skipped. Same script + safety gates as the fn/ pass
     (frontmatter-protected; per-file `original.split()==new.split()` + fence-count
     preserved). Independently re-verified all 35 token-identical to HEAD (0 drift);
     specialist SKILL.md frontmatter confirmed intact. Code/YAML/trees/`> JL:` untouched.

[2026-07-14 21:50 · SOLVED] frontmatter — description too long (R6)
  > JL: 我感觉这个 meta description 还是太长了 … 要不要定个标准？
  >> CC 22:30: JL approved the budget. Rewrote the description to EXACTLY 55 words
     (was ~110) — one purpose sentence + trigger keywords, mechanics dropped to the
     body. R6 CLOSED. (Standard for other skills: ≤ ~55 words.)

[2026-07-14 21:50 · SOLVED] "The QA/ folder" section — too complex
  > JL: 我觉得这里写 QA 是不是太多了？… 下面这些东西都可以去删掉了吧？
  >> CC 22:30: JL approved. Collapsed ~24 lines → 5: kept the load-bearing guardrails
     inline (ONE WRITER = this layer; a consumer NEVER writes; readable + no consumer
     vocabulary); pushed the template, TTL, race guard, supersession, checker codes to
     fn/qa.md. Removed the stale "three defects"/QA_WORKING_TTL_HOURS restatement.

[2026-07-14 21:50 · SOLVED] "Step-by-Step Protocol" — routing block too long
  > JL: 这一块是不是都要简写一下？（担心太短会丢信息）
  >> CC 22:30: JL approved, cautiously. Deduped CONSERVATIVELY: shortened only the
     `--check-only` paragraph (its probe-MATCH rationale lives in fn/qa.md) — ~2 lines.
     KEPT the numbered (0)–(7) cascade and the structure-not-name block with its
     examples + 31% stat INTACT (that's the info you worried about losing). A 20-line
     cut would have removed real routing info, so I stopped short — as you flagged.
