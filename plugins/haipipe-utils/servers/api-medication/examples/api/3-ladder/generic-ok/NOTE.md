# a name match is OK, never GOOD

service: describe-medication
HTTP 200

Tier B, 69.9% of rows. Two products can share a generic name and differ in salt, strength and route, so a name match answers 'which drug' and not 'which product'.
