# 255 is uint8 overflow, not a dose

service: describe-medication
HTTP 200

6,039 rows carry Dose 255 and the 99th percentile of the whole column IS 255. Typed as a sentinel: the value is withheld and MedConf is MISS.
