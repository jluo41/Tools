# three drugs in one string

service: describe-medication
HTTP 200

736 Shanghai rows carry more than one drug -- the same shape as describe-food's item_list. The record reports the first component; the dialect layer keeps all three.
