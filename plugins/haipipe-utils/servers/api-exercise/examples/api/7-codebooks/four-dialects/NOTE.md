# one activity, four dialects, one MET

HTTP 200

A patient typed 'Walk'. Apple called it 52. Validic called it 1001. The WellDoc app called it 100. All four reach compendium 17190 and MET 3.8 through ONE curated pick -- and each keeps its own TypeSource, because a name a patient typed is not the same evidence as a name read off an enum.
