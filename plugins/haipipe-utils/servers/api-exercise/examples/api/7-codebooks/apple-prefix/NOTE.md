# Apple codes are '20' + HKWorkoutActivityType

HTTP 200

EntrySourceID 20 is AppleHealthKit, and its codes carry the source id as a prefix: 20052 is HK 52, walking. Of the 54 distinct ns20 codes in 1-SourceStore, 47 strip to a valid HK case and the 7 that do not are exactly the private 209xx band.
