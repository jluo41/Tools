# the same digits from another vendor

HTTP 200

Identical strings, different verdicts. '20903' is a roll-up because EntrySourceID 20 issued it; the same digits from source 37 are a different vendor's code and do not inherit that.
