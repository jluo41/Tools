# Shanghai writes the unit in the string

service: describe-medication
HTTP 200

The log states grams; the pipeline stores milligrams. 'metformin 0.5 g' and 'metformin 500 mg' are one prescription and must not sort into two buckets three orders of magnitude apart.
