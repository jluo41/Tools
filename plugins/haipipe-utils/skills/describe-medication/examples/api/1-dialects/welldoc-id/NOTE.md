# a bare WellDoc MedicationID

service: describe-medication
HTTP 200

386,373 rows look like this: an integer and a number, no drug name anywhere in the row. The name lives in a pooled lexicon built from every cohort's ImportedMedication export.
