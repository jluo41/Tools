# a DIA measured on THIS patient

service: describe-insulin
HTTP 200

The only way to reach GOOD. WellDoc's MedPrescription carries a measured duration of insulin action for 15.7% of prescriptions. Note OnsetMin is unchanged -- only the duration was measured, and PKSource keeps both halves visible.
