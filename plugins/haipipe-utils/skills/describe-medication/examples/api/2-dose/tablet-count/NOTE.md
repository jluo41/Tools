# 1 means one tablet, not one milligram

service: describe-medication
HTTP 200

DoseUnit count. Median dose for non-insulin rows is exactly 1, because the app asks how many were taken. Reading that as milligrams would be wrong by three orders of magnitude.
