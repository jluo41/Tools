# Validic 25 decodes to 'Other'

HTTP 200

3,308 rows. The code book resolves it perfectly -- to a word that names no activity. Decoding is not naming, so it is a placeholder, and TypeConf still records that a book was consulted.
