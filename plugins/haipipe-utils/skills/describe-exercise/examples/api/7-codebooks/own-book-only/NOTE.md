# a code is read only in its own book

HTTP 200

Same digits, different issuer. Validic's 1001 is Walking; Apple has no 1001, so that row stays opaque rather than borrowing a neighbour's meaning. This is the church-supper defect generalised.
