[project]
name = "diagram-excalidraw-render"
version = "0.1.0"
description = "Render Excalidraw JSON to PNG via Playwright + locally vendored Excalidraw bundle"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
]
