---
status: confirmed
created: 2026-06-22
context: general (closing summary after the L01 split on P.0622)
fixed_in: ""
---

Reporter (JL): 我很喜欢这个总结，让人一眼看到我们做了什么，不错。
(Praise, not a complaint.) Referring to the return-contract tail at the end of
the turn: a fenced block with `status: / summary: / artifacts: / next:`.

This CONFIRMS the skill's existing "Return Contract" section is doing real work:
JL specifically values that it shows, at a glance, what was done and what is
next. Note the contrast within the same session: JL twice flagged dense,
multi-table probe output as "很难读 / 根本不知道干啥"; the compact tail is what
landed well.

Keep: every substantive probe turn should end with the status/summary/artifacts/
next tail, and bodies should stay scannable (lead with the at-a-glance recap,
avoid stacked walls of tables). A revision pass should strengthen, not weaken,
the Return Contract — consider making the tail mandatory in fn/console.md output,
not just listed in SKILL.md.
