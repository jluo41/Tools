# Run plan and inventory

This is a plan for the described scenario, not an audit of a supplied project directory. No worker was executed and no native Ticket or receipt was allocated. All `<...>` paths and `NN` counters below are unresolved placeholders; existing addresses must be read from the actual owners before execution.

Count one owner-qualified logical commission per Run. The described existing scope contains **five logical Runs**: one failed Task fit, three Design Runs, and one Insight RI binding. If the first Page's value evidence and paragraph session are new independent commissions, they add **two planned Runs**, giving **seven logical Runs after valid allocation**. They do not enter actual allocated inventory while merely planned. This is a count of the work described, not a complete project total. An actual audit may reveal earlier Design revisions, required Page Structure Runs, missing receipts, or other work.

| Record in scope | Existing logical Runs described | Proposed new allocations | History and counting |
|---|---:|---:|---|
| Task model fit | 1 | 0 | Failed attempt 1 is preserved; the same-contract retry becomes attempt 2 in this Run. |
| Fit reused by two Pages | 0 additional | 0 | Two consumer bindings to the same producer identity and accepted Result/hash. |
| First Page's VALUE Evidence Item | 0 stated | 1 | One local RE transformation/binding commission over the supporting fit; any Task-native execution alias denotes this same RE execution, not another Run. |
| First Page's paragraph writing session | 0 stated | 1 | One interactive RP; feedback cycles are Steps, and same-target reopenings are Versions. |
| Design Commission, Generate, Verify | 3 | 0 | Three independently closable recorded commissions. Verify must support the displayed candidate. |
| Design Delivery | 0 additional | 0 | Read-only projection of the exact Generate and passing independent Verify. |
| Insight RI with v001 and v002 | 1 | 0 | One immutable binding, two version-qualified publication/execution records. |
| **Described scope** | **5** | **2** | **7 only after the two proposed allocations exist and validate.** |

A Workflow runtime, gate, acceptance click, model call, retry, Result file, label, Card, Page consumer, or Delivery projection adds no logical Run. The RI history view reports two executions separately; v002 is not a second RI allocation. A referenced base R recipe is not counted as an executed Run unless independently commissioned production has its own receipt.

## Work plan, owners, storage, and close rules

1. **Resolve the current Task fit, then prepare its retry.** The Task owner (`haipipe-task`) resolves the existing full Task/BJTR address, selected Spec, frozen data/config, worker, and declared acceptance gate. Compare the Ticket/config/data hashes with the failed attempt before dispatch. Preserve the prior receipt, failure, logs and relevant artifacts through an owner-permitted attempt/history mechanism. The same intent, target, frozen inputs and acceptance retain the Run identity; changing any materially requires a new Run. Do not call the generic `training.fit` vocabulary an installed runner: resolve the actual worker from this Ticket.

   Authored Ticket: `<job>/<task>/runs/<existing-run>.sh`; config: `<task>/scripts/config/<existing-run>.yaml`. Generated Result and receipt: `$OUTPUT_ROOT/<task>/results/<existing-run>/` and its `runtime.yaml`. Output root follows the actual Ticket resolver: `RESULT_STORE`, then the Job's `store:`, then the Job itself, including the declared Job mirror mapping when a store is used. Preserve historical paths through their recorded resolver.

   The Ticket owns launch and terminal receipt updates; the worker owns its declared artifacts. An exit code alone cannot close the fit. Require the Ticket's declared artifacts and semantic gate, such as loading the fitted model and validating required fit metrics. A second failure remains a truthful failed Run and holds dependent evidence work. The standard Ticket template currently lacks attempt preservation; see the dispatch limitation below.

2. **Bind the accepted fit to both Pages.** After the fit's gate passes, each consumer Page owns its own evidence admission and applicability decision. Carry the producer's full identity, exact Result path and fingerprint, with any applicable execution version. Do not rerun the fit for the second Page, rename it, or copy/symlink its Result to imitate Page-local storage. Supporting Run appearances on both Pages remain one inventory identity.

   Bindings and evidence plans belong in each Page's `outline/<stem>-evidence-items.md`, with exact producer references in the relevant frozen Local Inputs and typed Results. Existing Page bindings are checked for staleness after the retry. The second Page's specific evidence obligation was not supplied: record its reuse dependency now; only commission an additional local RE when its independently accepted Page evidence contract is defined. Do not assume the first Page's future local Result is the second Page's input.

3. **Make the first Page's VALUE item ready through one RE.** The Page SHAPE/SURVEY owner defines one stable typed item, target Bullet, Expected payload, Acceptance, supporting fit reference, Local Input plan and authorized make decision. Planning does not allocate a Run. LAND, owned by Page EVIDENCE and the selected native Folder executor, validates the supporting fit, freezes one Local Input, checks for an existing compatible RE, and otherwise allocates the next `re-value-NN_<slug>`.

   For a Page-local dialect, the readable Ticket is `<page>/runs/re-value-NN_<slug>.md` and its governed Result is `<page>/results/re-value-NN_<slug>/`. A Task-backed Page instead uses the Task's real executable `rNN` Ticket and resolved external Result store, with the RE lineage bound to the native execution. Those are aliases for the same local evidence commission. The existing fit is a distinct supporting commission.

   The local Result contains `input.yaml` (or the declared frozen input packet), typed `result.yaml`, `runtime.yaml`, full supporting identities/hashes, provenance and observable acceptance checks. Require the requested value/coherent set, unit, population/denominator, model/method label and expected uncertainty. LAND admits the Result; EMBED writes the Page-specific interpretation into the next outline version. VALUE labels and the Evidence Card are projections, not new Runs. Do not create a separate `outline/evidence/value/` store.

4. **Run one interactive paragraph writing commission.** The Page workflow owns the session, human feedback and acceptance; the selected writing worker operates within that scope. This is `Page.interactive-writing`, scoped to one paragraph or jointly judged contiguous group, rather than a delegated paragraph worker commission. First resolve the closed `rp-struct-01`, frozen Page-global `Pxx` index, and exact Mermaid paragraph description. If absent, hold this allocation and plan the necessary Structure commission separately; its existence was not supplied and is not silently included in the seven above.

   Ticket: `<page>/runs/rp-para-NN_Pxx[-Pyy].md`. Paired Result directory: `<page>/results/<same-rp>/`, containing `runtime.yaml`, `working.md`, and append-only `v001.md`. Working candidates remain in the selected `outline/<stem>-outline-v*.md` Draft fields. Capture the person's verbatim feedback before editing and append the completed draft/review/diagnose/revise result to that Step. Several feedback turns do not create several Runs; an unfinished clarification is a pending Step. Ordinary waiting uses `waiting-for-feedback`; interrupted partial work is Held.

   Exact acceptance records who, words, target, source Version/Step and accepted text/hash. All commissioned targets, Bullets and required evidence must settle before RP closure; accepted wording with evidence still owed keeps the RP open. Seal the Version at closure. Reopening the same fixed paragraph target creates `v002.md` referencing the closed v001 hash; changed scope or goal requires a new Run. Page CONTENT adoption and whole-Page CHECK occur later under their own authority. Neither Task success nor RP acceptance closes the whole Page.

5. **Audit and reuse the recorded Design chain.** The Design Folder owns all three native Runs, with a Folder-wide `rdNN` counter. Commission is a human release/hold decision for the exact frozen item config. The Design plugin/caller records its authority and receipt; Generate and Verify use the selected `haipipe-design-unit` worker. The Verify actor must be independent of the producer.

   Tickets: `<design>/runs/rdNN_{commission|generate|verify}_<item-slug>.yaml`; Results: `<design>/results/<same-stem>/`. Commission emits `decision.yaml`; Generate emits `result.yaml`, `checks.yaml`, `content/`, and optional `render/` with hash-bound manifest. Verify pins the exact Generate Result/hash and provides coverage and verdict. The caller owns `runtime.yaml`; workers write their paired Results but not runtime or human decisions.

   Check the released Commission/config, the generation's records gate, and the exact independent Verify receipt before endorsing Delivery. Display alone is not proof. Delivery reads the accepted Generate/Verify identities and artifacts; it allocates neither an Adopt Run nor a Delivery Run. A new revision would require a new Generate commission, but none is requested here.

6. **Inspect the Insight RI's two publications and pin consumers explicitly.** The Insight instance owner preserves the immutable RI binding to base-R Ticket/hash, frozen dataset, question, target and acceptance. Ticket: `<instance>/runs/riNN_<slug>.yaml`; intent: `<instance>/workflow/insight.yaml`; publications: `<instance>/results/<ri>/v001/` and `v002/`, each with `input.yaml`, `runtime.yaml`, `result.yaml` and required review/checkpoint records.

   List one logical inventory row, `<instance>#<ri>`, and two exact execution records, `<instance>#<ri>@v001` and `@v002`. Accepted findings require an independent review of the exact candidate/input, not merely publication presence. A consumer pins version, finding, Result path/hash and base-R pointer, never `latest`. Existing v001 pins stay intact when v002 appears, but current-use applicability must be rechecked; a failed v002 does not erase an intact historical v001. No new Insight work is implied here.

These can be represented by Specs for fit, first-Page VALUE, first-Page paragraph, and the three existing Design operations, plus an indexed RI binding with its version history. The fit is reused as both Pages' dependency; the VALUE depends on its accepted Result. Paragraph drafting may use visible evidence placeholders once Structure is closed, but its final close waits on the required evidence. Design and Insight inspection have no assumed dependency on the fit. An aggregate Workflow runtime is useful for retry/resume and human waiting, but its owner chooses its storage; it adds no Run and cannot substitute for child receipts.

At explicit RP close, the Page protocol calls for one post-run analysis proposal. Keep it outside the core count until actually commissioned and allocated. That output-only work has its own native `rNN_page-run-analysis.md` and `results/<same-run>/{runtime.yaml,result.md}` under the Page profile. It reads the sealed journal by hash and cannot edit accepted prose or policies. The post-run reference requires approval before heavy background dispatch; no such dispatch or proposal after each feedback turn is implied here.

## A Ticket with no runtime receipt

Enumerate the union of native Tickets and Results/receipts, join by full owner-qualified identity, and keep the orphan row visible as **Needs attention / incomplete allocation: runtime receipt missing**. It is neither a valid Ready allocation nor completed work. Keep incomplete records in their own count and show their exact owner and next action.

First resolve the declared current and historical stores to distinguish a missing file from a path error. Preserve any surviving Result, logs and frozen inputs. If the receipt is genuinely absent, route repair through its native owner; the presenter must not create one on read. The owner may scaffold a truthful planned receipt only where it can establish no work started, using its native writer and actual timestamps. For attempted or ambiguous work, recover lifecycle facts from durable records through the owner's recovery procedure and leave unknown facts null. Do not invent success, failure, attempts or timestamps, hand-edit a Task-owned receipt, launch a worker merely to repair an inventory view, or allocate a new identity merely to hide the orphan. Hold dispatch if safe owner recovery is unresolved.

## Contract discrepancies and unresolved requirements

- **Task retry execution is not yet safe using the standard template alone.** The shared history contract requires preserving failed attempts before unchanged-contract retry. The inspected `ref/run-sh-template.sh` writes a replacement `runtime.yaml` at launch and termination and supplies no attempt archive/journal. The semantic decision is clear—reuse the fit Run—but the real owner must identify an existing history adapter or supply supported preservation before dispatch. I cannot claim this capability from the template.
- **Task Result paths disagree in Page references.** Page EVIDENCE and parts of `item-table.md` still show `$OUTPUT_ROOT/results/<task>/<run>`; the current Task runner, template, runtime schema and updated Task/Page evidence binding use `$OUTPUT_ROOT/<task>/results/<run>`. Follow the actual owner resolver for this plan and preserve historical recorded paths. This does not prevent logical counting; it prevents safely guessing an unseen project's physical path.
- **The Task/Page closure reference still illustrates retired compact RP names.** The selected canonical Page-family/interactive contracts explicitly reserve those for historical input and require typed `rp-para-NN_Pxx` identities for new work. I used the canonical typed profile. The closure handshake remains usable.
- **An executable Workflow cannot be frozen from this scenario alone.** The shared Run contract makes Workspace Cell binding conditional on declared Workspaces, while the Workflow skill's PLAN/validation language expects a Plugin roster and Cells. No roster is supplied. The conceptual graph can be stated without inventing a Plugin; formal freeze needs the owning Workflow's applicable workspace contract resolved.
- Missing project facts include actual owner folders, selected Workflow/Spec, Ticket/config/data/Result hashes, retry mechanism, Page target paragraph and closed Structure evidence, authored VALUE acceptance/decision, named human/worker actors, and native receipts. Thus concrete next IDs, actual statuses, full inventory coverage, current-use Insight acceptance and the claimed Design Delivery validity remain unverified. These gaps do not justify running workers or inventing records.

Only the contracts needed for this scenario were read. No evaluation reports/plans or parent-task history were read.
