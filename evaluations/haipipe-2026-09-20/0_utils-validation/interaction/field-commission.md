# Prepared commission; not dispatched

Mode: copilot. Execution state: NOT RELEASED; this validation only prepares the packet. The explicit instruction is “Do not execute the FIELD run.”

The requested isolated target is synthetic and falls short of field-test's “live artifact with history and debt” requirement. This is a bounded interaction rehearsal, not proof of a full real-task field run.

## Pasteable FIELD packet

LOAD /remote-error and /response-format from their shipped files. Read /field-test for the friction and receipt contract. Do not load any expectation ledger, design notes, previous turns or intended fixes.

Target: `/tmp/interaction-validation-lb40b3lu/report.py` and `/tmp/interaction-validation-lb40b3lu/supplied-failure.txt`; the only report directory is `/tmp/interaction-validation-lb40b3lu/register`. The fixture currently includes a previous local empty-input edit and its report. Preserve them. Do not reset the fixture to manufacture a new fix. Validate the existing report and state whether the supplied trace applies to the current source.

Authorization: preserve the exact supplied task instruction for bounded /tmp fixture inspection and report work. It is recorded in `/tmp/interaction-validation-lb40b3lu/authorization-evidence.md` as direct task scope, not an Insight grant. If a real target family grant is supplied with the commission, resolve its exact durable source, person, board/runtime, targets, permitted actions, expiry and revocation under that family's owner contract. Reuse covered authorization without asking the person to repeat it. Do not transplant Insight's schema to this generic Python register. No such live grant has been supplied here, so no live granted action is commissioned.

1. Inspect the canonical source header and trace; identify KIND, unit, step and exact error. Use generic mode unless explicit evidence establishes a profile.
2. Distinguish the original failing line from the current guarded line. Preserve previous evidence. Make only a repair that is still necessary and authorized; never claim an edit that did not occur.
3. Write or update only the supplied Markdown issue report using remote-error's report template. Preserve its Session label and use N/A for an unknown remote Run ID. Do not invent a register index, daily file, status file, project gate or checker rule.
4. Report the exact paths and actual local checks. The remote-error reply layout takes precedence when it is active; response-format does not change the report's Markdown headings. No local result proves a remote rerun.
5. Keep a numbered FRICTION LOG: every place a skill was unclear, wrong, self-contradictory, or missing a rule you needed. That log matters more than the work. Each row must identify the source file and sentence or missing rule; every timestamp comes from `date`. Record start and Close/end from `date`, actual checks, artifact paths, and receipt-backed cost if available. Unavailable token receipts remain unavailable.

Gates: preserve mode: copilot and each owner gate. Already granted operations require no repeated approval. New computation release, handoff signatures and out-of-scope work require their ordinary owner decision; the packet grants none. Do not execute Python or a FIELD Run under an empty run_ids field.

Scope fences: repository sources are read-only because this is fresh-context validation; all generated artifacts stay under this temporary directory to protect real work. No external services, network, providers, browser/OAuth, credentials or real user data because the commission is isolated. No skill edits during execution because the baseline must remain fixed. No auxiliary agents: only this future FIELD packet may be dispatched if separately released.

STOP after step 5 and return the report, numbered friction log, exact receipts and untested limits. Do not continue into other tasks or initiate a second run.
